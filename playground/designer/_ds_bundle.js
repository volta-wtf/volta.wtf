/* @ds-bundle: {"format":4,"namespace":"DesignerDesignSystem_6adbd8","components":[{"name":"ActionAlign","sourcePath":"components/actions/ActionAlign.jsx"},{"name":"ActionFill","sourcePath":"components/actions/ActionFill.jsx"},{"name":"ActionOpacity","sourcePath":"components/actions/ActionOpacity.jsx"},{"name":"ActionRotate","sourcePath":"components/actions/ActionRotate.jsx"},{"name":"AvatarStack","sourcePath":"components/collab/AvatarStack.jsx"},{"name":"CommentPin","sourcePath":"components/collab/CommentPin.jsx"},{"name":"CursorChip","sourcePath":"components/collab/CursorChip.jsx"},{"name":"CommandPalette","sourcePath":"components/commands/CommandPalette.jsx"},{"name":"ShortcutsOverlay","sourcePath":"components/commands/ShortcutsOverlay.jsx"},{"name":"BoundField","sourcePath":"components/data/BoundField.jsx"},{"name":"CollectionList","sourcePath":"components/data/CollectionList.jsx"},{"name":"CollectionTable","sourcePath":"components/data/CollectionTable.jsx"},{"name":"DataSourcePicker","sourcePath":"components/data/DataSourcePicker.jsx"},{"name":"Canvas","sourcePath":"components/designer/Canvas.jsx"},{"name":"Filmstrip","sourcePath":"components/designer/Filmstrip.jsx"},{"name":"FloatingToolbar","sourcePath":"components/designer/FloatingToolbar.jsx"},{"name":"Frame","sourcePath":"components/designer/Frame.jsx"},{"name":"LayerTree","sourcePath":"components/designer/LayerTree.jsx"},{"name":"Menubar","sourcePath":"components/designer/Menubar.jsx"},{"name":"PageList","sourcePath":"components/designer/PageList.jsx"},{"name":"Pane","sourcePath":"components/designer/Pane.jsx"},{"name":"Panel","sourcePath":"components/designer/Panel.jsx"},{"name":"StatusBar","sourcePath":"components/designer/StatusBar.jsx"},{"name":"Toolbar","sourcePath":"components/designer/Toolbar.jsx"},{"name":"ZoomControls","sourcePath":"components/designer/ZoomControls.jsx"},{"name":"BreakpointSwitcher","sourcePath":"components/devices/BreakpointSwitcher.jsx"},{"name":"DEVICE_SIZES","sourcePath":"components/devices/DeviceFrame.jsx"},{"name":"DeviceFrame","sourcePath":"components/devices/DeviceFrame.jsx"},{"name":"BooleanField","sourcePath":"components/fields/BooleanField.jsx"},{"name":"ColorField","sourcePath":"components/fields/ColorField.jsx"},{"name":"NumberField","sourcePath":"components/fields/NumberField.jsx"},{"name":"PositionField","sourcePath":"components/fields/PositionField.jsx"},{"name":"SelectField","sourcePath":"components/fields/SelectField.jsx"},{"name":"TextField","sourcePath":"components/fields/TextField.jsx"},{"name":"Icon","sourcePath":"components/icons/Icon.jsx"},{"name":"ICON_ROLE_GROUPS","sourcePath":"components/icons/roles.js"},{"name":"ICON_ROLES","sourcePath":"components/icons/roles.js"},{"name":"AssetGrid","sourcePath":"components/library/AssetGrid.jsx"},{"name":"LibraryItem","sourcePath":"components/library/LibraryItem.jsx"},{"name":"ConnectionLayer","sourcePath":"components/prototype/ConnectionLayer.jsx"},{"name":"InteractionEditor","sourcePath":"components/prototype/InteractionEditor.jsx"},{"name":"TokenList","sourcePath":"components/tokens/TokenList.jsx"},{"name":"TokenRow","sourcePath":"components/tokens/TokenRow.jsx"},{"name":"TokenSwatch","sourcePath":"components/tokens/TokenSwatch.jsx"},{"name":"PRESENCE_COLORS","sourcePath":"components/ui/Avatar.jsx"},{"name":"Avatar","sourcePath":"components/ui/Avatar.jsx"},{"name":"Badge","sourcePath":"components/ui/Badge.jsx"},{"name":"Button","sourcePath":"components/ui/Button.jsx"},{"name":"Checkbox","sourcePath":"components/ui/Checkbox.jsx"},{"name":"Dialog","sourcePath":"components/ui/Dialog.jsx"},{"name":"IconButton","sourcePath":"components/ui/IconButton.jsx"},{"name":"Input","sourcePath":"components/ui/Input.jsx"},{"name":"Kbd","sourcePath":"components/ui/Kbd.jsx"},{"name":"Label","sourcePath":"components/ui/Label.jsx"},{"name":"Menu","sourcePath":"components/ui/Menu.jsx"},{"name":"Select","sourcePath":"components/ui/Select.jsx"},{"name":"Separator","sourcePath":"components/ui/Separator.jsx"},{"name":"Slider","sourcePath":"components/ui/Slider.jsx"},{"name":"Switch","sourcePath":"components/ui/Switch.jsx"},{"name":"Tabs","sourcePath":"components/ui/Tabs.jsx"},{"name":"Toast","sourcePath":"components/ui/Toast.jsx"},{"name":"Tooltip","sourcePath":"components/ui/Tooltip.jsx"},{"name":"FilterStack","sourcePath":"components/utilities/FilterStack.jsx"},{"name":"GradientCss","sourcePath":"components/utilities/GradientEditor.jsx"},{"name":"GradientEditor","sourcePath":"components/utilities/GradientEditor.jsx"},{"name":"ShapeLibrary","sourcePath":"components/utilities/ShapeLibrary.jsx"}],"sourceHashes":{"components/actions/ActionAlign.jsx":"a35b27939b74","components/actions/ActionFill.jsx":"358d5be011ae","components/actions/ActionOpacity.jsx":"d2e647addd8e","components/actions/ActionRotate.jsx":"1a996571b596","components/collab/AvatarStack.jsx":"7f5a53bec936","components/collab/CommentPin.jsx":"73329b90d6a5","components/collab/CursorChip.jsx":"26535278284d","components/commands/CommandPalette.jsx":"655e28379d7d","components/commands/ShortcutsOverlay.jsx":"14d515704a2e","components/data/BoundField.jsx":"b33f1ae656d0","components/data/CollectionList.jsx":"8b5a02ba4a4f","components/data/CollectionTable.jsx":"03d2678df481","components/data/DataSourcePicker.jsx":"2dc76dab0e1d","components/designer/Canvas.jsx":"6a6ce1a6b8aa","components/designer/Filmstrip.jsx":"f4b76af696e4","components/designer/FloatingToolbar.jsx":"18c078ab4c89","components/designer/Frame.jsx":"0a8119985e28","components/designer/LayerTree.jsx":"53ebd41ad5bb","components/designer/Menubar.jsx":"38129ff00240","components/designer/PageList.jsx":"794f0d6ff55c","components/designer/Pane.jsx":"96e264e227c2","components/designer/Panel.jsx":"ba9a72b74738","components/designer/StatusBar.jsx":"75d46bacbc47","components/designer/Toolbar.jsx":"106d59949c7c","components/designer/ZoomControls.jsx":"93c578693d09","components/devices/BreakpointSwitcher.jsx":"fbec81394d29","components/devices/DeviceFrame.jsx":"90bcb36b404c","components/fields/BooleanField.jsx":"7aa21e6f1fb6","components/fields/ColorField.jsx":"a9ac06ccdb11","components/fields/NumberField.jsx":"4ecf5d3f5e75","components/fields/PositionField.jsx":"a73109d31f03","components/fields/SelectField.jsx":"67dd40138241","components/fields/TextField.jsx":"62a678b78747","components/icons/Icon.jsx":"1810455da2ad","components/icons/roles.js":"751677039941","components/library/AssetGrid.jsx":"fbf37d38feac","components/library/LibraryItem.jsx":"673decb92347","components/prototype/ConnectionLayer.jsx":"fd0076e87688","components/prototype/InteractionEditor.jsx":"420ab57f0084","components/tokens/TokenList.jsx":"de93d0a33885","components/tokens/TokenRow.jsx":"7366ac8ccb9a","components/tokens/TokenSwatch.jsx":"0dab2b5cf77e","components/ui/Avatar.jsx":"e75d28022fc8","components/ui/Badge.jsx":"5623d43fe80b","components/ui/Button.jsx":"a32d1637fc8f","components/ui/Checkbox.jsx":"fd954368b0ed","components/ui/Dialog.jsx":"a034fd289095","components/ui/IconButton.jsx":"304eb25eaa92","components/ui/Input.jsx":"9eb0c3dace69","components/ui/Kbd.jsx":"304d36d868cc","components/ui/Label.jsx":"eeb39fc19510","components/ui/Menu.jsx":"de62a2135e52","components/ui/Select.jsx":"d418c4b612e8","components/ui/Separator.jsx":"646c723e7af2","components/ui/Slider.jsx":"83c3ce178320","components/ui/Switch.jsx":"37f1fed6386f","components/ui/Tabs.jsx":"6bae4ca00a04","components/ui/Toast.jsx":"0f29077cc899","components/ui/Tooltip.jsx":"844fe84a9de2","components/utilities/FilterStack.jsx":"d16ed3a7cc0c","components/utilities/GradientEditor.jsx":"ab2a7f17cfde","components/utilities/ShapeLibrary.jsx":"95df32367f9d","ui_kits/editor/app.jsx":"ced9bfe00d61","ui_kits/editor/chrome-theme.jsx":"9f102e021ac3","ui_kits/editor/data.js":"3a73310d0e80","ui_kits/editor/inspector-panes.jsx":"c37ff62eaba7","ui_kits/editor/project-components.jsx":"5a3dfe65899e","ui_kits/editor/screen-board-slides.jsx":"974fedd7c9a6","ui_kits/editor/screen-buzz-make.jsx":"49527a1fccb8","ui_kits/editor/screen-design.jsx":"d84bbc923e9f","ui_kits/editor/screen-dev.jsx":"f3d028a32d0d","ui_kits/editor/screen-sites.jsx":"bda360447d9b","ui_kits/editor/shell.jsx":"6058700ebc84","ui_kits/editor/store.js":"5703ef3de6bc","ui_kits/editor/theme-panel.jsx":"05a39082aaca"},"inlinedExternals":[],"unexposedExports":[{"name":"presenceColor","sourcePath":"components/ui/Avatar.jsx"}]} */

(() => {

const __ds_ns = (window.DesignerDesignSystem_6adbd8 = window.DesignerDesignSystem_6adbd8 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/designer/Canvas.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const clampZoom = z => Math.min(8, Math.max(0.1, z));

/**
 * Canvas — infinite pan/zoom surface with dot grid.
 * Wheel = pan · ⌘/ctrl+wheel (or pinch) = zoom to cursor · drag background = pan.
 * Semi-controlled: pass `view` + `onViewChange`, or let it own its state.
 */
function Canvas({
  view,
  defaultView = {
    zoom: 1,
    x: 0,
    y: 0
  },
  onViewChange,
  grid = true,
  gridSize = 16,
  panOnDrag = true,
  onBackgroundPointerDown,
  hud,
  className = '',
  style,
  children,
  ...rest
}) {
  const controlled = view !== undefined;
  const [inner, setInner] = React.useState(defaultView);
  const v = controlled ? view : inner;
  const vRef = React.useRef(v);
  vRef.current = v;
  const [panning, setPanning] = React.useState(false);
  const ref = React.useRef(null);
  const set = next => {
    if (!controlled) setInner(next);
    if (onViewChange) onViewChange(next);
  };
  const setRef = React.useRef(set);
  setRef.current = set;
  React.useEffect(() => {
    const el = ref.current;
    if (!el) return undefined;
    const onWheel = e => {
      e.preventDefault();
      const cur = vRef.current;
      if (e.ctrlKey || e.metaKey) {
        const rect = el.getBoundingClientRect();
        const px = e.clientX - rect.left;
        const py = e.clientY - rect.top;
        const zoom = clampZoom(cur.zoom * Math.exp(-e.deltaY * 0.0018));
        const k = zoom / cur.zoom;
        setRef.current({
          zoom,
          x: px - (px - cur.x) * k,
          y: py - (py - cur.y) * k
        });
      } else {
        setRef.current({
          ...cur,
          x: cur.x - e.deltaX,
          y: cur.y - e.deltaY
        });
      }
    };
    el.addEventListener('wheel', onWheel, {
      passive: false
    });
    return () => el.removeEventListener('wheel', onWheel);
  }, []);
  const startPan = e => {
    if (onBackgroundPointerDown) onBackgroundPointerDown(e);
    if (!panOnDrag || e.button !== 0 && e.button !== 1) return;
    const start = vRef.current;
    const sx = e.clientX;
    const sy = e.clientY;
    setPanning(true);
    const move = ev => setRef.current({
      ...vRef.current,
      x: start.x + ev.clientX - sx,
      y: start.y + ev.clientY - sy
    });
    const up = () => {
      setPanning(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  const cls = ['dsr-canvas', grid ? '' : 'dsr-canvas--no-grid', panning ? 'dsr-canvas--panning' : '', className].filter(Boolean).join(' ');
  const gridStyle = grid ? {
    backgroundSize: `${gridSize * v.zoom}px ${gridSize * v.zoom}px`,
    backgroundPosition: `${v.x}px ${v.y}px`
  } : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    ref: ref,
    className: cls,
    style: {
      ...gridStyle,
      ...style
    },
    onPointerDown: startPan
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "dsr-canvas__world",
    style: {
      transform: `translate(${v.x}px, ${v.y}px) scale(${v.zoom})`
    }
  }, children), hud);
}
Object.assign(__ds_scope, { Canvas });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/Canvas.jsx", error: String((e && e.message) || e) }); }

// components/designer/Filmstrip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Filmstrip — numbered slide thumbnails; `renderThumb` draws the preview. */
function Filmstrip({
  slides = [],
  selectedId,
  onSelect,
  renderThumb,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-filmstrip ${className}`.trim()
  }, rest), slides.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: s.id,
    className: `dsr-filmstrip__item${selectedId === s.id ? ' is-selected' : ''}`,
    onClick: () => onSelect && onSelect(s.id)
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-filmstrip__num"
  }, i + 1), /*#__PURE__*/React.createElement("div", {
    className: "dsr-filmstrip__thumb"
  }, renderThumb ? renderThumb(s, i) : null), s.name ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-filmstrip__name"
  }, s.name) : null)));
}
Object.assign(__ds_scope, { Filmstrip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/Filmstrip.jsx", error: String((e && e.message) || e) }); }

// components/designer/FloatingToolbar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** FloatingToolbar — blurred pill floating over the canvas, bottom-center. */
function FloatingToolbar({
  position = 'bottom',
  className = '',
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "toolbar",
    className: `dsr-floatbar ${position === 'top' ? 'dsr-floatbar--top' : ''} ${className}`.trim()
  }, rest), children);
}
Object.assign(__ds_scope, { FloatingToolbar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/FloatingToolbar.jsx", error: String((e && e.message) || e) }); }

// components/designer/Frame.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Frame — an artboard in canvas world coordinates.
 * Click selects · drag moves (if onMove) · corner handles resize (if onResize).
 * Pass `zoom` so move/resize deltas stay correct at any zoom level.
 */
function Frame({
  x = 0,
  y = 0,
  width = 320,
  height = 240,
  name,
  selected = false,
  zoom = 1,
  clip = true,
  fill,
  onSelect,
  onMove,
  onResize,
  className = '',
  style,
  children,
  ...rest
}) {
  const startMove = e => {
    if (e.button !== 0) return;
    e.stopPropagation();
    if (onSelect) onSelect(e);
    if (!onMove) return;
    const sx = e.clientX;
    const sy = e.clientY;
    let moved = false;
    const move = ev => {
      const dx = (ev.clientX - sx) / zoom;
      const dy = (ev.clientY - sy) / zoom;
      if (!moved && Math.abs(dx) < 2 && Math.abs(dy) < 2) return;
      moved = true;
      onMove({
        x: Math.round(x + dx),
        y: Math.round(y + dy)
      });
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  const startResize = corner => e => {
    if (e.button !== 0 || !onResize) return;
    e.stopPropagation();
    e.preventDefault();
    const sx = e.clientX;
    const sy = e.clientY;
    const move = ev => {
      const dx = (ev.clientX - sx) / zoom;
      const dy = (ev.clientY - sy) / zoom;
      let nx = x,
        ny = y,
        nw = width,
        nh = height;
      if (corner.includes('e')) nw = width + dx;
      if (corner.includes('s')) nh = height + dy;
      if (corner.includes('w')) {
        nw = width - dx;
        nx = x + dx;
      }
      if (corner.includes('n')) {
        nh = height - dy;
        ny = y + dy;
      }
      if (nw >= 8 && nh >= 8) onResize({
        x: Math.round(nx),
        y: Math.round(ny),
        width: Math.round(nw),
        height: Math.round(nh)
      });
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    "data-canvas-item": true,
    className: `dsr-frame ${selected ? 'is-selected' : ''} ${clip ? '' : 'dsr-frame--no-clip'} ${className}`.trim(),
    style: {
      left: x,
      top: y,
      width,
      height,
      background: fill,
      ...style
    },
    onPointerDown: startMove
  }, rest), name ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-frame__label"
  }, name) : null, /*#__PURE__*/React.createElement("div", {
    className: "dsr-frame__content"
  }, children), selected ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "dsr-frame__handle dsr-frame__handle--nw",
    onPointerDown: startResize('nw')
  }), /*#__PURE__*/React.createElement("span", {
    className: "dsr-frame__handle dsr-frame__handle--ne",
    onPointerDown: startResize('ne')
  }), /*#__PURE__*/React.createElement("span", {
    className: "dsr-frame__handle dsr-frame__handle--sw",
    onPointerDown: startResize('sw')
  }), /*#__PURE__*/React.createElement("span", {
    className: "dsr-frame__handle dsr-frame__handle--se",
    onPointerDown: startResize('se')
  })) : null);
}
Object.assign(__ds_scope, { Frame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/Frame.jsx", error: String((e && e.message) || e) }); }

// components/designer/PageList.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** PageList — document pages (above the layer tree). */
function PageList({
  pages = [],
  selectedId,
  onSelect,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-pagelist ${className}`.trim()
  }, rest), pages.map(p => /*#__PURE__*/React.createElement("div", {
    key: p.id,
    className: `dsr-page-row${selectedId === p.id ? ' is-selected' : ''}`,
    onClick: () => onSelect && onSelect(p.id)
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-page-row__dot"
  }), /*#__PURE__*/React.createElement("span", null, p.name))));
}
Object.assign(__ds_scope, { PageList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/PageList.jsx", error: String((e && e.message) || e) }); }

// components/designer/Panel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Panel — docked side panel: optional header, scrollable body, optional footer. */
function Panel({
  side = 'left',
  header,
  footer,
  width,
  className = '',
  style,
  children,
  ...rest
}) {
  const s = width ? {
    ...style,
    width,
    minWidth: width
  } : style;
  return /*#__PURE__*/React.createElement("aside", _extends({
    className: `dsr-panel dsr-panel--${side} ${className}`.trim(),
    style: s
  }, rest), header ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-panel__header"
  }, header) : null, /*#__PURE__*/React.createElement("div", {
    className: "dsr-panel__body"
  }, children), footer ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-panel__footer"
  }, footer) : null);
}
Object.assign(__ds_scope, { Panel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/Panel.jsx", error: String((e && e.message) || e) }); }

// components/designer/StatusBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** StatusBar — bottom chrome strip: save state, selection info, coords/zoom. */
function StatusBar({
  saving = false,
  savedText = 'Saved',
  selection,
  right,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("footer", _extends({
    className: `dsr-statusbar ${className}`.trim()
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "dsr-statusbar__side"
  }, /*#__PURE__*/React.createElement("span", {
    className: `dsr-statusbar__dot${saving ? ' is-saving' : ''}`
  }), /*#__PURE__*/React.createElement("span", null, saving ? 'Saving…' : savedText)), selection ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-statusbar__side"
  }, selection) : null, /*#__PURE__*/React.createElement("span", {
    className: "dsr-statusbar__side dsr-statusbar__mono"
  }, right));
}
Object.assign(__ds_scope, { StatusBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/StatusBar.jsx", error: String((e && e.message) || e) }); }

// components/designer/Toolbar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Toolbar — docked strip of IconButtons; separate groups with <Separator>. */
function Toolbar({
  orientation = 'horizontal',
  className = '',
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "toolbar",
    "aria-orientation": orientation,
    className: `dsr-toolbar dsr-toolbar--${orientation} ${className}`.trim()
  }, rest), children);
}
Object.assign(__ds_scope, { Toolbar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/Toolbar.jsx", error: String((e && e.message) || e) }); }

// components/devices/DeviceFrame.jsx
try { (() => {
const DEVICE_SIZES = {
  'phone-sm': {
    width: 320,
    height: 568,
    class: 'phone'
  },
  phone: {
    width: 390,
    height: 844,
    class: 'phone'
  },
  'phone-lg': {
    width: 430,
    height: 932,
    class: 'phone'
  },
  tablet: {
    width: 768,
    height: 1024,
    class: 'tablet'
  },
  'tablet-lg': {
    width: 834,
    height: 1194,
    class: 'tablet'
  },
  laptop: {
    width: 1280,
    height: 832,
    class: 'laptop'
  },
  'laptop-lg': {
    width: 1440,
    height: 900,
    class: 'laptop'
  },
  desktop: {
    width: 1920,
    height: 1080,
    class: 'desktop'
  },
  watch: {
    width: 184,
    height: 224,
    class: 'watch'
  }
};

/**
 * DeviceFrame — generic neutral device bezel around screen content.
 * `device` is an id from data/devices.json (or {width, height, class}).
 * Content renders at TRUE device size, scaled by `scale`.
 */
function DeviceFrame({
  device = 'phone',
  scale = 0.35,
  url,
  label,
  className = '',
  style,
  children
}) {
  const d = typeof device === 'string' ? DEVICE_SIZES[device] || DEVICE_SIZES.phone : device;
  const cls = d.class || 'phone';
  const isBrowser = cls === 'laptop' || cls === 'desktop';
  return /*#__PURE__*/React.createElement("figure", {
    className: `dsr-device ${className}`.trim(),
    style: {
      margin: 0,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: `dsr-device__shell dsr-device__shell--${cls}`,
    style: {
      transform: `scale(${scale})`,
      transformOrigin: 'top center',
      marginBottom: (d.height + (isBrowser ? 28 : 24)) * (scale - 1)
    }
  }, isBrowser ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-device__browserbar"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-device__dots"
  }, /*#__PURE__*/React.createElement("i", {
    className: "dsr-device__dot"
  }), /*#__PURE__*/React.createElement("i", {
    className: "dsr-device__dot"
  }), /*#__PURE__*/React.createElement("i", {
    className: "dsr-device__dot"
  })), /*#__PURE__*/React.createElement("span", {
    className: "dsr-device__url"
  }, url || 'preview.local'), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 33
    }
  })) : null, /*#__PURE__*/React.createElement("div", {
    className: "dsr-device__screen",
    style: {
      width: d.width,
      height: d.height
    }
  }, children)), label ? /*#__PURE__*/React.createElement("figcaption", {
    className: "dsr-device__label"
  }, label) : null);
}
Object.assign(__ds_scope, { DEVICE_SIZES, DeviceFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/devices/DeviceFrame.jsx", error: String((e && e.message) || e) }); }

// components/fields/ColorField.jsx
try { (() => {
const isHex = s => /^#([0-9a-f]{3}|[0-9a-f]{6})$/i.test(s);

/** ColorField — swatch (native picker) + hex text + optional alpha %. */
function ColorField({
  value,
  defaultValue = '#1a1a1a',
  onChange,
  alpha,
  onAlphaChange,
  className = '',
  style
}) {
  const controlled = value !== undefined;
  const [inner, setInner] = React.useState(defaultValue);
  const [text, setText] = React.useState(null);
  const val = controlled ? value : inner;
  const set = c => {
    if (!controlled) setInner(c);
    if (onChange) onChange(c);
  };
  const commit = t => {
    let c = t.trim().toLowerCase();
    if (c && c[0] !== '#') c = '#' + c;
    if (isHex(c)) set(c);
    setText(null);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-colorfield ${className}`.trim(),
    style: style
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-colorfield__swatch",
    style: {
      background: val
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "color",
    value: isHex(val) ? val : '#000000',
    onChange: e => set(e.target.value),
    "aria-label": "Pick color"
  })), /*#__PURE__*/React.createElement("input", {
    className: "dsr-colorfield__hex",
    type: "text",
    spellCheck: false,
    value: text != null ? text : val,
    onChange: e => setText(e.target.value),
    onBlur: e => commit(e.target.value),
    onKeyDown: e => {
      if (e.key === 'Enter') {
        commit(e.currentTarget.value);
        e.currentTarget.blur();
      }
    },
    "aria-label": "Hex color"
  }), alpha !== undefined ? /*#__PURE__*/React.createElement("input", {
    className: "dsr-colorfield__alpha",
    type: "text",
    inputMode: "numeric",
    value: `${alpha}%`,
    onChange: e => {
      const n = parseInt(e.target.value, 10);
      if (!Number.isNaN(n) && onAlphaChange) onAlphaChange(Math.max(0, Math.min(100, n)));
    },
    "aria-label": "Opacity"
  }) : null);
}
Object.assign(__ds_scope, { ColorField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/fields/ColorField.jsx", error: String((e && e.message) || e) }); }

// components/actions/ActionFill.jsx
try { (() => {
const SWATCHES = ['#1a1a1a', '#ffffff', '#d97757', '#4864c8', '#3d8a5f', '#c8b64a'];

/** ActionFill — fill property action: color + quick swatches. */
function ActionFill({
  value,
  defaultValue = '#1a1a1a',
  onChange,
  alpha,
  onAlphaChange,
  swatches = SWATCHES,
  className = '',
  style
}) {
  const controlled = value !== undefined;
  const [inner, setInner] = React.useState(defaultValue);
  const val = controlled ? value : inner;
  const set = c => {
    if (!controlled) setInner(c);
    if (onChange) onChange(c);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: className,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ColorField, {
    value: val,
    onChange: set,
    alpha: alpha,
    onAlphaChange: onAlphaChange
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4
    }
  }, swatches.map(c => /*#__PURE__*/React.createElement("button", {
    key: c,
    type: "button",
    title: c,
    onClick: () => set(c),
    style: {
      width: 18,
      height: 18,
      padding: 0,
      cursor: 'pointer',
      borderRadius: 4,
      border: 'none',
      background: c,
      boxShadow: val === c ? '0 0 0 1.5px var(--background), 0 0 0 3px var(--selection)' : 'inset 0 0 0 1px oklch(0 0 0 / 0.12)'
    }
  }))));
}
Object.assign(__ds_scope, { ActionFill });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ActionFill.jsx", error: String((e && e.message) || e) }); }

// components/icons/roles.js
try { (() => {
// ------------------------------------------------------------------
// Icon role registry — runtime copy of data/icon-roles.json.
// Semantic role → Lucide glyph (PascalCase, lucide@0.525.0).
// Components reference ROLES, never raw glyph names. Keep in sync
// with the JSON (the JSON is the documented source of truth).
// ------------------------------------------------------------------

const ICON_ROLE_GROUPS = {
  Tools: {
    select: 'MousePointer2',
    move: 'Move',
    hand: 'Hand',
    frame: 'Frame',
    rectangle: 'Square',
    ellipse: 'Circle',
    line: 'Minus',
    polygon: 'Pentagon',
    star: 'Star',
    pen: 'PenTool',
    pencil: 'Pencil',
    text: 'Type',
    image: 'Image',
    comment: 'MessageSquare',
    crop: 'Crop',
    eyedropper: 'Pipette',
    measure: 'Ruler',
    'zoom-in': 'ZoomIn',
    'zoom-out': 'ZoomOut'
  },
  Layers: {
    layers: 'Layers',
    group: 'Group',
    component: 'Component',
    shape: 'Shapes',
    visible: 'Eye',
    hidden: 'EyeOff',
    locked: 'Lock',
    unlocked: 'LockOpen',
    expand: 'ChevronRight',
    collapse: 'ChevronDown',
    drag: 'GripVertical'
  },
  Panels: {
    'panel-layers': 'Layers',
    'panel-assets': 'Blocks',
    'panel-tokens': 'SwatchBook',
    'panel-data': 'Database',
    'panel-plugins': 'Puzzle',
    pages: 'File',
    settings: 'Settings2',
    search: 'Search'
  },
  Fields: {
    'field-text': 'Type',
    'field-richtext': 'AlignLeft',
    'field-number': 'Hash',
    'field-boolean': 'ToggleLeft',
    'field-image': 'Image',
    'field-select': 'List',
    'field-date': 'Calendar',
    'field-color': 'Palette',
    'field-url': 'Link'
  },
  Data: {
    data: 'Database',
    collection: 'Table2',
    record: 'Rows3',
    bind: 'Link2',
    unbind: 'Unlink',
    refresh: 'RefreshCw'
  },
  Transform: {
    'align-left': 'AlignStartVertical',
    'align-center-h': 'AlignCenterVertical',
    'align-right': 'AlignEndVertical',
    'align-top': 'AlignStartHorizontal',
    'align-center-v': 'AlignCenterHorizontal',
    'align-bottom': 'AlignEndHorizontal',
    'distribute-h': 'AlignHorizontalDistributeCenter',
    'distribute-v': 'AlignVerticalDistributeCenter',
    rotate: 'RotateCw',
    'flip-h': 'FlipHorizontal2',
    'flip-v': 'FlipVertical2',
    radius: 'Radius'
  },
  Text: {
    bold: 'Bold',
    italic: 'Italic',
    underline: 'Underline',
    strikethrough: 'Strikethrough',
    'text-left': 'AlignLeft',
    'text-center': 'AlignCenter',
    'text-right': 'AlignRight',
    'line-height': 'UnfoldVertical',
    'letter-spacing': 'UnfoldHorizontal'
  },
  Properties: {
    fill: 'PaintBucket',
    stroke: 'Brush',
    opacity: 'Blend',
    gradient: 'Droplet',
    effects: 'Sparkles',
    adjust: 'SlidersHorizontal'
  },
  Devices: {
    'device-desktop': 'Monitor',
    'device-laptop': 'Laptop',
    'device-tablet': 'Tablet',
    'device-phone': 'Smartphone',
    'device-watch': 'Watch'
  },
  Prototype: {
    click: 'MousePointerClick',
    connection: 'Spline',
    transition: 'ArrowLeftRight',
    delay: 'Timer',
    easing: 'TrendingUp'
  },
  Chrome: {
    add: 'Plus',
    remove: 'Minus',
    close: 'X',
    check: 'Check',
    'chevron-up': 'ChevronUp',
    'chevron-down': 'ChevronDown',
    'chevron-left': 'ChevronLeft',
    'chevron-right': 'ChevronRight',
    'chevrons-updown': 'ChevronsUpDown',
    more: 'Ellipsis',
    menu: 'Menu',
    duplicate: 'Copy',
    delete: 'Trash2',
    edit: 'SquarePen',
    undo: 'Undo2',
    redo: 'Redo2',
    history: 'History',
    download: 'Download',
    upload: 'Upload',
    play: 'Play',
    link: 'Link',
    external: 'ExternalLink',
    info: 'Info',
    warning: 'TriangleAlert',
    error: 'CircleAlert',
    help: 'CircleHelp',
    keyboard: 'Keyboard',
    theme: 'SunMoon',
    sidebar: 'PanelLeft',
    inspector: 'PanelRight',
    'zoom-fit': 'Maximize',
    code: 'Code',
    grid: 'LayoutGrid',
    folder: 'Folder',
    'file-text': 'FileText',
    package: 'Package',
    users: 'Users',
    quote: 'Quote',
    calendar: 'Calendar',
    clock: 'Clock'
  }
};

// Flat map: role → glyph
const ICON_ROLES = Object.assign({}, ...Object.values(ICON_ROLE_GROUPS));
Object.assign(__ds_scope, { ICON_ROLE_GROUPS, ICON_ROLES });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/roles.js", error: String((e && e.message) || e) }); }

// components/icons/Icon.jsx
try { (() => {
const toPascal = s => String(s).split(/[-_ ]+/).map(w => w ? w[0].toUpperCase() + w.slice(1) : '').join('');
const renderNode = (node, i) => {
  const [tag, attrs, children] = node;
  return React.createElement(tag, {
    key: i,
    ...attrs
  }, Array.isArray(children) ? children.map(renderNode) : undefined);
};

/**
 * Icon — the ONLY way glyphs enter the UI.
 * Resolves a semantic role (preferred: "visible", "field-number") or a raw
 * Lucide name via the role registry, and renders inline SVG from the
 * lucide UMD global (load lucide@0.525.0 before use).
 * Unknown names render a dashed placeholder + console.warn, never crash.
 */
function Icon({
  name,
  size = 16,
  strokeWidth,
  title,
  style,
  className
}) {
  const lucide = typeof window !== 'undefined' ? window.lucide : null;
  const glyph = __ds_scope.ICON_ROLES[name] || name;
  const icons = lucide && lucide.icons || lucide || {};
  const node = icons[glyph] || icons[toPascal(glyph)];
  const sw = strokeWidth != null ? strokeWidth : size <= 12 ? 2 : 1.5;
  if (!node || !Array.isArray(node)) {
    if (typeof console !== 'undefined' && !icons[glyph]) {
      console.warn(`[Icon] unresolved icon "${name}" (glyph "${glyph}") — is lucide loaded?`);
    }
    return /*#__PURE__*/React.createElement("svg", {
      width: size,
      height: size,
      viewBox: "0 0 24 24",
      fill: "none",
      style: style,
      className: className,
      "aria-hidden": "true"
    }, /*#__PURE__*/React.createElement("rect", {
      x: "4",
      y: "4",
      width: "16",
      height: "16",
      rx: "3",
      stroke: "currentColor",
      strokeWidth: "1.5",
      strokeDasharray: "3 2"
    }));
  }
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: sw,
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: style,
    className: className,
    role: title ? 'img' : undefined,
    "aria-hidden": title ? undefined : 'true'
  }, title ? /*#__PURE__*/React.createElement("title", null, title) : null, node.map(renderNode));
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/Icon.jsx", error: String((e && e.message) || e) }); }

// components/data/BoundField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** BoundField — selection-blue chip marking a property bound to data. */
function BoundField({
  collection,
  field,
  onUnbind,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: `dsr-bound ${className}`.trim(),
    title: `Bound to ${collection}.${field}`
  }, rest), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "bind",
    size: 11
  }), collection ? `${collection}.` : '', field, onUnbind ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "dsr-bound__unbind",
    "aria-label": "Unbind",
    onClick: onUnbind
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "close",
    size: 11
  })) : null);
}
Object.assign(__ds_scope, { BoundField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/BoundField.jsx", error: String((e && e.message) || e) }); }

// components/data/CollectionList.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** CollectionList — the collections index (data/collections.json entries). */
function CollectionList({
  collections = [],
  selectedId,
  onSelect,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-collections ${className}`.trim()
  }, rest), collections.map(c => /*#__PURE__*/React.createElement("div", {
    key: c.id,
    className: `dsr-collection-row${selectedId === c.id ? ' is-selected' : ''}`,
    onClick: () => onSelect && onSelect(c.id)
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-collection-row__icon"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: c.icon || 'collection',
    size: 14
  })), /*#__PURE__*/React.createElement("span", {
    className: "dsr-collection-row__name"
  }, c.name), /*#__PURE__*/React.createElement("span", {
    className: "dsr-collection-row__count"
  }, c.count != null ? c.count : (c.records || []).length))));
}
Object.assign(__ds_scope, { CollectionList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/CollectionList.jsx", error: String((e && e.message) || e) }); }

// components/data/CollectionTable.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const FIELD_ICON = {
  text: 'field-text',
  richtext: 'field-richtext',
  number: 'field-number',
  boolean: 'field-boolean',
  image: 'field-image',
  select: 'field-select',
  date: 'field-date',
  color: 'field-color',
  url: 'field-url'
};
function Cell({
  field,
  value
}) {
  if (value == null) return /*#__PURE__*/React.createElement("span", {
    className: "dsr-datatable__bool"
  }, "\u2014");
  switch (field.type) {
    case 'image':
      return /*#__PURE__*/React.createElement("img", {
        className: "dsr-datatable__img",
        src: value,
        alt: "",
        loading: "lazy",
        onError: e => {
          e.target.style.visibility = 'hidden';
        }
      });
    case 'boolean':
      return /*#__PURE__*/React.createElement("span", {
        className: `dsr-datatable__bool${value ? ' is-true' : ''}`
      }, value ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
        name: "check",
        size: 13
      }) : '—');
    case 'number':
      return /*#__PURE__*/React.createElement("span", {
        className: "dsr-datatable__num"
      }, value, field.unit ? ` ${field.unit}` : '');
    case 'date':
      return /*#__PURE__*/React.createElement("span", {
        className: "dsr-datatable__num"
      }, value);
    default:
      return String(value);
  }
}

/** CollectionTable — a collection's records as a compact grid. */
function CollectionTable({
  collection,
  maxRecords,
  onRecordClick,
  className = '',
  ...rest
}) {
  if (!collection) return null;
  const records = maxRecords ? collection.records.slice(0, maxRecords) : collection.records;
  return /*#__PURE__*/React.createElement("table", _extends({
    className: `dsr-datatable ${className}`.trim()
  }, rest), /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, collection.fields.map(f => /*#__PURE__*/React.createElement("th", {
    key: f.key
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-datatable__field"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: FIELD_ICON[f.type] || 'field-text',
    size: 12
  }), f.label))))), /*#__PURE__*/React.createElement("tbody", null, records.map(r => /*#__PURE__*/React.createElement("tr", {
    key: r.id,
    onClick: onRecordClick ? () => onRecordClick(r) : undefined
  }, collection.fields.map(f => /*#__PURE__*/React.createElement("td", {
    key: f.key
  }, /*#__PURE__*/React.createElement(Cell, {
    field: f,
    value: r[f.key]
  })))))));
}
Object.assign(__ds_scope, { CollectionTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/CollectionTable.jsx", error: String((e && e.message) || e) }); }

// components/designer/Menubar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Menubar — the 48px top app bar. Three slots: left (identity + doc),
 * center (mode tabs / breakpoints), right (share, present, avatars).
 */
function Menubar({
  wordmark = 'Designer',
  doc,
  docMeta,
  left,
  center,
  right,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("header", _extends({
    className: `dsr-menubar ${className}`.trim()
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "dsr-menubar__side"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-menubar__wordmark"
  }, wordmark), doc ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "dsr-menubar__doc"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-menubar__doc-name"
  }, doc), docMeta ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-menubar__doc-meta"
  }, docMeta) : null, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron-down",
    size: 12
  })) : null, left), center ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-menubar__center"
  }, center) : null, /*#__PURE__*/React.createElement("div", {
    className: "dsr-menubar__side",
    style: {
      justifyContent: 'flex-end'
    }
  }, right));
}
Object.assign(__ds_scope, { Menubar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/Menubar.jsx", error: String((e && e.message) || e) }); }

// components/designer/Pane.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Pane — collapsible inspector section: eyebrow title + actions + fields. */
function Pane({
  title,
  actions,
  defaultOpen = true,
  className = '',
  children,
  ...rest
}) {
  const [open, setOpen] = React.useState(defaultOpen);
  return /*#__PURE__*/React.createElement("section", _extends({
    className: `dsr-pane ${open ? 'is-open' : ''} ${className}`.trim()
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "dsr-pane__header",
    role: "button",
    tabIndex: 0,
    "aria-expanded": open,
    onClick: () => setOpen(!open),
    onKeyDown: e => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        setOpen(!open);
      }
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-pane__title"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "collapse",
    size: 12,
    className: "dsr-pane__chevron"
  }), title), actions ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-pane__actions",
    onClick: e => e.stopPropagation()
  }, actions) : null), open ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-pane__body"
  }, children) : null);
}
Object.assign(__ds_scope, { Pane });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/Pane.jsx", error: String((e && e.message) || e) }); }

// components/fields/NumberField.jsx
try { (() => {
const round2 = n => Math.round(n * 100) / 100;

/**
 * NumberField — the unit system in one control.
 * Affix (label/icon) is scrubbable (drag ←→); ↑/↓ steps, ⇧ steps ×10.
 */
function NumberField({
  label,
  icon,
  unit,
  value,
  defaultValue = 0,
  onChange,
  min,
  max,
  step = 1,
  title,
  className = '',
  style
}) {
  const controlled = value !== undefined;
  const [inner, setInner] = React.useState(defaultValue);
  const [text, setText] = React.useState(null); // transient edit buffer
  const val = controlled ? value : inner;
  const set = n => {
    let next = n;
    if (min != null) next = Math.max(min, next);
    if (max != null) next = Math.min(max, next);
    next = round2(next);
    if (!controlled) setInner(next);
    if (onChange) onChange(next);
  };
  const commit = t => {
    const n = parseFloat(t);
    if (!Number.isNaN(n)) set(n);
    setText(null);
  };
  const onKeyDown = e => {
    if (e.key === 'ArrowUp' || e.key === 'ArrowDown') {
      e.preventDefault();
      const d = (e.key === 'ArrowUp' ? 1 : -1) * (e.shiftKey ? step * 10 : step);
      const cur = text != null ? parseFloat(text) || 0 : Number(val) || 0;
      set(cur + d);
      setText(null);
    } else if (e.key === 'Enter') {
      commit(e.currentTarget.value);
      e.currentTarget.blur();
    } else if (e.key === 'Escape') {
      setText(null);
      e.currentTarget.blur();
    }
  };
  const onScrub = e => {
    e.preventDefault();
    const startX = e.clientX;
    const start = Number(val) || 0;
    const move = ev => set(start + Math.round((ev.clientX - startX) / 2) * step);
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-numfield ${className}`.trim(),
    style: style
  }, icon || label ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-numfield__affix",
    onPointerDown: onScrub,
    title: title || 'Drag to adjust'
  }, icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 12
  }) : label) : /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6
    }
  }), /*#__PURE__*/React.createElement("input", {
    className: "dsr-numfield__input",
    type: "text",
    inputMode: "decimal",
    value: text != null ? text : String(val),
    onChange: e => setText(e.target.value),
    onBlur: e => commit(e.target.value),
    onKeyDown: onKeyDown,
    "aria-label": title || (typeof label === 'string' ? label : 'Value')
  }), unit ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-numfield__unit"
  }, unit) : null);
}
Object.assign(__ds_scope, { NumberField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/fields/NumberField.jsx", error: String((e && e.message) || e) }); }

// components/fields/PositionField.jsx
try { (() => {
/** PositionField — paired NumberFields: X/Y, W/H, etc. */
function PositionField({
  labels = ['X', 'Y'],
  values = [0, 0],
  onChange,
  units,
  step = 1,
  min,
  max,
  className = '',
  style
}) {
  const update = (idx, n) => {
    const next = [...values];
    next[idx] = n;
    if (onChange) onChange(next);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-posfield ${className}`.trim(),
    style: style
  }, /*#__PURE__*/React.createElement(__ds_scope.NumberField, {
    label: labels[0],
    value: values[0],
    unit: units && units[0],
    step: step,
    min: min,
    max: max,
    onChange: n => update(0, n)
  }), /*#__PURE__*/React.createElement(__ds_scope.NumberField, {
    label: labels[1],
    value: values[1],
    unit: units && units[1],
    step: step,
    min: min,
    max: max,
    onChange: n => update(1, n)
  }));
}
Object.assign(__ds_scope, { PositionField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/fields/PositionField.jsx", error: String((e && e.message) || e) }); }

// components/library/LibraryItem.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** LibraryItem — one insertable asset (component, shape, block). */
function LibraryItem({
  name,
  icon,
  thumb,
  onInsert,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: `dsr-libitem ${className}`.trim(),
    draggable: true,
    title: `Insert ${name}`,
    onClick: onInsert,
    onDragStart: e => {
      try {
        e.dataTransfer.setData('text/plain', String(name));
      } catch (err) {/* noop */}
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "dsr-libitem__thumb"
  }, thumb || /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon || 'component',
    size: 18
  })), /*#__PURE__*/React.createElement("span", {
    className: "dsr-libitem__name"
  }, name));
}
Object.assign(__ds_scope, { LibraryItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/library/LibraryItem.jsx", error: String((e && e.message) || e) }); }

// components/prototype/ConnectionLayer.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const path = c => {
  const dx = Math.max(40, Math.abs(c.to.x - c.from.x) * 0.45);
  return `M ${c.from.x} ${c.from.y} C ${c.from.x + dx} ${c.from.y}, ${c.to.x - dx} ${c.to.y}, ${c.to.x} ${c.to.y}`;
};

/**
 * ConnectionLayer — prototype "noodles" between frames, in world
 * coordinates. Place INSIDE Canvas, after the Frames.
 */
function ConnectionLayer({
  connections = [],
  activeId,
  className = '',
  ...rest
}) {
  const mid = React.useId ? React.useId().replace(/[:]/g, '') : 'dsr';
  return /*#__PURE__*/React.createElement("svg", _extends({
    className: `dsr-connections ${className}`.trim(),
    width: "1",
    height: "1",
    "aria-hidden": "true"
  }, rest), /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("marker", {
    id: `arrow-${mid}`,
    viewBox: "0 0 10 10",
    refX: "8",
    refY: "5",
    markerWidth: "7",
    markerHeight: "7",
    orient: "auto-start-reverse"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M 0 1 L 8 5 L 0 9",
    fill: "none",
    stroke: "var(--selection)",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), connections.map(c => /*#__PURE__*/React.createElement("g", {
    key: c.id
  }, /*#__PURE__*/React.createElement("path", {
    className: `dsr-connections__path${activeId && activeId !== c.id ? ' is-dim' : ''}`,
    d: path(c),
    markerEnd: `url(#arrow-${mid})`
  }), /*#__PURE__*/React.createElement("circle", {
    className: "dsr-connections__port",
    cx: c.from.x,
    cy: c.from.y,
    r: "3.5"
  }))));
}
Object.assign(__ds_scope, { ConnectionLayer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/prototype/ConnectionLayer.jsx", error: String((e && e.message) || e) }); }

// components/tokens/TokenSwatch.jsx
try { (() => {
const dimPx = v => {
  const n = parseFloat(v);
  if (Number.isNaN(n)) return 8;
  const px = String(v).includes('rem') ? n * 16 : n;
  return Math.max(2, Math.min(20, px));
};

/** TokenSwatch — visual preview of a typed token ($type-aware). */
function TokenSwatch({
  token
}) {
  const t = token.$type;
  const v = token.cssVar ? `var(${token.cssVar})` : token.$value;
  let inner = null;
  if (t === 'color') {
    inner = /*#__PURE__*/React.createElement("span", {
      className: "dsr-tokenswatch__color",
      style: {
        background: v
      }
    });
  } else if (t === 'dimension') {
    inner = /*#__PURE__*/React.createElement("span", {
      className: "dsr-tokenswatch__dim",
      style: {
        width: dimPx(token.$value)
      }
    });
  } else if (t === 'shadow') {
    inner = /*#__PURE__*/React.createElement("span", {
      className: "dsr-tokenswatch__shadow",
      style: {
        boxShadow: v
      }
    });
  } else if (t === 'fontFamily') {
    inner = /*#__PURE__*/React.createElement("span", {
      className: "dsr-tokenswatch__text",
      style: {
        fontFamily: Array.isArray(token.$value) ? token.$value.join(', ') : token.$value
      }
    }, "Ag");
  } else if (t === 'fontWeight') {
    inner = /*#__PURE__*/React.createElement("span", {
      className: "dsr-tokenswatch__text",
      style: {
        fontFamily: 'var(--font-sans)',
        fontWeight: token.$value
      }
    }, "Ag");
  } else if (t === 'duration') {
    inner = /*#__PURE__*/React.createElement("span", {
      className: "dsr-tokenswatch__dim",
      style: {
        width: Math.min(20, parseFloat(token.$value) / 16),
        background: 'var(--brand)'
      }
    });
  } else {
    inner = /*#__PURE__*/React.createElement("span", {
      className: "dsr-tokenswatch__text",
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 9,
        color: 'var(--muted-foreground)'
      }
    }, '{}');
  }
  return /*#__PURE__*/React.createElement("span", {
    className: "dsr-tokenswatch"
  }, inner);
}
Object.assign(__ds_scope, { TokenSwatch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/tokens/TokenSwatch.jsx", error: String((e && e.message) || e) }); }

// components/ui/Avatar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const PRESENCE_COLORS = ['oklch(0.62 0.19 256)',
// ultramarine
'oklch(0.66 0.13 43)',
// terracotta
'oklch(0.60 0.12 160)',
// green
'oklch(0.65 0.13 300)',
// violet
'oklch(0.70 0.12 85)' // amber
];
const presenceColor = (name = '') => {
  let h = 0;
  for (let i = 0; i < name.length; i += 1) h = (h * 31 + name.charCodeAt(i)) % 997;
  return PRESENCE_COLORS[h % PRESENCE_COLORS.length];
};
const initials = (name = '') => name.split(/\s+/).slice(0, 2).map(w => w[0] || '').join('').toUpperCase();

/** Avatar — initials or image; `presence` shows the colored ring. */
function Avatar({
  name = '',
  src,
  size = 'md',
  presence = false,
  className = '',
  style,
  ...rest
}) {
  const ring = presence ? {
    '--presence': presenceColor(name)
  } : null;
  return /*#__PURE__*/React.createElement("span", _extends({
    className: `dsr-avatar${size !== 'md' ? ` dsr-avatar--${size}` : ''}${presence ? ' has-ring' : ''} ${className}`.trim(),
    style: {
      ...ring,
      ...style
    },
    title: name
  }, rest), src ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: name,
    onError: e => {
      e.target.style.display = 'none';
    }
  }) : initials(name));
}
Object.assign(__ds_scope, { PRESENCE_COLORS, presenceColor, Avatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/collab/AvatarStack.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** AvatarStack — overlapping presence avatars + overflow count. */
function AvatarStack({
  users = [],
  max = 4,
  size = 'md',
  presence = true,
  className = '',
  ...rest
}) {
  const shown = users.slice(0, max);
  const extra = users.length - shown.length;
  return /*#__PURE__*/React.createElement("span", _extends({
    className: `dsr-avatarstack ${className}`.trim()
  }, rest), shown.map(u => /*#__PURE__*/React.createElement(__ds_scope.Avatar, {
    key: u.name,
    name: u.name,
    src: u.src,
    size: size,
    presence: presence
  })), extra > 0 ? /*#__PURE__*/React.createElement("span", {
    className: `dsr-avatar${size !== 'md' ? ` dsr-avatar--${size}` : ''} dsr-avatarstack__more`
  }, "+", extra) : null);
}
Object.assign(__ds_scope, { AvatarStack });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/collab/AvatarStack.jsx", error: String((e && e.message) || e) }); }

// components/collab/CommentPin.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const initials = (name = '') => name.split(/\s+/).slice(0, 2).map(w => w[0] || '').join('').toUpperCase();

/** CommentPin — comment marker at {x, y}; `open` shows the thread bubble. */
function CommentPin({
  name = '',
  text,
  time,
  x = 0,
  y = 0,
  open = false,
  onClick,
  className = '',
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-commentpin ${className}`.trim(),
    style: {
      left: x,
      top: y,
      '--presence': __ds_scope.presenceColor(name),
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "dsr-commentpin__marker",
    "aria-label": `Comment by ${name}`,
    onClick: onClick
  }, initials(name)), open ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-commentpin__bubble"
  }, /*#__PURE__*/React.createElement("div", {
    className: "dsr-commentpin__meta"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-commentpin__name"
  }, name), time ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-commentpin__time"
  }, time) : null), /*#__PURE__*/React.createElement("p", {
    className: "dsr-commentpin__text"
  }, text)) : null);
}
Object.assign(__ds_scope, { CommentPin });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/collab/CommentPin.jsx", error: String((e && e.message) || e) }); }

// components/collab/CursorChip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** CursorChip — a collaborator's cursor at {x, y} (world or screen coords). */
function CursorChip({
  name = '',
  x = 0,
  y = 0,
  color,
  className = '',
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-cursor ${className}`.trim(),
    style: {
      left: x,
      top: y,
      '--presence': color || __ds_scope.presenceColor(name),
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "dsr-cursor__arrow"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "select",
    size: 16,
    strokeWidth: 2
  })), name ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-cursor__tag"
  }, name.split(' ')[0]) : null);
}
Object.assign(__ds_scope, { CursorChip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/collab/CursorChip.jsx", error: String((e && e.message) || e) }); }

// components/ui/Badge.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Badge — small status/count chip. */
function Badge({
  variant = 'secondary',
  className = '',
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: `dsr-badge dsr-badge--${variant} ${className}`.trim()
  }, rest), children);
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Badge.jsx", error: String((e && e.message) || e) }); }

// components/tokens/TokenRow.jsx
try { (() => {
/** TokenRow — one token: swatch · name · value · type. */
function TokenRow({
  token,
  showType = true,
  onClick
}) {
  const value = Array.isArray(token.$value) ? typeof token.$value[0] === 'object' ? 'composite' : token.$value.join(', ') : String(token.$value);
  return /*#__PURE__*/React.createElement("div", {
    className: "dsr-token-row",
    onClick: onClick,
    title: token.$description || token.name
  }, /*#__PURE__*/React.createElement(__ds_scope.TokenSwatch, {
    token: token
  }), /*#__PURE__*/React.createElement("span", {
    className: "dsr-token-row__name"
  }, token.cssVar || token.name), /*#__PURE__*/React.createElement("span", {
    className: "dsr-token-row__value"
  }, value), showType ? /*#__PURE__*/React.createElement(__ds_scope.Badge, {
    variant: "outline"
  }, token.$type) : /*#__PURE__*/React.createElement("span", null));
}
Object.assign(__ds_scope, { TokenRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/tokens/TokenRow.jsx", error: String((e && e.message) || e) }); }

// components/ui/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Button — the standard action control. Variants map 1:1 to .dsr-btn--* classes. */
function Button({
  variant = 'primary',
  size = 'md',
  className = '',
  children,
  ...rest
}) {
  const sizeCls = size === 'md' ? '' : ` dsr-btn--${size}`;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: `dsr-btn dsr-btn--${variant}${sizeCls} ${className}`.trim()
  }, rest), children);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Button.jsx", error: String((e && e.message) || e) }); }

// components/ui/Checkbox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Checkbox — 16px square. Pass `label` to get the wrapping row for free. */
function Checkbox({
  label,
  className = '',
  ...rest
}) {
  const box = /*#__PURE__*/React.createElement("input", _extends({
    type: "checkbox",
    className: `dsr-checkbox ${className}`.trim()
  }, rest));
  if (!label) return box;
  return /*#__PURE__*/React.createElement("label", {
    className: "dsr-check-row"
  }, box, /*#__PURE__*/React.createElement("span", null, label));
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/ui/IconButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** IconButton — square icon-only control. `label` is mandatory (a11y + tooltip). */
function IconButton({
  name,
  label,
  size = 'md',
  inverse = false,
  active = false,
  tooltip = true,
  className = '',
  ...rest
}) {
  const cls = ['dsr-iconbtn', size !== 'md' ? `dsr-iconbtn--${size}` : '', inverse ? 'dsr-iconbtn--inverse' : '', active ? 'is-active' : '', tooltip && label ? 'dsr-tooltip-host' : '', className].filter(Boolean).join(' ');
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls,
    "aria-label": label,
    "aria-pressed": active || undefined,
    "data-tooltip": tooltip && label ? label : undefined
  }, rest), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: name,
    size: size === 'sm' ? 14 : size === 'lg' ? 18 : 16
  }));
}
Object.assign(__ds_scope, { IconButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/IconButton.jsx", error: String((e && e.message) || e) }); }

// components/actions/ActionAlign.jsx
try { (() => {
const ALIGNS = [['align-left', 'Align left'], ['align-center-h', 'Align horizontal centers'], ['align-right', 'Align right'], ['align-top', 'Align top'], ['align-center-v', 'Align vertical centers'], ['align-bottom', 'Align bottom']];
const DISTS = [['distribute-h', 'Distribute horizontally'], ['distribute-v', 'Distribute vertically']];

/** ActionAlign — the inspector's alignment row; fires the align type. */
function ActionAlign({
  onAlign,
  distribute = true,
  disabled = false,
  className = '',
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: className,
    role: "toolbar",
    "aria-label": "Align",
    style: {
      display: 'flex',
      gap: 2,
      justifyContent: 'space-between',
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      gap: 2
    }
  }, ALIGNS.map(([role, label]) => /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    key: role,
    size: "sm",
    name: role,
    label: label,
    disabled: disabled,
    onClick: () => onAlign && onAlign(role)
  }))), distribute ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      gap: 2
    }
  }, DISTS.map(([role, label]) => /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    key: role,
    size: "sm",
    name: role,
    label: label,
    disabled: disabled,
    onClick: () => onAlign && onAlign(role)
  }))) : null);
}
Object.assign(__ds_scope, { ActionAlign });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ActionAlign.jsx", error: String((e && e.message) || e) }); }

// components/actions/ActionRotate.jsx
try { (() => {
/** ActionRotate — rotation action: scrubbable degrees + quick turns/flips. */
function ActionRotate({
  value,
  defaultValue = 0,
  onChange,
  onFlipH,
  onFlipV,
  className = '',
  style
}) {
  const controlled = value !== undefined;
  const [inner, setInner] = React.useState(defaultValue);
  const deg = controlled ? value : inner;
  const set = n => {
    const norm = (n % 360 + 540) % 360 - 180; // wrap to −180…180
    if (!controlled) setInner(norm);
    if (onChange) onChange(norm);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: className,
    style: {
      display: 'flex',
      gap: 4,
      alignItems: 'center',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.NumberField, {
    icon: "rotate",
    unit: "deg",
    value: deg,
    min: -180,
    max: 180,
    onChange: set,
    title: "Rotation",
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    name: "redo",
    label: "Rotate 90\xB0 cw",
    onClick: () => set(deg + 90)
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    name: "flip-h",
    label: "Flip horizontal",
    onClick: onFlipH,
    disabled: !onFlipH
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    name: "flip-v",
    label: "Flip vertical",
    onClick: onFlipV,
    disabled: !onFlipV
  }));
}
Object.assign(__ds_scope, { ActionRotate });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ActionRotate.jsx", error: String((e && e.message) || e) }); }

// components/designer/LayerTree.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const TYPE_ICON = {
  frame: 'frame',
  group: 'group',
  component: 'component',
  text: 'text',
  image: 'image',
  shape: 'shape',
  rectangle: 'rectangle',
  ellipse: 'ellipse'
};
function Row({
  layer,
  depth,
  selectedId,
  onSelect,
  onToggleHidden,
  onToggleLocked,
  expanded,
  toggleExpand
}) {
  const hasKids = layer.children && layer.children.length > 0;
  const open = expanded.has(layer.id);
  const pinned = layer.hidden || layer.locked;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: `dsr-layer-row${selectedId === layer.id ? ' is-selected' : ''}${layer.hidden ? ' is-hidden' : ''}`,
    style: {
      paddingLeft: 4 + depth * 16
    },
    onPointerDown: () => onSelect && onSelect(layer.id)
  }, /*#__PURE__*/React.createElement("span", {
    className: `dsr-layer-row__chevron${open ? ' is-open' : ''}`,
    style: {
      visibility: hasKids ? 'visible' : 'hidden'
    },
    onPointerDown: e => {
      e.stopPropagation();
      toggleExpand(layer.id);
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "collapse",
    size: 12
  })), /*#__PURE__*/React.createElement("span", {
    className: "dsr-layer-row__icon"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: TYPE_ICON[layer.type] || 'shape',
    size: 14
  })), /*#__PURE__*/React.createElement("span", {
    className: "dsr-layer-row__name"
  }, layer.name), layer.binding ? /*#__PURE__*/React.createElement(__ds_scope.Badge, {
    variant: "selection",
    className: "dsr-layer-row__badge"
  }, layer.binding) : null, /*#__PURE__*/React.createElement("span", {
    className: `dsr-layer-row__meta${pinned ? ' is-pinned' : ''}`
  }, /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    tooltip: false,
    name: layer.locked ? 'locked' : 'unlocked',
    label: layer.locked ? 'Unlock' : 'Lock',
    onPointerDown: e => e.stopPropagation(),
    onClick: () => onToggleLocked && onToggleLocked(layer.id)
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    tooltip: false,
    name: layer.hidden ? 'hidden' : 'visible',
    label: layer.hidden ? 'Show' : 'Hide',
    onPointerDown: e => e.stopPropagation(),
    onClick: () => onToggleHidden && onToggleHidden(layer.id)
  }))), hasKids && open ? layer.children.map(c => /*#__PURE__*/React.createElement(Row, {
    key: c.id,
    layer: c,
    depth: depth + 1,
    selectedId: selectedId,
    onSelect: onSelect,
    onToggleHidden: onToggleHidden,
    onToggleLocked: onToggleLocked,
    expanded: expanded,
    toggleExpand: toggleExpand
  })) : null);
}

/** LayerTree — recursive layer list. Data in, callbacks out. */
function LayerTree({
  layers = [],
  selectedId,
  onSelect,
  onToggleHidden,
  onToggleLocked,
  defaultExpandedIds,
  className = '',
  ...rest
}) {
  const [expanded, setExpanded] = React.useState(() => {
    if (defaultExpandedIds) return new Set(defaultExpandedIds);
    const all = new Set();
    const walk = ls => ls.forEach(l => {
      if (l.children) {
        all.add(l.id);
        walk(l.children);
      }
    });
    walk(layers);
    return all;
  });
  const toggleExpand = id => setExpanded(prev => {
    const next = new Set(prev);
    if (next.has(id)) next.delete(id);else next.add(id);
    return next;
  });
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-layertree ${className}`.trim(),
    role: "tree"
  }, rest), layers.map(l => /*#__PURE__*/React.createElement(Row, {
    key: l.id,
    layer: l,
    depth: 0,
    selectedId: selectedId,
    onSelect: onSelect,
    onToggleHidden: onToggleHidden,
    onToggleLocked: onToggleLocked,
    expanded: expanded,
    toggleExpand: toggleExpand
  })));
}
Object.assign(__ds_scope, { LayerTree });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/LayerTree.jsx", error: String((e && e.message) || e) }); }

// components/designer/ZoomControls.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const clampZoom = z => Math.min(8, Math.max(0.1, z));

/** ZoomControls — floating zoom HUD. Click the % to reset to 100. */
function ZoomControls({
  view,
  onViewChange,
  onFit,
  className = '',
  ...rest
}) {
  const zoomBy = f => onViewChange && onViewChange({
    ...view,
    zoom: clampZoom(view.zoom * f)
  });
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-zoomhud ${className}`.trim()
  }, rest), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    name: "zoom-out",
    label: "Zoom out",
    onClick: () => zoomBy(1 / 1.25)
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "dsr-zoomhud__value",
    title: "Zoom to 100% \u2014 \u21E70",
    onClick: () => onViewChange && onViewChange({
      ...view,
      zoom: 1
    })
  }, Math.round(view.zoom * 100), "%"), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    name: "zoom-in",
    label: "Zoom in",
    onClick: () => zoomBy(1.25)
  }), onFit ? /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    name: "zoom-fit",
    label: "Zoom to fit \u2014 \u21E71",
    onClick: onFit
  }) : null);
}
Object.assign(__ds_scope, { ZoomControls });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/designer/ZoomControls.jsx", error: String((e && e.message) || e) }); }

// components/devices/BreakpointSwitcher.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DEFAULT_BPS = [{
  id: 'desktop',
  icon: 'device-desktop',
  label: 'Desktop — 1920'
}, {
  id: 'laptop',
  icon: 'device-laptop',
  label: 'Laptop — 1280'
}, {
  id: 'tablet',
  icon: 'device-tablet',
  label: 'Tablet — 768'
}, {
  id: 'phone',
  icon: 'device-phone',
  label: 'Phone — 390'
}];

/** BreakpointSwitcher — the sites-builder viewport toggle (menubar center). */
function BreakpointSwitcher({
  breakpoints = DEFAULT_BPS,
  value,
  onChange,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: className,
    style: {
      display: 'inline-flex',
      gap: 2
    },
    role: "radiogroup",
    "aria-label": "Breakpoint"
  }, rest), breakpoints.map(bp => /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    key: bp.id,
    name: bp.icon || 'device-desktop',
    label: bp.label || bp.id,
    active: value === bp.id,
    onClick: () => onChange && onChange(bp.id)
  })));
}
Object.assign(__ds_scope, { BreakpointSwitcher });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/devices/BreakpointSwitcher.jsx", error: String((e && e.message) || e) }); }

// components/ui/Dialog.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Dialog — modal over the app shell (absolute overlay, Esc/backdrop close). */
function Dialog({
  open = false,
  title,
  description,
  footer,
  onClose,
  className = '',
  children,
  ...rest
}) {
  React.useEffect(() => {
    if (!open || !onClose) return undefined;
    const h = e => {
      if (e.key === 'Escape') onClose();
    };
    window.addEventListener('keydown', h);
    return () => window.removeEventListener('keydown', h);
  }, [open, onClose]);
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    className: "dsr-dialog-overlay",
    onPointerDown: e => {
      if (e.target === e.currentTarget && onClose) onClose();
    }
  }, /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-dialog ${className}`.trim(),
    role: "dialog",
    "aria-modal": "true"
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "dsr-dialog__header"
  }, /*#__PURE__*/React.createElement("div", null, title ? /*#__PURE__*/React.createElement("h2", {
    className: "dsr-dialog__title"
  }, title) : null, description ? /*#__PURE__*/React.createElement("p", {
    className: "dsr-dialog__desc"
  }, description) : null), onClose ? /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    name: "close",
    label: "Close",
    size: "sm",
    tooltip: false,
    onClick: onClose
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "dsr-dialog__body"
  }, children), footer ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-dialog__footer"
  }, footer) : null));
}
Object.assign(__ds_scope, { Dialog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Dialog.jsx", error: String((e && e.message) || e) }); }

// components/ui/Input.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Input — single-line text input. `quiet` = borderless muted fill (panel-dense). */
function Input({
  size = 'md',
  mono = false,
  quiet = false,
  className = '',
  ...rest
}) {
  const cls = ['dsr-input', size !== 'md' ? `dsr-input--${size}` : '', mono ? 'dsr-input--mono' : '', quiet ? 'dsr-input--quiet' : '', className].filter(Boolean).join(' ');
  return /*#__PURE__*/React.createElement("input", _extends({
    className: cls
  }, rest));
}
Object.assign(__ds_scope, { Input });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Input.jsx", error: String((e && e.message) || e) }); }

// components/fields/TextField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** TextField — labeled row: 84px label + quiet input. */
function TextField({
  label,
  mono = false,
  className = '',
  style,
  ...inputProps
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-field ${className}`.trim(),
    style: style
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, label), /*#__PURE__*/React.createElement(__ds_scope.Input, _extends({
    quiet: true,
    size: "sm",
    mono: mono
  }, inputProps)));
}
Object.assign(__ds_scope, { TextField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/fields/TextField.jsx", error: String((e && e.message) || e) }); }

// components/ui/Kbd.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const KEY_MAP = {
  mod: '⌘',
  cmd: '⌘',
  shift: '⇧',
  alt: '⌥',
  ctrl: '⌃',
  enter: '↵',
  backspace: '⌫',
  esc: 'esc',
  tab: '⇥',
  up: '↑',
  down: '↓',
  left: '←',
  right: '→'
};

/** Kbd — keyboard shortcut caps. Accepts `keys={['mod','K']}` or children. */
function Kbd({
  keys,
  className = '',
  children,
  ...rest
}) {
  const list = keys || (children != null ? [children] : []);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: `dsr-kbd-group ${className}`.trim()
  }, rest), list.map((k, i) => /*#__PURE__*/React.createElement("kbd", {
    key: i,
    className: "dsr-kbd"
  }, KEY_MAP[String(k).toLowerCase()] || k)));
}
Object.assign(__ds_scope, { Kbd });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Kbd.jsx", error: String((e && e.message) || e) }); }

// components/commands/CommandPalette.jsx
try { (() => {
/**
 * CommandPalette — ⌘K. Renders data/keybindings.json groups; filters,
 * arrows navigate, Enter runs, Esc closes. Position: absolute within
 * the nearest positioned ancestor (the app shell).
 */
function CommandPalette({
  groups = [],
  open = false,
  onClose,
  onRun,
  placeholder = 'Type a command…'
}) {
  const [q, setQ] = React.useState('');
  const [idx, setIdx] = React.useState(0);
  const inputRef = React.useRef(null);
  const flat = React.useMemo(() => {
    const ql = q.trim().toLowerCase();
    const out = [];
    groups.forEach(g => {
      (g.commands || []).forEach(c => {
        if (!ql || c.title.toLowerCase().includes(ql) || g.name.toLowerCase().includes(ql)) {
          out.push({
            ...c,
            group: g.name
          });
        }
      });
    });
    return out;
  }, [groups, q]);
  React.useEffect(() => {
    setIdx(0);
  }, [q, open]);
  React.useEffect(() => {
    if (open && inputRef.current) inputRef.current.focus();
    if (open) setQ('');
  }, [open]);
  if (!open) return null;
  const run = cmd => {
    if (onRun) onRun(cmd);
    if (onClose) onClose();
  };
  const onKeyDown = e => {
    if (e.key === 'ArrowDown') {
      e.preventDefault();
      setIdx(i => Math.min(i + 1, flat.length - 1));
    } else if (e.key === 'ArrowUp') {
      e.preventDefault();
      setIdx(i => Math.max(i - 1, 0));
    } else if (e.key === 'Enter' && flat[idx]) {
      e.preventDefault();
      run(flat[idx]);
    } else if (e.key === 'Escape' && onClose) onClose();
  };
  let lastGroup = null;
  return /*#__PURE__*/React.createElement("div", {
    className: "dsr-cmdk-overlay",
    onPointerDown: e => {
      if (e.target === e.currentTarget && onClose) onClose();
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "dsr-cmdk",
    role: "dialog",
    "aria-label": "Command palette"
  }, /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    className: "dsr-cmdk__input",
    placeholder: placeholder,
    value: q,
    onChange: e => setQ(e.target.value),
    onKeyDown: onKeyDown
  }), /*#__PURE__*/React.createElement("div", {
    className: "dsr-cmdk__list"
  }, flat.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-cmdk__empty"
  }, "No commands match \u201C", q, "\u201D.") : null, flat.map((c, i) => {
    const head = c.group !== lastGroup ? /*#__PURE__*/React.createElement("div", {
      key: `h-${c.group}`,
      className: "dsr-cmdk__group-label"
    }, /*#__PURE__*/React.createElement("span", {
      className: "dsr-label dsr-label--eyebrow"
    }, c.group)) : null;
    lastGroup = c.group;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: c.id
    }, head, /*#__PURE__*/React.createElement("div", {
      className: `dsr-cmdk__row${i === idx ? ' is-active' : ''}`,
      onPointerEnter: () => setIdx(i),
      onClick: () => run(c)
    }, /*#__PURE__*/React.createElement("span", {
      className: "dsr-cmdk__row-icon"
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: c.icon || 'chevron-right',
      size: 14
    })), /*#__PURE__*/React.createElement("span", {
      className: "dsr-cmdk__row-title"
    }, c.title), c.keys && c.keys.length ? /*#__PURE__*/React.createElement(__ds_scope.Kbd, {
      keys: c.keys
    }) : null));
  }))));
}
Object.assign(__ds_scope, { CommandPalette });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/commands/CommandPalette.jsx", error: String((e && e.message) || e) }); }

// components/ui/Label.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Label — control caption. `eyebrow` = uppercase 11px section label. */
function Label({
  eyebrow = false,
  hint,
  className = '',
  children,
  ...rest
}) {
  const cls = ['dsr-label', eyebrow ? 'dsr-label--eyebrow' : '', className].filter(Boolean).join(' ');
  return /*#__PURE__*/React.createElement("label", _extends({
    className: cls
  }, rest), children, hint ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-label__hint"
  }, hint) : null);
}
Object.assign(__ds_scope, { Label });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Label.jsx", error: String((e && e.message) || e) }); }

// components/commands/ShortcutsOverlay.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** ShortcutsOverlay — the keyboard map, grouped (data/keybindings.json). */
function ShortcutsOverlay({
  groups = [],
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-shortcuts ${className}`.trim()
  }, rest), groups.map(g => /*#__PURE__*/React.createElement("section", {
    key: g.id
  }, /*#__PURE__*/React.createElement(__ds_scope.Label, {
    eyebrow: true,
    style: {
      display: 'block',
      marginBottom: 6
    }
  }, g.name), (g.commands || []).filter(c => c.keys && c.keys.length).map(c => /*#__PURE__*/React.createElement("div", {
    key: c.id,
    className: "dsr-shortcuts__row"
  }, /*#__PURE__*/React.createElement("span", null, c.title), /*#__PURE__*/React.createElement(__ds_scope.Kbd, {
    keys: c.keys
  }))))));
}
Object.assign(__ds_scope, { ShortcutsOverlay });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/commands/ShortcutsOverlay.jsx", error: String((e && e.message) || e) }); }

// components/library/AssetGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** AssetGrid — sectioned grid of LibraryItems. */
function AssetGrid({
  title,
  count,
  children,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", _extends({
    className: className
  }, rest), title ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: 'var(--space-2) var(--space-3) 0'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Label, {
    eyebrow: true
  }, title), count != null ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 'var(--text-ui-xs)',
      color: 'var(--muted-foreground)'
    }
  }, count) : null) : null, /*#__PURE__*/React.createElement("div", {
    className: "dsr-assetgrid"
  }, children));
}
Object.assign(__ds_scope, { AssetGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/library/AssetGrid.jsx", error: String((e && e.message) || e) }); }

// components/tokens/TokenList.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * TokenList — grouped token manager list. Feed it data/tokens.json
 * ({ groups: [{ id, name, tokens: [...] }] }) or a filtered subset.
 */
function TokenList({
  groups = [],
  filter = '',
  showType = true,
  onTokenClick,
  className = '',
  ...rest
}) {
  const q = filter.trim().toLowerCase();
  const match = t => !q || t.name.toLowerCase().includes(q) || (t.cssVar || '').toLowerCase().includes(q) || t.$type.toLowerCase().includes(q);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-tokenlist ${className}`.trim()
  }, rest), groups.map(g => {
    const tokens = (g.tokens || []).filter(match);
    if (!tokens.length) return null;
    return /*#__PURE__*/React.createElement("section", {
      key: g.id,
      className: "dsr-tokenlist__group"
    }, /*#__PURE__*/React.createElement("div", {
      className: "dsr-tokenlist__head"
    }, /*#__PURE__*/React.createElement(__ds_scope.Label, {
      eyebrow: true
    }, g.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--text-ui-xs)',
        color: 'var(--muted-foreground)',
        fontFamily: 'var(--font-mono)'
      }
    }, tokens.length)), tokens.map(t => /*#__PURE__*/React.createElement(__ds_scope.TokenRow, {
      key: t.name,
      token: t,
      showType: showType,
      onClick: onTokenClick ? () => onTokenClick(t) : undefined
    })));
  }));
}
Object.assign(__ds_scope, { TokenList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/tokens/TokenList.jsx", error: String((e && e.message) || e) }); }

// components/ui/Menu.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Menu — dropdown/context menu surface. Items are data; position it
 * yourself (absolute, at the trigger or pointer).
 */
function Menu({
  items = [],
  onSelect,
  className = '',
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-menu ${className}`.trim(),
    role: "menu",
    style: style
  }, rest), items.map((it, i) => {
    if (it === '-' || it.separator) return /*#__PURE__*/React.createElement("div", {
      key: `s${i}`,
      className: "dsr-menu__sep",
      role: "separator"
    });
    return /*#__PURE__*/React.createElement("div", {
      key: it.id || it.title,
      role: "menuitem",
      className: `dsr-menu__item${it.danger ? ' is-danger' : ''}${it.disabled ? ' is-disabled' : ''}`,
      onClick: () => !it.disabled && onSelect && onSelect(it)
    }, /*#__PURE__*/React.createElement("span", {
      className: "dsr-menu__icon"
    }, it.icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: it.icon,
      size: 14
    }) : null), /*#__PURE__*/React.createElement("span", {
      className: "dsr-menu__title"
    }, it.title), it.keys && it.keys.length ? /*#__PURE__*/React.createElement(__ds_scope.Kbd, {
      keys: it.keys
    }) : null);
  }));
}
Object.assign(__ds_scope, { Menu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Menu.jsx", error: String((e && e.message) || e) }); }

// components/ui/Select.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Select — styled native select. `options`: strings or {value, label}. */
function Select({
  options = [],
  size = 'md',
  quiet = false,
  className = '',
  ...rest
}) {
  const cls = ['dsr-select', size !== 'md' ? `dsr-select--${size}` : '', quiet ? 'dsr-select--quiet' : '', className].filter(Boolean).join(' ');
  return /*#__PURE__*/React.createElement("span", {
    className: cls
  }, /*#__PURE__*/React.createElement("select", _extends({
    className: "dsr-select__native"
  }, rest), options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  })), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevrons-updown",
    size: 12,
    className: "dsr-select__chevron"
  }));
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Select.jsx", error: String((e && e.message) || e) }); }

// components/fields/SelectField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** SelectField — labeled row: 84px label + quiet select. */
function SelectField({
  label,
  options = [],
  className = '',
  style,
  ...selectProps
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-field ${className}`.trim(),
    style: style
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, label), /*#__PURE__*/React.createElement(__ds_scope.Select, _extends({
    quiet: true,
    size: "sm",
    options: options
  }, selectProps)));
}
Object.assign(__ds_scope, { SelectField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/fields/SelectField.jsx", error: String((e && e.message) || e) }); }

// components/data/DataSourcePicker.jsx
try { (() => {
/**
 * DataSourcePicker — two-step binding control: collection → field.
 * Optionally filter bindable fields by type (e.g. only "image").
 */
function DataSourcePicker({
  collections = [],
  value = {},
  onChange,
  fieldType,
  className = '',
  style
}) {
  const col = collections.find(c => c.id === value.collection) || null;
  const fields = col ? col.fields.filter(f => !fieldType || f.type === fieldType) : [];
  const set = patch => onChange && onChange({
    ...value,
    ...patch
  });
  return /*#__PURE__*/React.createElement("div", {
    className: className,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4,
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.SelectField, {
    label: "Collection",
    options: [{
      value: '',
      label: '—'
    }, ...collections.map(c => ({
      value: c.id,
      label: c.name
    }))],
    value: value.collection || '',
    onChange: e => set({
      collection: e.target.value || undefined,
      field: undefined
    })
  }), /*#__PURE__*/React.createElement(__ds_scope.SelectField, {
    label: "Field",
    options: [{
      value: '',
      label: '—'
    }, ...fields.map(f => ({
      value: f.key,
      label: f.label
    }))],
    value: value.field || '',
    disabled: !col,
    onChange: e => set({
      field: e.target.value || undefined
    })
  }));
}
Object.assign(__ds_scope, { DataSourcePicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/DataSourcePicker.jsx", error: String((e && e.message) || e) }); }

// components/prototype/InteractionEditor.jsx
try { (() => {
const opts = list => list.map(o => ({
  value: o.id,
  label: o.name
}));

/**
 * InteractionEditor — one prototype interaction: trigger → action →
 * target + transition (easing/duration map to motion tokens).
 * Vocabulary comes from data/interactions.json.
 */
function InteractionEditor({
  vocab,
  value,
  onChange,
  targets = [],
  className = '',
  style
}) {
  const v = value || {};
  const set = patch => onChange && onChange({
    ...v,
    ...patch
  });
  const showMotion = v.transition && v.transition !== 'instant';
  return /*#__PURE__*/React.createElement("div", {
    className: className,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      fontSize: 'var(--text-ui-sm)',
      color: 'var(--selection)',
      padding: '2px 0 4px'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "connection",
    size: 13
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)'
    }
  }, (vocab.triggers.find(t => t.id === v.trigger) || vocab.triggers[0]).name, ' → ', (vocab.actions.find(a => a.id === v.action) || vocab.actions[0]).name)), /*#__PURE__*/React.createElement(__ds_scope.SelectField, {
    label: "Trigger",
    options: opts(vocab.triggers),
    value: v.trigger || 'click',
    onChange: e => set({
      trigger: e.target.value
    })
  }), /*#__PURE__*/React.createElement(__ds_scope.SelectField, {
    label: "Action",
    options: opts(vocab.actions),
    value: v.action || 'navigate',
    onChange: e => set({
      action: e.target.value
    })
  }), targets.length ? /*#__PURE__*/React.createElement(__ds_scope.SelectField, {
    label: "Target",
    options: targets,
    value: v.target || targets[0].value || targets[0],
    onChange: e => set({
      target: e.target.value
    })
  }) : null, /*#__PURE__*/React.createElement(__ds_scope.SelectField, {
    label: "Transition",
    options: opts(vocab.transitions),
    value: v.transition || 'smart',
    onChange: e => set({
      transition: e.target.value
    })
  }), showMotion ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Motion"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 88px',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Select, {
    quiet: true,
    size: "sm",
    options: opts(vocab.easings),
    value: v.easing || 'ease-standard',
    onChange: e => set({
      easing: e.target.value
    }),
    "aria-label": "Easing"
  }), /*#__PURE__*/React.createElement(__ds_scope.NumberField, {
    icon: "delay",
    unit: "ms",
    value: v.duration != null ? v.duration : vocab.durations.default,
    min: vocab.durations.min,
    max: vocab.durations.max,
    step: vocab.durations.step,
    onChange: duration => set({
      duration
    }),
    title: "Duration"
  }))) : null);
}
Object.assign(__ds_scope, { InteractionEditor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/prototype/InteractionEditor.jsx", error: String((e && e.message) || e) }); }

// components/ui/Separator.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Separator — 1px hairline, horizontal or vertical. */
function Separator({
  orientation = 'horizontal',
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "separator",
    "aria-orientation": orientation,
    className: `dsr-separator dsr-separator--${orientation} ${className}`.trim()
  }, rest));
}
Object.assign(__ds_scope, { Separator });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Separator.jsx", error: String((e && e.message) || e) }); }

// components/ui/Slider.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Slider — native range input, quiet 2px track. */
function Slider({
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("input", _extends({
    type: "range",
    className: `dsr-slider ${className}`.trim()
  }, rest));
}
Object.assign(__ds_scope, { Slider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Slider.jsx", error: String((e && e.message) || e) }); }

// components/actions/ActionOpacity.jsx
try { (() => {
/** ActionOpacity — opacity action: slider + exact %. */
function ActionOpacity({
  value,
  defaultValue = 100,
  onChange,
  className = '',
  style
}) {
  const controlled = value !== undefined;
  const [inner, setInner] = React.useState(defaultValue);
  const op = controlled ? value : inner;
  const set = n => {
    const c = Math.max(0, Math.min(100, n));
    if (!controlled) setInner(c);
    if (onChange) onChange(c);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: className,
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 76px',
      gap: 8,
      alignItems: 'center',
      ...style
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Slider, {
    min: 0,
    max: 100,
    value: op,
    onChange: e => set(Number(e.target.value)),
    "aria-label": "Opacity"
  }), /*#__PURE__*/React.createElement(__ds_scope.NumberField, {
    icon: "opacity",
    unit: "%",
    value: op,
    min: 0,
    max: 100,
    onChange: set,
    title: "Opacity"
  }));
}
Object.assign(__ds_scope, { ActionOpacity });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ActionOpacity.jsx", error: String((e && e.message) || e) }); }

// components/ui/Switch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Switch — instant on/off toggle. Pass `label` for the wrapping row. */
function Switch({
  label,
  className = '',
  ...rest
}) {
  const sw = /*#__PURE__*/React.createElement("input", _extends({
    type: "checkbox",
    role: "switch",
    className: `dsr-switch ${className}`.trim()
  }, rest));
  if (!label) return sw;
  return /*#__PURE__*/React.createElement("label", {
    className: "dsr-check-row",
    style: {
      justifyContent: 'space-between',
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("span", null, label), sw);
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Switch.jsx", error: String((e && e.message) || e) }); }

// components/fields/BooleanField.jsx
try { (() => {
/** BooleanField — labeled row: 84px label + switch (right-aligned). */
function BooleanField({
  label,
  className = '',
  style,
  ...switchProps
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-field ${className}`.trim(),
    style: style
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Switch, switchProps)));
}
Object.assign(__ds_scope, { BooleanField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/fields/BooleanField.jsx", error: String((e && e.message) || e) }); }

// components/ui/Tabs.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Tabs — segmented control. Controlled: pass `value` + `onChange`. */
function Tabs({
  items = [],
  value,
  onChange,
  full = false,
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "tablist",
    className: `dsr-tabs ${full ? 'dsr-tabs--full' : ''} ${className}`.trim()
  }, rest), items.map(it => {
    const v = typeof it === 'object' ? it.value : it;
    const l = typeof it === 'object' ? it.label : it;
    const selected = v === value;
    return /*#__PURE__*/React.createElement("button", {
      key: v,
      type: "button",
      role: "tab",
      "aria-selected": selected,
      className: `dsr-tabs__tab${selected ? ' is-selected' : ''}`,
      onClick: () => onChange && onChange(v)
    }, l);
  }));
}
Object.assign(__ds_scope, { Tabs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Tabs.jsx", error: String((e && e.message) || e) }); }

// components/ui/Toast.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Toast — one feedback capsule. Stack them inside <div className="dsr-toaststack">. */
function Toast({
  icon = 'check',
  variant = 'default',
  action,
  onAction,
  className = '',
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: `dsr-toast${variant !== 'default' ? ` dsr-toast--${variant}` : ''} ${className}`.trim(),
    role: "status"
  }, rest), icon ? /*#__PURE__*/React.createElement("span", {
    className: "dsr-toast__icon"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 14
  })) : null, /*#__PURE__*/React.createElement("span", null, children), action ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "dsr-toast__action",
    onClick: onAction
  }, action) : null);
}
Object.assign(__ds_scope, { Toast });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Toast.jsx", error: String((e && e.message) || e) }); }

// components/ui/Tooltip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Tooltip — CSS-only label on hover/focus (350ms delay). Wraps its child. */
function Tooltip({
  label,
  side = 'top',
  className = '',
  children,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: `dsr-tooltip-host ${className}`.trim(),
    "data-tooltip": label,
    "data-side": side === 'bottom' ? 'bottom' : undefined,
    style: {
      display: 'inline-flex'
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Tooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ui/Tooltip.jsx", error: String((e && e.message) || e) }); }

// components/utilities/FilterStack.jsx
try { (() => {
let uid = 0;
const renderPrimitive = (p, i) => React.createElement(p.tag, {
  key: i,
  ...p.attrs
}, Array.isArray(p.children) ? p.children.map(renderPrimitive) : undefined);

/**
 * FilterStack — the SVG-filter plugin pattern: presets are DATA
 * (data/filters.json primitives), rendered into real <filter> defs and
 * applied to the preview (and to anything you point `target` CSS at).
 */
function FilterStack({
  presets = [],
  enabledIds,
  onToggle,
  preview,
  className = '',
  style
}) {
  const idBase = React.useMemo(() => `dsrfx-${++uid}`, []);
  const [innerOn, setInnerOn] = React.useState(() => new Set(presets.slice(0, 1).map(p => p.id)));
  const on = enabledIds ? new Set(enabledIds) : innerOn;
  const toggle = id => {
    if (onToggle) {
      onToggle(id);
      return;
    }
    setInnerOn(prev => {
      const next = new Set(prev);
      if (next.has(id)) next.delete(id);else next.add(id);
      return next;
    });
  };
  const active = presets.filter(p => on.has(p.id));
  const filterCss = active.map(p => `url(#${idBase}-${p.id})`).join(' ') || 'none';
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-filterstack ${className}`.trim(),
    style: style
  }, /*#__PURE__*/React.createElement("svg", {
    width: "0",
    height: "0",
    style: {
      position: 'absolute'
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("defs", null, presets.map(p => /*#__PURE__*/React.createElement("filter", {
    key: p.id,
    id: `${idBase}-${p.id}`,
    x: "-30%",
    y: "-30%",
    width: "160%",
    height: "160%"
  }, p.primitives.map(renderPrimitive))))), /*#__PURE__*/React.createElement("div", {
    className: "dsr-filterstack__preview"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      filter: filterCss,
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, preview || /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 40,
      height: 40,
      borderRadius: 10,
      background: 'var(--brand)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-serif)',
      fontSize: 34,
      color: 'var(--foreground)'
    }
  }, "Ag"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 40,
      height: 40,
      borderRadius: 999,
      background: 'var(--selection)'
    }
  })))), /*#__PURE__*/React.createElement("div", {
    className: "dsr-filterstack__rows"
  }, presets.map(p => /*#__PURE__*/React.createElement("div", {
    key: p.id,
    className: `dsr-filterstack__row${on.has(p.id) ? '' : ' is-off'}`
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-filterstack__icon"
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: on.has(p.id) ? 'effects' : 'adjust',
    size: 14
  })), /*#__PURE__*/React.createElement("span", {
    className: "dsr-filterstack__name"
  }, p.name), /*#__PURE__*/React.createElement(__ds_scope.Switch, {
    checked: on.has(p.id),
    onChange: () => toggle(p.id)
  })))));
}
Object.assign(__ds_scope, { FilterStack });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/utilities/FilterStack.jsx", error: String((e && e.message) || e) }); }

// components/utilities/GradientEditor.jsx
try { (() => {
/** Serialize a gradient value ({type, angle, stops}) to CSS. */
function GradientCss(g) {
  const stops = g.stops.map(s => `${s.color} ${s.position}%`).join(', ');
  if (g.type === 'radial') return `radial-gradient(circle, ${stops})`;
  if (g.type === 'conic') return `conic-gradient(from ${g.angle || 0}deg, ${stops})`;
  return `linear-gradient(${g.angle != null ? g.angle : 135}deg, ${stops})`;
}

/** GradientEditor — micro-tool: type, angle, stops, presets, CSS out. */
function GradientEditor({
  value,
  defaultValue,
  onChange,
  presets = [],
  className = '',
  style
}) {
  const [inner, setInner] = React.useState(defaultValue || {
    type: 'linear',
    angle: 135,
    stops: [{
      color: '#d97757',
      position: 0
    }, {
      color: '#4864c8',
      position: 100
    }]
  });
  const g = value !== undefined ? value : inner;
  const set = next => {
    if (value === undefined) setInner(next);
    if (onChange) onChange(next);
  };
  const patchStop = (i, patch) => {
    const stops = g.stops.map((s, j) => j === i ? {
      ...s,
      ...patch
    } : s);
    set({
      ...g,
      stops
    });
  };
  const css = GradientCss(g);
  return /*#__PURE__*/React.createElement("div", {
    className: `dsr-gradient ${className}`.trim(),
    style: style
  }, /*#__PURE__*/React.createElement("div", {
    className: "dsr-gradient__preview",
    style: {
      background: css
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.SelectField, {
    label: "Type",
    options: ['linear', 'radial', 'conic'],
    value: g.type,
    onChange: e => set({
      ...g,
      type: e.target.value
    })
  }), /*#__PURE__*/React.createElement(__ds_scope.NumberField, {
    icon: "rotate",
    unit: "deg",
    value: g.angle || 0,
    min: 0,
    max: 360,
    step: 5,
    onChange: angle => set({
      ...g,
      angle
    }),
    title: "Angle"
  })), /*#__PURE__*/React.createElement("div", {
    className: "dsr-gradient__stops"
  }, g.stops.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "dsr-gradient__stop"
  }, /*#__PURE__*/React.createElement(__ds_scope.ColorField, {
    value: s.color,
    onChange: color => patchStop(i, {
      color
    })
  }), /*#__PURE__*/React.createElement(__ds_scope.NumberField, {
    unit: "%",
    value: s.position,
    min: 0,
    max: 100,
    onChange: position => patchStop(i, {
      position
    }),
    title: "Stop position"
  }), /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    size: "sm",
    name: "remove",
    label: "Remove stop",
    tooltip: false,
    disabled: g.stops.length <= 2,
    onClick: () => set({
      ...g,
      stops: g.stops.filter((_, j) => j !== i)
    })
  }))), /*#__PURE__*/React.createElement(__ds_scope.Button, {
    size: "sm",
    variant: "ghost",
    style: {
      alignSelf: 'flex-start'
    },
    onClick: () => set({
      ...g,
      stops: [...g.stops, {
        color: '#888888',
        position: 50
      }]
    })
  }, "Add stop")), /*#__PURE__*/React.createElement("div", {
    className: "dsr-gradient__css",
    title: "Click to copy",
    onClick: () => {
      try {
        navigator.clipboard.writeText(css);
      } catch (e) {/* noop */}
    }
  }, css), presets.length ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-gradient__presets"
  }, presets.map(p => /*#__PURE__*/React.createElement("button", {
    key: p.id,
    type: "button",
    className: "dsr-gradient__preset",
    title: p.name,
    style: {
      background: GradientCss(p)
    },
    onClick: () => set({
      type: p.type,
      angle: p.angle,
      stops: p.stops
    })
  }))) : null);
}
Object.assign(__ds_scope, { GradientCss, GradientEditor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/utilities/GradientEditor.jsx", error: String((e && e.message) || e) }); }

// components/utilities/ShapeLibrary.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** ShapeLibrary — insertable generic geometry (data/shapes.json). */
function ShapeLibrary({
  shapes = [],
  onInsert,
  title = 'Shapes',
  className = '',
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.AssetGrid, _extends({
    title: title,
    count: shapes.length,
    className: className
  }, rest), shapes.map(s => /*#__PURE__*/React.createElement(__ds_scope.LibraryItem, {
    key: s.id,
    name: s.name,
    thumb: /*#__PURE__*/React.createElement("svg", {
      viewBox: "-4 -4 108 108",
      width: "26",
      height: "26",
      "aria-hidden": "true"
    }, /*#__PURE__*/React.createElement("path", {
      d: s.path,
      fill: "currentColor"
    })),
    onInsert: onInsert ? () => onInsert(s) : undefined
  })));
}
Object.assign(__ds_scope, { ShapeLibrary });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/utilities/ShapeLibrary.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/app.jsx
try { (() => {
const {
  BreakpointSwitcher: AppBp,
  Button: AppButton,
  IconButton: AppIconBtn,
  Tabs: AppTabs
} = window.DesignerDesignSystem_6adbd8;
const hashFileId = () => {
  const m = window.location.hash.match(/file=([\w-]+)/);
  return m ? m[1] : null;
};
function App() {
  const [data, setData] = React.useState(null);
  const [store, setStore] = React.useState(() => window.EDITOR_STORE.load());
  const [fid, setFid] = React.useState(() => hashFileId() || window.EDITOR_STORE.load().lastOpen);
  const [mode, setMode] = React.useState('design');
  const [bp, setBp] = React.useState('laptop');
  const [chrome] = React.useState(() => window.chromeThemeLoad());
  React.useEffect(() => {
    window.EDITOR_LOAD(setData);
  }, []);
  React.useEffect(() => {
    window.chromeApplyScale(chrome);
  }, [chrome]);
  React.useEffect(() => {
    const h = () => {
      const id = hashFileId();
      if (id) setFid(id);
    };
    window.addEventListener('hashchange', h);
    return () => window.removeEventListener('hashchange', h);
  }, []);
  if (!data) return null;
  const file = store.files[fid] || store.files[store.fileOrder[0]];
  const onPersist = patch => {
    window.EDITOR_STORE.patchFile(store, file.id, patch);
    setStore({
      ...store
    });
  };
  const openFile = id => {
    if (!store.files[id]) return;
    window.EDITOR_STORE.save({
      ...store,
      lastOpen: id
    });
    window.location.hash = `file=${id}`;
    setFid(id);
  };
  const newFile = () => {
    const f = window.EDITOR_STORE.newFile(store, 'Untitled');
    setStore({
      ...store
    });
    window.location.hash = `file=${f.id}`;
    setFid(f.id);
  };
  const goWorkspace = () => {
    window.location.href = '../../index.html';
  };
  const center = mode === 'sites' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(window.ShellTabs, {
    mode: mode,
    onMode: setMode
  }), /*#__PURE__*/React.createElement(AppBp, {
    value: bp,
    onChange: setBp
  })) : null;
  const right = mode === 'slides' || mode === 'design' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(AppIconBtn, {
    name: "play",
    label: "Present"
  }), /*#__PURE__*/React.createElement(AppButton, {
    size: "sm"
  }, "Share")) : /*#__PURE__*/React.createElement(AppButton, {
    size: "sm"
  }, mode === 'buzz' ? 'Export all' : mode === 'make' ? 'Publish' : 'Share');
  return /*#__PURE__*/React.createElement("div", {
    className: chrome.theme.mode === 'dark' ? 'dark' : '',
    style: {
      height: '100%',
      background: 'var(--background)',
      color: 'var(--foreground)',
      ...window.chromeCssVars(chrome)
    }
  }, /*#__PURE__*/React.createElement(EditorShell, {
    mode: mode,
    onMode: setMode,
    kb: data.keybindings,
    docName: file.name,
    files: store.fileOrder.map(id => store.files[id]).filter(Boolean),
    fileId: file.id,
    onOpenFile: openFile,
    onNewFile: newFile,
    onWorkspace: goWorkspace,
    center: center,
    right: right
  }, mode === 'design' ? /*#__PURE__*/React.createElement(DesignScreen, {
    key: file.id,
    data: data,
    file: file,
    onPersist: onPersist
  }) : null, mode === 'components' ? /*#__PURE__*/React.createElement(ComponentsScreen, {
    key: `c-${file.id}`,
    file: file,
    onPersist: onPersist
  }) : null, mode === 'board' ? /*#__PURE__*/React.createElement(BoardScreen, null) : null, mode === 'slides' ? /*#__PURE__*/React.createElement(SlidesScreen, null) : null, mode === 'sites' ? /*#__PURE__*/React.createElement(SitesScreen, {
    data: data,
    bp: bp
  }) : null, mode === 'buzz' ? /*#__PURE__*/React.createElement(BuzzScreen, {
    data: data
  }) : null, mode === 'make' ? /*#__PURE__*/React.createElement(MakeScreen, null) : null, mode === 'dev' ? /*#__PURE__*/React.createElement(DevScreen, {
    data: data
  }) : null));
}

// Mode tabs helper (kept for the sites-mode center slot).
const {
  Tabs: ShellTabsInner
} = window.DesignerDesignSystem_6adbd8;
function ShellTabs({
  mode,
  onMode
}) {
  return /*#__PURE__*/React.createElement(ShellTabsInner, {
    value: mode,
    onChange: onMode,
    items: [{
      value: 'design',
      label: 'Design'
    }, {
      value: 'components',
      label: 'Components'
    }, {
      value: 'board',
      label: 'Board'
    }, {
      value: 'slides',
      label: 'Slides'
    }, {
      value: 'sites',
      label: 'Sites'
    }, {
      value: 'buzz',
      label: 'Buzz'
    }, {
      value: 'make',
      label: 'Make'
    }, {
      value: 'dev',
      label: 'Dev'
    }]
  });
}
Object.assign(window, {
  ShellTabs
});
ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/app.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/chrome-theme.jsx
try { (() => {
/* Editor CHROME theme — independent from any project. Persists to its
   own localStorage key; applied as CSS vars + .dark on the editor root. */
const CHROME_THEME_KEY = 'dsrEditor.chrome.v1';
const CHROME_THEME_DEFAULTS = {
  mode: 'light',
  accent: '#4864c8',
  radius: 'lg',
  font: 'geist',
  spacing: 4,
  scale: 1
};
const CHROME_VAR_DEFAULTS = {};
const CHROME_VAR_GROUPS = [{
  name: 'Surfaces',
  vars: [{
    key: '--background',
    label: 'background',
    type: 'color',
    default: '#fdfcfa'
  }, {
    key: '--muted',
    label: 'muted',
    type: 'color',
    default: '#f5f4f1'
  }, {
    key: '--border',
    label: 'border',
    type: 'color',
    default: '#e7e5e0'
  }, {
    key: '--canvas',
    label: 'canvas',
    type: 'color',
    default: '#f5f3f0'
  }]
}, {
  name: 'Accent',
  vars: [{
    key: '--selection',
    label: 'selection',
    type: 'color',
    default: '#4864c8'
  }, {
    key: '--brand',
    label: 'brand',
    type: 'color',
    default: '#d97757'
  }]
}, {
  name: 'Density',
  vars: [{
    key: '--control-height',
    label: 'control height',
    type: 'number',
    unit: 'px',
    min: 22,
    max: 36,
    default: 28
  }, {
    key: '--row-height',
    label: 'row height',
    type: 'number',
    unit: 'px',
    min: 24,
    max: 44,
    default: 32
  }, {
    key: '--panel-width',
    label: 'panel width',
    type: 'number',
    unit: 'px',
    min: 200,
    max: 360,
    step: 4,
    default: 264
  }]
}];
const CHROME_RADIUS_SEED = {
  none: '0px',
  sm: '6px',
  md: '8px',
  lg: '10px',
  xl: '14px',
  full: '18px'
};
const CHROME_FONT_STACK = {
  geist: "'Geist', ui-sans-serif, system-ui, sans-serif",
  serif: "'Source Serif 4', Georgia, serif",
  mono: "'Geist Mono', ui-monospace, monospace",
  system: 'system-ui, sans-serif'
};
function chromeThemeLoad() {
  try {
    const raw = localStorage.getItem(CHROME_THEME_KEY);
    if (raw) {
      const s = JSON.parse(raw);
      return {
        theme: {
          ...CHROME_THEME_DEFAULTS,
          ...(s.theme || {})
        },
        vars: s.vars || {}
      };
    }
  } catch (e) {/* ignore */}
  return {
    theme: {
      ...CHROME_THEME_DEFAULTS
    },
    vars: {}
  };
}
function chromeThemeSave(state) {
  try {
    localStorage.setItem(CHROME_THEME_KEY, JSON.stringify(state));
  } catch (e) {/* ignore */}
}

/** CSS custom properties for the editor root from theme + raw overrides. */
function chromeCssVars(state) {
  const t = {
    ...CHROME_THEME_DEFAULTS,
    ...state.theme
  };
  const out = {
    '--selection': t.accent,
    '--selection-muted': `color-mix(in oklab, ${t.accent} 16%, transparent)`,
    '--ring-color': `color-mix(in oklab, ${t.accent} 35%, transparent)`,
    '--radius': CHROME_RADIUS_SEED[t.radius] || '10px',
    '--font-sans': CHROME_FONT_STACK[t.font] || CHROME_FONT_STACK.geist,
    '--control-height': `${Math.round(28 * (t.spacing / 4) * 0 + 24 + t.spacing)}px`
  };
  // spacing → density: base 4 → 28px controls; each step ±1px on controls, ±2 on rows.
  out['--control-height'] = `${24 + t.spacing}px`;
  out['--control-height-sm'] = `${20 + t.spacing}px`;
  out['--row-height'] = `${24 + t.spacing * 2}px`;
  out['--pane-header-height'] = `${30 + Math.round(t.spacing * 1.5)}px`;
  Object.entries(state.vars || {}).forEach(([k, v]) => {
    out[k] = typeof v === 'number' ? `${v}px` : v;
  });
  return out;
}

/** Font-size scale applies to the <html> root so rem tokens follow. */
function chromeApplyScale(state) {
  const t = {
    ...CHROME_THEME_DEFAULTS,
    ...state.theme
  };
  try {
    document.documentElement.style.fontSize = `${Math.round(16 * t.scale * 100) / 100}px`;
  } catch (e) {/* ignore */}
}

/** ChromeThemePanel — Theme + Variables tabs, persisted; chrome components only. */
function ChromeThemePanel({
  state,
  onState,
  onClose
}) {
  const set = next => {
    chromeThemeSave(next);
    onState(next);
  };
  return /*#__PURE__*/React.createElement(ThemePanelShell, {
    title: "Editor theme",
    theme: state.theme,
    defaults: CHROME_THEME_DEFAULTS,
    onChange: theme => set({
      ...state,
      theme
    }),
    varGroups: CHROME_VAR_GROUPS,
    varValues: state.vars,
    onVarChange: (k, v) => set({
      ...state,
      vars: {
        ...state.vars,
        [k]: v
      }
    }),
    onResetVars: () => set({
      ...state,
      vars: {}
    }),
    footnote: "Solo el chrome del editor \u2014 independiente del proyecto. Persistente en este navegador.",
    onClose: onClose
  });
}
Object.assign(window, {
  ChromeThemePanel,
  chromeThemeLoad,
  chromeThemeSave,
  chromeCssVars,
  chromeApplyScale,
  CHROME_THEME_DEFAULTS,
  CHROME_VAR_GROUPS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/chrome-theme.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/data.js
try { (() => {
// Shared demo data + JSON loader (plain script — runs before the JSX files).
window.EDITOR_DATA = {
  users: [{
    name: 'Maya Lindqvist'
  }, {
    name: 'Diego Ferrer'
  }, {
    name: 'Ana Kovač'
  }, {
    name: 'Tom Okafor'
  }, {
    name: 'Iris Chen'
  }],
  // Canvas frames for the Design screen
  frames: {
    cover: {
      x: 0,
      y: 0,
      w: 420,
      h: 300,
      name: 'Cover'
    },
    detail: {
      x: 480,
      y: 0,
      w: 300,
      h: 300,
      name: 'Detail'
    },
    card: {
      x: 0,
      y: 360,
      w: 260,
      h: 180,
      name: 'Product card'
    }
  },
  layers: [{
    id: 'cover',
    name: 'Cover',
    type: 'frame',
    children: [{
      id: 'cover-title',
      name: 'Title',
      type: 'text',
      binding: 'title'
    }, {
      id: 'cover-media',
      name: 'Media',
      type: 'image'
    }, {
      id: 'cover-bg',
      name: 'Backdrop',
      type: 'rectangle'
    }]
  }, {
    id: 'detail',
    name: 'Detail',
    type: 'frame',
    children: [{
      id: 'detail-img',
      name: 'Photo',
      type: 'image',
      binding: 'image'
    }, {
      id: 'detail-price',
      name: 'Price',
      type: 'text',
      binding: 'price'
    }]
  }, {
    id: 'card',
    name: 'Product card',
    type: 'component',
    children: [{
      id: 'card-name',
      name: 'Name',
      type: 'text'
    }, {
      id: 'card-cta',
      name: 'Buy button',
      type: 'shape'
    }]
  }],
  pages: [{
    id: 'p1',
    name: 'Page 1'
  }, {
    id: 'p2',
    name: 'Exploration'
  }],
  slides: [{
    id: 's1',
    name: 'Title',
    kind: 'title',
    title: 'Designer',
    sub: 'Components for building design tools'
  }, {
    id: 's2',
    name: 'Problem',
    kind: 'body',
    title: 'The canvas should disappear',
    bullets: ['Gray, quiet chrome', 'The work is the loudest thing', 'Every control 28px']
  }, {
    id: 's3',
    name: 'Data',
    kind: 'body',
    title: 'Collections on canvas',
    bullets: ['Bind a layer to a field', 'Repeat frames over records', 'Real content drives layout']
  }, {
    id: 's4',
    name: 'Close',
    kind: 'title',
    title: 'Keyboard first',
    sub: 'V · F · R · T — and ⌘K for the rest'
  }],
  stickies: [{
    id: 'n1',
    x: 40,
    y: 40,
    color: 'oklch(0.92 0.06 95)',
    text: 'Idea: bind opacity to stock level'
  }, {
    id: 'n2',
    x: 250,
    y: 90,
    color: 'oklch(0.90 0.05 150)',
    text: 'Ship the unit system first'
  }, {
    id: 'n3',
    x: 120,
    y: 240,
    color: 'oklch(0.90 0.05 250)',
    text: '⌘K everything'
  }],
  // Fallbacks if fetch() fails (file:// / offline)
  fallback: {
    products: {
      id: 'products',
      name: 'Products',
      icon: 'package',
      fields: [{
        key: 'title',
        label: 'Title',
        type: 'text'
      }, {
        key: 'price',
        label: 'Price',
        type: 'number',
        unit: '€'
      }, {
        key: 'image',
        label: 'Image',
        type: 'image'
      }, {
        key: 'category',
        label: 'Category',
        type: 'select'
      }, {
        key: 'inStock',
        label: 'In stock',
        type: 'boolean'
      }],
      records: [{
        id: 'p1',
        title: 'Arc Lounge Chair',
        price: 640,
        category: 'Seating',
        inStock: true
      }, {
        id: 'p2',
        title: 'Slab Coffee Table',
        price: 420,
        category: 'Tables',
        inStock: true
      }, {
        id: 'p3',
        title: 'Cone Floor Lamp',
        price: 210,
        category: 'Lighting',
        inStock: false
      }, {
        id: 'p4',
        title: 'Grid Shelving Unit',
        price: 890,
        category: 'Storage',
        inStock: true
      }, {
        id: 'p5',
        title: 'Puck Side Table',
        price: 180,
        category: 'Tables',
        inStock: true
      }, {
        id: 'p6',
        title: 'Fold Desk Lamp',
        price: 120,
        category: 'Lighting',
        inStock: false
      }]
    },
    blocks: [{
      id: 'navbar',
      name: 'Navbar',
      icon: 'menu',
      height: 64
    }, {
      id: 'hero',
      name: 'Hero',
      icon: 'star',
      height: 420
    }, {
      id: 'features',
      name: 'Features',
      icon: 'component',
      height: 320
    }, {
      id: 'gallery',
      name: 'Gallery',
      icon: 'image',
      height: 360
    }, {
      id: 'quote',
      name: 'Quote',
      icon: 'quote',
      height: 240
    }, {
      id: 'cta',
      name: 'CTA band',
      icon: 'play',
      height: 200
    }, {
      id: 'footer',
      name: 'Footer',
      icon: 'menu',
      height: 220
    }],
    keybindings: {
      groups: [{
        id: 'tools',
        name: 'Tools',
        commands: [{
          id: 'tool.select',
          title: 'Select',
          icon: 'select',
          keys: ['V']
        }, {
          id: 'tool.frame',
          title: 'Frame',
          icon: 'frame',
          keys: ['F']
        }, {
          id: 'tool.rectangle',
          title: 'Rectangle',
          icon: 'rectangle',
          keys: ['R']
        }, {
          id: 'tool.text',
          title: 'Text',
          icon: 'text',
          keys: ['T']
        }]
      }, {
        id: 'file',
        name: 'File',
        commands: [{
          id: 'file.palette',
          title: 'Command palette',
          icon: 'search',
          keys: ['mod', 'K']
        }, {
          id: 'file.export',
          title: 'Export…',
          icon: 'download',
          keys: ['mod', 'shift', 'E']
        }]
      }]
    },
    interactions: {
      triggers: [{
        id: 'click',
        name: 'On click'
      }, {
        id: 'hover',
        name: 'While hovering'
      }],
      actions: [{
        id: 'navigate',
        name: 'Navigate to'
      }, {
        id: 'overlay',
        name: 'Open overlay'
      }],
      transitions: [{
        id: 'instant',
        name: 'Instant'
      }, {
        id: 'smart',
        name: 'Smart animate'
      }, {
        id: 'push',
        name: 'Push'
      }],
      easings: [{
        id: 'ease-standard',
        name: 'Standard'
      }, {
        id: 'ease-out',
        name: 'Ease out'
      }],
      durations: {
        default: 240,
        min: 0,
        max: 2000,
        step: 20
      }
    }
  }
};

// Load real JSON when served over http; merge onto fallbacks.
window.EDITOR_LOAD = function (cb) {
  var out = {
    products: window.EDITOR_DATA.fallback.products,
    blocks: window.EDITOR_DATA.fallback.blocks,
    keybindings: window.EDITOR_DATA.fallback.keybindings,
    interactions: window.EDITOR_DATA.fallback.interactions,
    shapes: [],
    gradients: []
  };
  var jobs = [['../../data/collections/products.json', function (d) {
    out.products = d;
  }], ['../../data/blocks.json', function (d) {
    out.blocks = d.blocks;
  }], ['../../data/keybindings.json', function (d) {
    out.keybindings = d;
  }], ['../../data/interactions.json', function (d) {
    out.interactions = d;
  }], ['../../data/shapes.json', function (d) {
    out.shapes = d.shapes;
  }], ['../../data/gradients.json', function (d) {
    out.gradients = d.presets;
  }]];
  var left = jobs.length;
  jobs.forEach(function (j) {
    fetch(j[0]).then(function (r) {
      return r.json();
    }).then(j[1]).catch(function () {}).finally(function () {
      left -= 1;
      if (left === 0) cb(out);
    });
  });
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/data.js", error: String((e && e.message) || e) }); }

// ui_kits/editor/inspector-panes.jsx
try { (() => {
/* Inspector panes + component registry for the visual component editor.
   Loaded before screen-design.jsx; exports via window. */
const {
  Pane: IpPane,
  Label: IpLabel,
  IconButton: IpIconBtn,
  Badge: IpBadge,
  TextField: IpText,
  SelectField: IpSelect,
  BooleanField: IpBool,
  NumberField: IpNum,
  ColorField: IpColor,
  ActionFill: IpFill,
  GradientEditor: IpGrad,
  AssetGrid: IpGrid,
  LibraryItem: IpItem,
  Tabs: IpTabs
} = window.DesignerDesignSystem_6adbd8;
const IpNS = window.DesignerDesignSystem_6adbd8;
const IpGradientCss = IpNS.GradientCss || (g => `linear-gradient(${g.angle || 135}deg, ${g.stops.map(s => `${s.color} ${s.position}%`).join(', ')})`);

/* ── deployable component registry ─────────────────────────────── */
const COMPONENT_DEFS = {
  Button: {
    icon: 'component',
    w: 120,
    h: 28,
    defaults: {
      children: 'Publish',
      variant: 'primary',
      size: 'md'
    },
    props: {
      children: {
        label: 'Label',
        type: 'text'
      },
      variant: {
        type: 'enum',
        options: ['primary', 'secondary', 'outline', 'ghost', 'destructive', 'link']
      },
      size: {
        type: 'enum',
        options: ['sm', 'md', 'lg']
      },
      disabled: {
        type: 'boolean'
      }
    }
  },
  Badge: {
    icon: 'component',
    w: 76,
    h: 18,
    defaults: {
      children: 'Updated',
      variant: 'secondary'
    },
    props: {
      children: {
        label: 'Label',
        type: 'text'
      },
      variant: {
        type: 'enum',
        options: ['default', 'secondary', 'outline', 'destructive', 'brand', 'selection']
      }
    }
  },
  Input: {
    icon: 'field-text',
    w: 180,
    h: 28,
    defaults: {
      placeholder: 'Search layers…',
      size: 'md'
    },
    props: {
      placeholder: {
        type: 'text'
      },
      size: {
        type: 'enum',
        options: ['sm', 'md']
      },
      quiet: {
        type: 'boolean'
      },
      mono: {
        type: 'boolean'
      },
      disabled: {
        type: 'boolean'
      }
    }
  },
  Switch: {
    icon: 'field-boolean',
    w: 150,
    h: 20,
    defaults: {
      label: 'Autosave',
      defaultChecked: true
    },
    props: {
      label: {
        type: 'text'
      },
      defaultChecked: {
        label: 'On',
        type: 'boolean',
        remount: true
      }
    }
  },
  Checkbox: {
    icon: 'check',
    w: 150,
    h: 20,
    defaults: {
      label: 'Clip content',
      defaultChecked: true
    },
    props: {
      label: {
        type: 'text'
      },
      defaultChecked: {
        label: 'Checked',
        type: 'boolean',
        remount: true
      }
    }
  },
  Avatar: {
    icon: 'users',
    w: 32,
    h: 32,
    defaults: {
      name: 'Maya Lindqvist',
      presence: true,
      size: 'lg'
    },
    props: {
      name: {
        type: 'text'
      },
      presence: {
        type: 'boolean'
      },
      size: {
        type: 'enum',
        options: ['sm', 'md', 'lg']
      }
    }
  },
  Kbd: {
    icon: 'keyboard',
    w: 60,
    h: 18,
    defaults: {
      keysText: 'mod,K'
    },
    props: {
      keysText: {
        label: 'Keys',
        type: 'text',
        hint: 'coma-separado'
      }
    },
    map: p => ({
      keys: String(p.keysText || '').split(',').map(s => s.trim()).filter(Boolean)
    })
  },
  IconButton: {
    icon: 'select',
    w: 28,
    h: 28,
    defaults: {
      name: 'select',
      label: 'Tool',
      active: true
    },
    props: {
      name: {
        label: 'Icon role',
        type: 'text'
      },
      label: {
        type: 'text'
      },
      active: {
        type: 'boolean'
      }
    }
  }
};
function RenderComponent({
  el
}) {
  const def = COMPONENT_DEFS[el.component];
  const C = IpNS[el.component];
  if (!def || !C) return null;
  const raw = {
    ...def.defaults,
    ...el.props
  };
  const props = def.map ? {
    ...raw,
    ...def.map(raw)
  } : raw;
  if (def.map) delete props.keysText;
  const key = JSON.stringify(props); // remount on prop change (defaultChecked etc.)
  return /*#__PURE__*/React.createElement("div", {
    key: key,
    style: {
      pointerEvents: 'none',
      display: 'flex',
      alignItems: 'center',
      width: '100%',
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(C, props));
}

/* ── computed visual style (fills, shadows, stroke) ────────────── */
const hexA = (hex, alpha) => {
  const a = Math.round(Math.max(0, Math.min(100, alpha == null ? 100 : alpha)) * 2.55);
  return `${hex}${a.toString(16).padStart(2, '0')}`;
};
function elVisualStyle(el) {
  const s = {};
  // fill
  const ft = el.fillType || (el.gradient ? 'gradient' : 'solid');
  if (el.type === 'rectangle' || el.type === 'ellipse' || el.type === 'frame') {
    if (ft === 'none') s.background = 'transparent';else if (ft === 'gradient' && el.gradient) s.background = IpGradientCss(el.gradient);else if (el.type !== 'frame' || el.fill) s.background = el.fill || undefined;
  }
  // stroke
  if (el.strokeWidth) s.boxShadow = `inset 0 0 0 ${el.strokeWidth}px ${el.stroke || '#1a1a1a'}`;
  // shadows
  if (el.shadows && el.shadows.length) {
    const sh = el.shadows.map(x => `${x.x}px ${x.y}px ${x.blur}px ${x.spread || 0}px ${hexA(x.color || '#000000', x.alpha != null ? x.alpha : 24)}`).join(', ');
    s.boxShadow = s.boxShadow ? `${s.boxShadow}, ${sh}` : sh;
  }
  if (el.type === 'rectangle' || el.type === 'frame') s.borderRadius = el.radius != null ? el.radius : el.type === 'frame' ? 0 : 10;
  if (el.type === 'ellipse') s.borderRadius = '50%';
  return s;
}
const DEFAULT_GRADIENT = {
  type: 'linear',
  angle: 135,
  stops: [{
    color: '#d97757',
    position: 0
  }, {
    color: '#4864c8',
    position: 100
  }]
};

/* ── panes ─────────────────────────────────────────────────────── */
function FillPane({
  el,
  onPatch,
  gradients
}) {
  const ft = el.fillType || 'solid';
  return /*#__PURE__*/React.createElement(IpPane, {
    title: "Fill"
  }, /*#__PURE__*/React.createElement(IpSelect, {
    label: "Type",
    options: [{
      value: 'none',
      label: 'None'
    }, {
      value: 'solid',
      label: 'Solid'
    }, {
      value: 'gradient',
      label: 'Gradient'
    }],
    value: ft,
    onChange: e => onPatch({
      fillType: e.target.value,
      gradient: e.target.value === 'gradient' ? el.gradient || DEFAULT_GRADIENT : el.gradient
    })
  }), ft === 'solid' ? /*#__PURE__*/React.createElement(IpFill, {
    value: el.fill || '#d97757',
    onChange: fill => onPatch({
      fill
    })
  }) : null, ft === 'gradient' ? /*#__PURE__*/React.createElement(IpGrad, {
    value: el.gradient || DEFAULT_GRADIENT,
    onChange: gradient => onPatch({
      gradient
    }),
    presets: gradients || []
  }) : null);
}
function StrokePane({
  el,
  onPatch
}) {
  return /*#__PURE__*/React.createElement(IpPane, {
    title: "Stroke",
    defaultOpen: !!el.strokeWidth
  }, /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Width"), /*#__PURE__*/React.createElement(IpNum, {
    label: "W",
    unit: "px",
    value: el.strokeWidth || 0,
    min: 0,
    max: 20,
    onChange: strokeWidth => onPatch({
      strokeWidth
    }),
    title: "Stroke width"
  })), /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Color"), /*#__PURE__*/React.createElement(IpColor, {
    value: el.stroke || '#1a1a1a',
    onChange: stroke => onPatch({
      stroke
    })
  })));
}
function EffectsPane({
  el,
  onPatch
}) {
  const shadows = el.shadows || [];
  const patchShadow = (i, p) => onPatch({
    shadows: shadows.map((s, j) => j === i ? {
      ...s,
      ...p
    } : s)
  });
  return /*#__PURE__*/React.createElement(IpPane, {
    title: "Effects",
    defaultOpen: !!shadows.length,
    actions: /*#__PURE__*/React.createElement(IpIconBtn, {
      size: "sm",
      name: "add",
      label: "Add shadow",
      tooltip: false,
      onClick: () => onPatch({
        shadows: [...shadows, {
          x: 0,
          y: 8,
          blur: 24,
          spread: -4,
          color: '#000000',
          alpha: 24
        }]
      })
    })
  }, shadows.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      color: 'var(--muted-foreground)'
    }
  }, "Sin sombras. A\xF1ade una con +.") : null, shadows.map((sh, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4,
      paddingBottom: 6,
      borderBottom: i < shadows.length - 1 ? '1px solid var(--border)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement(IpBadge, {
    variant: "outline"
  }, "shadow ", i + 1), /*#__PURE__*/React.createElement(IpIconBtn, {
    size: "sm",
    name: "remove",
    label: "Remove",
    tooltip: false,
    onClick: () => onPatch({
      shadows: shadows.filter((_, j) => j !== i)
    })
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(IpNum, {
    label: "X",
    unit: "px",
    value: sh.x,
    min: -64,
    max: 64,
    onChange: x => patchShadow(i, {
      x
    }),
    title: "Offset X"
  }), /*#__PURE__*/React.createElement(IpNum, {
    label: "Y",
    unit: "px",
    value: sh.y,
    min: -64,
    max: 64,
    onChange: y => patchShadow(i, {
      y
    }),
    title: "Offset Y"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(IpNum, {
    label: "B",
    unit: "px",
    value: sh.blur,
    min: 0,
    max: 128,
    onChange: blur => patchShadow(i, {
      blur
    }),
    title: "Blur"
  }), /*#__PURE__*/React.createElement(IpNum, {
    label: "S",
    unit: "px",
    value: sh.spread || 0,
    min: -32,
    max: 32,
    onChange: spread => patchShadow(i, {
      spread
    }),
    title: "Spread"
  })), /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Color"), /*#__PURE__*/React.createElement(IpColor, {
    value: sh.color || '#000000',
    onChange: color => patchShadow(i, {
      color
    }),
    alpha: sh.alpha != null ? sh.alpha : 24,
    onAlphaChange: alpha => patchShadow(i, {
      alpha
    })
  })))));
}
function AutoLayoutPane({
  el,
  onPatch
}) {
  const lay = el.layout || {
    direction: 'none',
    gap: 12,
    padding: 16,
    align: 'start'
  };
  const set = p => onPatch({
    layout: {
      ...lay,
      ...p
    }
  });
  return /*#__PURE__*/React.createElement(IpPane, {
    title: "Auto layout",
    defaultOpen: lay.direction !== 'none'
  }, /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Direction"), /*#__PURE__*/React.createElement(IpTabs, {
    full: true,
    value: lay.direction,
    onChange: direction => set({
      direction
    }),
    items: [{
      value: 'none',
      label: '—'
    }, {
      value: 'row',
      label: '→'
    }, {
      value: 'column',
      label: '↓'
    }]
  })), lay.direction !== 'none' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(IpNum, {
    label: "G",
    unit: "px",
    value: lay.gap,
    min: 0,
    max: 96,
    step: 4,
    onChange: gap => set({
      gap
    }),
    title: "Gap"
  }), /*#__PURE__*/React.createElement(IpNum, {
    label: "P",
    unit: "px",
    value: lay.padding,
    min: 0,
    max: 96,
    step: 4,
    onChange: padding => set({
      padding
    }),
    title: "Padding"
  })), /*#__PURE__*/React.createElement(IpSelect, {
    label: "Align",
    value: lay.align,
    options: [{
      value: 'start',
      label: 'Start'
    }, {
      value: 'center',
      label: 'Center'
    }, {
      value: 'end',
      label: 'End'
    }, {
      value: 'space-between',
      label: 'Space between'
    }],
    onChange: e => set({
      align: e.target.value
    })
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      color: 'var(--muted-foreground)',
      lineHeight: 1.4
    }
  }, "Los hijos del frame se ordenan por flex; crea elementos con click dentro del frame.")) : null);
}
function ComponentPropsPane({
  el,
  onPatch
}) {
  const def = COMPONENT_DEFS[el.component];
  if (!def) return null;
  const raw = {
    ...def.defaults,
    ...el.props
  };
  const set = (k, v) => onPatch({
    props: {
      ...raw,
      [k]: v
    }
  });
  return /*#__PURE__*/React.createElement(IpPane, {
    title: `Props · ${el.component}`
  }, Object.entries(def.props).map(([k, spec]) => {
    const label = spec.label || k;
    const val = raw[k];
    if (spec.type === 'enum') {
      return /*#__PURE__*/React.createElement(IpSelect, {
        key: k,
        label: label,
        options: spec.options,
        value: String(val != null ? val : spec.options[0]),
        onChange: e => set(k, e.target.value)
      });
    }
    if (spec.type === 'boolean') {
      return /*#__PURE__*/React.createElement(IpBool, {
        key: k,
        label: label,
        checked: !!val,
        onChange: e => set(k, e.target.checked)
      });
    }
    return /*#__PURE__*/React.createElement(IpText, {
      key: k,
      label: label,
      value: String(val != null ? val : ''),
      onChange: e => set(k, e.target.value)
    });
  }));
}
function InsertSection({
  onInsert
}) {
  return /*#__PURE__*/React.createElement(IpGrid, {
    title: "Components",
    count: Object.keys(COMPONENT_DEFS).length
  }, Object.keys(COMPONENT_DEFS).map(name => /*#__PURE__*/React.createElement(IpItem, {
    key: name,
    name: name,
    icon: COMPONENT_DEFS[name].icon || 'component',
    onInsert: () => onInsert(name)
  })));
}
Object.assign(window, {
  COMPONENT_DEFS,
  RenderComponent,
  elVisualStyle,
  FillPane,
  StrokePane,
  EffectsPane,
  AutoLayoutPane,
  ComponentPropsPane,
  InsertSection
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/inspector-panes.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/project-components.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Project base components — styled ONLY from the project's variables
   (accent, radius, font, spacing, scale, mode). Deployables of the
   DOCUMENT, not the chrome. + ComponentsScreen: editable catalog page. */
const {
  Panel: PcPanel,
  Label: PcLabel,
  Input: PcInput,
  SelectField: PcSelect,
  TextField: PcText
} = window.DesignerDesignSystem_6adbd8;

/* helpers: resolve project theme → concrete values */
function pcTokens(theme) {
  const t = {
    ...PROJECT_THEME_DEFAULTS,
    ...theme
  };
  const doc = themeDoc(t);
  return {
    accent: t.accent,
    radius: THEME_RADIUS_PX[t.radius],
    font: THEME_FONTS[t.font],
    sp: t.spacing,
    scale: t.scale,
    surface: doc.surface,
    text: doc.text,
    muted: doc.muted
  };
}

/* ── the base set ──────────────────────────────────────────────── */
function PButton({
  theme,
  label = 'Action',
  kind = 'primary'
}) {
  const k = pcTokens(theme);
  const solid = kind === 'primary';
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    style: {
      fontFamily: k.font,
      fontSize: 14 * k.scale,
      fontWeight: 500,
      padding: `${k.sp * 2}px ${k.sp * 4}px`,
      borderRadius: Math.min(k.radius, 24),
      border: solid ? '1px solid transparent' : `1px solid ${k.text}33`,
      background: solid ? k.accent : 'transparent',
      color: solid ? '#fff' : k.text,
      cursor: 'pointer'
    }
  }, label);
}
function PTag({
  theme,
  label = 'Tag'
}) {
  const k = pcTokens(theme);
  return /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: k.font,
      fontSize: 11 * k.scale,
      fontWeight: 500,
      padding: `${k.sp / 2}px ${k.sp * 1.5}px`,
      borderRadius: Math.min(k.radius, 999),
      background: `${k.accent}22`,
      color: k.accent,
      display: 'inline-block'
    }
  }, label);
}
function PInputBase({
  theme,
  placeholder = 'Email address'
}) {
  const k = pcTokens(theme);
  return /*#__PURE__*/React.createElement("input", {
    placeholder: placeholder,
    style: {
      fontFamily: k.font,
      fontSize: 14 * k.scale,
      padding: `${k.sp * 2}px ${k.sp * 3}px`,
      borderRadius: Math.min(k.radius, 16),
      border: `1px solid ${k.text}2e`,
      background: k.surface,
      color: k.text,
      outline: 'none',
      width: '100%',
      boxSizing: 'border-box'
    }
  });
}
function PCard({
  theme,
  title = 'Card title',
  body = 'Supporting copy for the card.',
  cta = 'Learn more'
}) {
  const k = pcTokens(theme);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: k.surface,
      color: k.text,
      border: `1px solid ${k.text}1f`,
      borderRadius: k.radius,
      padding: k.sp * 4,
      display: 'flex',
      flexDirection: 'column',
      gap: k.sp * 2,
      width: '100%',
      boxSizing: 'border-box',
      boxShadow: '0 4px 16px -8px rgb(0 0 0 / 0.15)'
    }
  }, /*#__PURE__*/React.createElement(PTag, {
    theme: theme,
    label: "New"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: k.font,
      fontSize: 17 * k.scale,
      fontWeight: 600,
      letterSpacing: '-0.01em'
    }
  }, title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: k.font,
      fontSize: 13 * k.scale,
      opacity: 0.65,
      lineHeight: 1.5
    }
  }, body), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(PButton, {
    theme: theme,
    label: cta
  })));
}
function PHeading({
  theme,
  text = 'Design at 60fps'
}) {
  const k = pcTokens(theme);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: k.font,
      fontSize: 30 * k.scale,
      fontWeight: 600,
      letterSpacing: '-0.02em',
      color: k.text
    }
  }, text);
}
function PSwitchBase({
  theme,
  on = true
}) {
  const k = pcTokens(theme);
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      width: 36,
      height: 20,
      borderRadius: 999,
      background: on ? k.accent : `${k.text}30`,
      position: 'relative',
      transition: 'background 120ms'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 2,
      left: on ? 18 : 2,
      width: 16,
      height: 16,
      borderRadius: 999,
      background: '#fff',
      boxShadow: '0 1px 2px rgb(0 0 0 / 0.2)',
      transition: 'left 120ms'
    }
  }));
}

/* Catalog registry: name → {render, samples editable via fields} */
const PROJECT_COMPONENTS = {
  Heading: {
    fields: {
      text: 'text'
    },
    defaults: {
      text: 'Design at 60fps'
    },
    render: (th, p) => /*#__PURE__*/React.createElement(PHeading, _extends({
      theme: th
    }, p))
  },
  Button: {
    fields: {
      label: 'text',
      kind: ['primary', 'outline']
    },
    defaults: {
      label: 'Get started',
      kind: 'primary'
    },
    render: (th, p) => /*#__PURE__*/React.createElement(PButton, _extends({
      theme: th
    }, p))
  },
  Tag: {
    fields: {
      label: 'text'
    },
    defaults: {
      label: 'New'
    },
    render: (th, p) => /*#__PURE__*/React.createElement(PTag, _extends({
      theme: th
    }, p))
  },
  Input: {
    fields: {
      placeholder: 'text'
    },
    defaults: {
      placeholder: 'Email address'
    },
    render: (th, p) => /*#__PURE__*/React.createElement(PInputBase, _extends({
      theme: th
    }, p))
  },
  Switch: {
    fields: {
      on: ['on', 'off']
    },
    defaults: {
      on: 'on'
    },
    render: (th, p) => /*#__PURE__*/React.createElement(PSwitchBase, {
      theme: th,
      on: p.on !== 'off'
    })
  },
  Card: {
    fields: {
      title: 'text',
      body: 'text',
      cta: 'text'
    },
    defaults: {
      title: 'Arc Lounge Chair',
      body: 'Bent oak, wool upholstery. Ships in 2 weeks.',
      cta: 'Buy · 640 €'
    },
    render: (th, p) => /*#__PURE__*/React.createElement(PCard, _extends({
      theme: th
    }, p))
  }
};

/* ── Components page (mode): editable catalog bound to project vars ── */
function ComponentsScreen({
  file,
  onPersist
}) {
  const [theme, setTheme] = React.useState({
    ...PROJECT_THEME_DEFAULTS,
    ...(file.theme || {})
  });
  const [samples, setSamples] = React.useState(file.samples || {});
  const [selName, setSelName] = React.useState('Card');
  React.useEffect(() => {
    if (!onPersist) return undefined;
    const t = setTimeout(() => onPersist({
      theme,
      samples
    }), 350);
    return () => clearTimeout(t);
  }, [theme, samples]);
  const k = pcTokens(theme);
  const def = PROJECT_COMPONENTS[selName];
  const sample = {
    ...def.defaults,
    ...(samples[selName] || {})
  };
  const setSample = (kk, v) => setSamples(s => ({
    ...s,
    [selName]: {
      ...sample,
      [kk]: v
    }
  }));
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      overflowY: 'auto',
      background: 'var(--canvas)',
      padding: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 860,
      margin: '0 auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(PcLabel, {
    eyebrow: true
  }, "Base components \xB7 vinculados a las variables del proyecto"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fill, minmax(240px, 1fr))',
      gap: 14
    }
  }, Object.entries(PROJECT_COMPONENTS).map(([name, d]) => {
    const p = {
      ...d.defaults,
      ...(samples[name] || {})
    };
    const selected = selName === name;
    return /*#__PURE__*/React.createElement("div", {
      key: name,
      onClick: () => setSelName(name),
      style: {
        background: k.surface,
        borderRadius: 10,
        cursor: 'pointer',
        border: '1px solid var(--border)',
        outline: selected ? '1.5px solid var(--selection)' : 'none',
        outlineOffset: -1,
        display: 'flex',
        flexDirection: 'column',
        minHeight: 150
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 18
      }
    }, d.render(theme, p)), /*#__PURE__*/React.createElement("div", {
      style: {
        padding: '6px 10px',
        borderTop: '1px solid var(--border)',
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--text-ui-sm)',
        fontWeight: 'var(--font-weight-medium)',
        color: 'var(--foreground)'
      }
    }, name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 9.5,
        color: 'var(--muted-foreground)'
      }
    }, "project")));
  })))), /*#__PURE__*/React.createElement(PcPanel, {
    side: "right",
    width: 300
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 12,
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(PcLabel, {
    eyebrow: true,
    style: {
      display: 'block',
      marginBottom: 6
    }
  }, "Sample \xB7 ", selName), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, Object.entries(def.fields).map(([kk, kind]) => Array.isArray(kind) ? /*#__PURE__*/React.createElement(PcSelect, {
    key: kk,
    label: kk,
    options: kind,
    value: String(sample[kk]),
    onChange: e => setSample(kk, e.target.value)
  }) : /*#__PURE__*/React.createElement(PcText, {
    key: kk,
    label: kk,
    value: String(sample[kk]),
    onChange: e => setSample(kk, e.target.value)
  })))), /*#__PURE__*/React.createElement(window.ProjectThemePanel, {
    theme: theme,
    onChange: setTheme,
    varValues: theme.vars || {},
    onVarChange: (kk, v) => setTheme(t => ({
      ...t,
      vars: {
        ...(t.vars || {}),
        [kk]: v
      }
    })),
    onResetVars: () => setTheme(t => ({
      ...t,
      vars: {}
    })),
    onClose: () => {}
  }))));
}
Object.assign(window, {
  ComponentsScreen,
  PROJECT_COMPONENTS,
  pcTokens
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/project-components.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/screen-board-slides.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const {
  Panel: BsPanel,
  Canvas: BsCanvas,
  FloatingToolbar,
  IconButton: BsIconBtn,
  Separator: BsSep,
  Filmstrip,
  Button: BsButton,
  ZoomControls: BsZoom,
  Label: BsLabel,
  TextField: BsTextField,
  ConnectionLayer: BsNoodles
} = window.DesignerDesignSystem_6adbd8;

/* ══ Board (whiteboard / FigJam class) ══════════════════════════
   Functional: stickies (add/drag/edit), shapes (add/drag/label),
   pen strokes (freehand), connectors (click A → click B), delete. */
const NOTE_COLORS = ['oklch(0.92 0.06 95)', 'oklch(0.90 0.05 150)', 'oklch(0.90 0.05 250)', 'oklch(0.91 0.05 25)'];
let bsSeq = 1;
const bsId = p => `${p}${Date.now().toString(36).slice(-4)}${bsSeq++}`;
function BsDraggable({
  item,
  zoom,
  selected,
  onSelect,
  onMove,
  children,
  style
}) {
  const drag = e => {
    if (e.button !== 0) return;
    e.stopPropagation();
    onSelect(e);
    const sx = e.clientX;
    const sy = e.clientY;
    const move = ev => onMove({
      x: Math.round(item.x + (ev.clientX - sx) / zoom),
      y: Math.round(item.y + (ev.clientY - sy) / zoom)
    });
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    "data-canvas-item": true,
    onPointerDown: drag,
    style: {
      position: 'absolute',
      left: item.x,
      top: item.y,
      cursor: 'default',
      ...style
    }
  }, children);
}
function BoardScreen() {
  const [tool, setTool] = React.useState('select');
  const [viewState, setViewState] = React.useState({
    zoom: 1,
    x: 120,
    y: 60
  });
  const [notes, setNotes] = React.useState(() => window.EDITOR_DATA.stickies.map((n, i) => ({
    ...n,
    tilt: i % 2 ? -1 : 1.5
  })));
  const [shapes, setShapes] = React.useState([{
    id: 'sh1',
    x: 470,
    y: 150,
    w: 150,
    h: 90,
    label: 'Unit system'
  }]);
  const [strokes, setStrokes] = React.useState([]);
  const [connectors, setConnectors] = React.useState([{
    id: 'c1',
    from: 'n2',
    to: 'sh1'
  }]);
  const [sel, setSel] = React.useState(null);
  const [pendingLink, setPendingLink] = React.useState(null);
  const [liveStroke, setLiveStroke] = React.useState(null);
  const patchNote = (id, p) => setNotes(ns => ns.map(n => n.id === id ? {
    ...n,
    ...p
  } : n));
  const patchShape = (id, p) => setShapes(ss => ss.map(s => s.id === id ? {
    ...s,
    ...p
  } : s));
  const itemBox = id => {
    const n = notes.find(x => x.id === id);
    if (n) return {
      x: n.x,
      y: n.y,
      w: 168,
      h: 128
    };
    const s = shapes.find(x => x.id === id);
    if (s) return {
      x: s.x,
      y: s.y,
      w: s.w,
      h: s.h
    };
    return null;
  };
  const noodleList = connectors.map(c => {
    const a = itemBox(c.from);
    const b = itemBox(c.to);
    if (!a || !b) return null;
    const leftFirst = a.x + a.w / 2 <= b.x + b.w / 2;
    return {
      id: c.id,
      from: {
        x: leftFirst ? a.x + a.w : a.x,
        y: a.y + a.h / 2
      },
      to: {
        x: leftFirst ? b.x : b.x + b.w,
        y: b.y + b.h / 2
      }
    };
  }).filter(Boolean);

  // Click an item with the connector tool: first click = source, second = target.
  const linkClick = id => {
    if (tool !== 'line') return false;
    if (!pendingLink) {
      setPendingLink(id);
      return true;
    }
    if (pendingLink !== id) setConnectors(cs => [...cs, {
      id: bsId('c'),
      from: pendingLink,
      to: id
    }]);
    setPendingLink(null);
    setTool('select');
    return true;
  };
  const worldPoint = (e, rect) => [Math.round((e.clientX - rect.left - viewState.x) / viewState.zoom), Math.round((e.clientY - rect.top - viewState.y) / viewState.zoom)];
  const onCanvasDown = e => {
    const rect = e.currentTarget.getBoundingClientRect();
    if (tool === 'text') {
      const [x, y] = worldPoint(e, rect);
      const id = bsId('n');
      setNotes(ns => [...ns, {
        id,
        text: 'New note',
        x: x - 84,
        y: y - 64,
        color: NOTE_COLORS[ns.length % NOTE_COLORS.length],
        tilt: ns.length % 2 ? -1 : 1
      }]);
      setSel(id);
      setTool('select');
      return;
    }
    if (tool === 'rectangle') {
      const [x, y] = worldPoint(e, rect);
      const id = bsId('sh');
      setShapes(ss => [...ss, {
        id,
        x: x - 75,
        y: y - 45,
        w: 150,
        h: 90,
        label: 'Label'
      }]);
      setSel(id);
      setTool('select');
      return;
    }
    if (tool === 'pen') {
      const pts = [worldPoint(e, rect)];
      setLiveStroke({
        points: pts
      });
      const move = ev => {
        pts.push(worldPoint(ev, rect));
        setLiveStroke({
          points: [...pts]
        });
      };
      const up = () => {
        window.removeEventListener('pointermove', move);
        window.removeEventListener('pointerup', up);
        if (pts.length > 2) setStrokes(st => [...st, {
          id: bsId('st'),
          points: pts
        }]);
        setLiveStroke(null);
      };
      window.addEventListener('pointermove', move);
      window.addEventListener('pointerup', up);
      return;
    }
    setSel(null);
    setPendingLink(null);
  };
  React.useEffect(() => {
    const h = e => {
      if (e.target.closest && e.target.closest('input, textarea, [contenteditable="true"]')) return;
      if ((e.key === 'Backspace' || e.key === 'Delete') && sel) {
        e.preventDefault();
        setNotes(ns => ns.filter(n => n.id !== sel));
        setShapes(ss => ss.filter(s => s.id !== sel));
        setStrokes(st => st.filter(s => s.id !== sel));
        setConnectors(cs => cs.filter(c => c.id !== sel && c.from !== sel && c.to !== sel));
        setSel(null);
      }
    };
    window.addEventListener('keydown', h);
    return () => window.removeEventListener('keydown', h);
  }, [sel]);
  const strokePath = pts => 'M ' + pts.map(p => `${p[0]} ${p[1]}`).join(' L ');
  const linkable = tool === 'line';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(BsCanvas, {
    view: viewState,
    onViewChange: setViewState,
    gridSize: 24,
    panOnDrag: tool === 'select' || tool === 'hand',
    onBackgroundPointerDown: onCanvasDown,
    style: {
      cursor: tool === 'pen' ? 'crosshair' : tool === 'text' || tool === 'rectangle' ? 'copy' : undefined
    }
  }, /*#__PURE__*/React.createElement("svg", {
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      overflow: 'visible',
      pointerEvents: 'none'
    },
    width: "1",
    height: "1",
    "aria-hidden": "true"
  }, strokes.map(s => /*#__PURE__*/React.createElement("path", {
    key: s.id,
    d: strokePath(s.points),
    fill: "none",
    stroke: sel === s.id ? 'var(--selection)' : 'var(--foreground)',
    strokeWidth: "2.5",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      pointerEvents: 'stroke',
      cursor: 'default'
    },
    onPointerDown: e => {
      e.stopPropagation();
      setSel(s.id);
    }
  })), liveStroke ? /*#__PURE__*/React.createElement("path", {
    d: strokePath(liveStroke.points),
    fill: "none",
    stroke: "var(--foreground)",
    strokeWidth: "2.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }) : null), /*#__PURE__*/React.createElement(BsNoodles, {
    connections: noodleList,
    activeId: sel
  }), shapes.map(s => /*#__PURE__*/React.createElement(BsDraggable, {
    key: s.id,
    item: s,
    zoom: viewState.zoom,
    selected: sel === s.id,
    onSelect: () => {
      if (!linkClick(s.id)) setSel(s.id);
    },
    onMove: ({
      x,
      y
    }) => patchShape(s.id, {
      x,
      y
    }),
    style: {
      width: s.w,
      height: s.h,
      borderRadius: 10,
      background: 'var(--frame)',
      border: `1.5px solid ${sel === s.id || pendingLink === s.id ? 'var(--selection)' : 'var(--foreground)'}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontSize: 13,
      boxShadow: pendingLink === s.id ? '0 0 0 3px var(--selection-muted)' : undefined,
      outline: linkable ? '2px dashed var(--selection-muted)' : 'none',
      outlineOffset: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    contentEditable: true,
    suppressContentEditableWarning: true,
    onPointerDown: e => e.stopPropagation(),
    onBlur: e => patchShape(s.id, {
      label: e.currentTarget.textContent
    }),
    style: {
      outline: 'none',
      cursor: 'text',
      textAlign: 'center',
      padding: '0 8px'
    }
  }, s.label))), notes.map(n => /*#__PURE__*/React.createElement(BsDraggable, {
    key: n.id,
    item: n,
    zoom: viewState.zoom,
    selected: sel === n.id,
    onSelect: () => {
      if (!linkClick(n.id)) setSel(n.id);
    },
    onMove: ({
      x,
      y
    }) => patchNote(n.id, {
      x,
      y
    }),
    style: {
      width: 168,
      minHeight: 128,
      background: n.color,
      borderRadius: 4,
      padding: 14,
      fontSize: 13,
      lineHeight: 1.4,
      color: 'oklch(0.25 0.02 80)',
      boxShadow: sel === n.id || pendingLink === n.id ? '0 0 0 1.5px var(--selection), var(--shadow-md)' : 'var(--shadow-md)',
      transform: `rotate(${n.tilt || 0}deg)`,
      outline: linkable ? '2px dashed var(--selection-muted)' : 'none',
      outlineOffset: 3
    }
  }, /*#__PURE__*/React.createElement("div", {
    contentEditable: true,
    suppressContentEditableWarning: true,
    onPointerDown: e => e.stopPropagation(),
    onBlur: e => patchNote(n.id, {
      text: e.currentTarget.textContent
    }),
    style: {
      outline: 'none',
      minHeight: 96,
      cursor: 'text',
      whiteSpace: 'pre-wrap'
    }
  }, n.text)))), /*#__PURE__*/React.createElement(BsZoom, {
    view: viewState,
    onViewChange: setViewState,
    onFit: () => setViewState({
      zoom: 1,
      x: 120,
      y: 60
    })
  }), /*#__PURE__*/React.createElement(FloatingToolbar, null, [['select', 'Select — V'], ['pen', 'Draw — drag on canvas'], ['text', 'Sticky — click canvas'], ['rectangle', 'Shape — click canvas'], ['line', 'Connector — click A, then B']].map(([n, l]) => /*#__PURE__*/React.createElement(BsIconBtn, {
    key: n,
    name: n,
    label: l,
    size: "lg",
    active: tool === n,
    onClick: () => {
      setTool(n);
      setPendingLink(null);
    }
  })), /*#__PURE__*/React.createElement(BsSep, {
    orientation: "vertical"
  }), /*#__PURE__*/React.createElement(BsIconBtn, {
    name: "delete",
    label: "Delete \u2014 \u232B",
    size: "lg",
    disabled: !sel,
    onClick: () => {
      setNotes(ns => ns.filter(n => n.id !== sel));
      setShapes(ss => ss.filter(s => s.id !== sel));
      setStrokes(st => st.filter(s => s.id !== sel));
      setConnectors(cs => cs.filter(c => c.id !== sel && c.from !== sel && c.to !== sel));
      setSel(null);
    }
  })));
}

/* ══ Slides — inline editing ON the slide + inspector ═══════════ */
function SlideBody({
  slide,
  scale,
  onPatch
}) {
  const s = n => n * scale;
  const editable = !!onPatch;
  const edit = patch => e => onPatch && onPatch(typeof patch === 'function' ? patch(e.currentTarget.textContent) : {
    [patch]: e.currentTarget.textContent
  });
  const eProps = commit => editable ? {
    contentEditable: true,
    suppressContentEditableWarning: true,
    onBlur: commit,
    onPointerDown: e => e.stopPropagation()
  } : {};
  const eStyle = editable ? {
    outline: 'none',
    cursor: 'text'
  } : null;
  if (slide.kind === 'title') {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: s(10),
        padding: s(40),
        textAlign: 'center'
      }
    }, /*#__PURE__*/React.createElement("div", _extends({
      style: {
        fontFamily: 'var(--font-serif)',
        fontSize: s(44),
        letterSpacing: '-0.02em',
        ...eStyle
      }
    }, eProps(edit('title'))), slide.title), /*#__PURE__*/React.createElement("div", _extends({
      style: {
        fontSize: s(15),
        color: 'var(--muted-foreground)',
        ...eStyle
      }
    }, eProps(edit('sub'))), slide.sub));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      gap: s(12),
      padding: s(36)
    }
  }, /*#__PURE__*/React.createElement("div", _extends({
    style: {
      fontSize: s(26),
      fontWeight: 600,
      letterSpacing: '-0.015em',
      ...eStyle
    }
  }, eProps(edit('title'))), slide.title), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: s(8),
      marginTop: s(6)
    }
  }, (slide.bullets || []).map((b, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      gap: s(8),
      alignItems: 'center',
      fontSize: s(14),
      color: 'var(--muted-foreground)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: s(4),
      height: s(4),
      borderRadius: 999,
      background: 'var(--brand)',
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("span", _extends({
    style: eStyle || undefined
  }, eProps(e => {
    const bullets = [...(slide.bullets || [])];
    bullets[i] = e.currentTarget.textContent;
    onPatch({
      bullets
    });
  })), b))), editable ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => onPatch({
      bullets: [...(slide.bullets || []), 'New point']
    }),
    style: {
      alignSelf: 'flex-start',
      border: 'none',
      background: 'none',
      cursor: 'pointer',
      color: 'var(--muted-foreground)',
      font: `500 ${s(12)}px var(--font-sans)`,
      padding: 0,
      opacity: 0.7
    }
  }, "+ Add point") : null));
}
let slSeq = 1;
function SlidesScreen() {
  const [slides, setSlides] = React.useState(window.EDITOR_DATA.slides);
  const [cur, setCur] = React.useState('s1');
  const slide = slides.find(s => s.id === cur) || slides[0];
  const patch = p => setSlides(ss => ss.map(s => s.id === slide.id ? {
    ...s,
    ...p
  } : s));
  const addSlide = () => {
    const id = `s${Date.now().toString(36)}${slSeq++}`;
    setSlides(ss => [...ss, {
      id,
      name: `Slide ${ss.length + 1}`,
      kind: 'body',
      title: 'New slide',
      bullets: ['First point']
    }]);
    setCur(id);
  };
  const removeSlide = () => {
    if (slides.length <= 1) return;
    const idx = slides.findIndex(s => s.id === slide.id);
    setSlides(ss => ss.filter(s => s.id !== slide.id));
    setCur((slides[idx - 1] || slides[idx + 1]).id);
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(BsPanel, {
    side: "left",
    width: 168,
    footer: /*#__PURE__*/React.createElement(BsButton, {
      size: "sm",
      variant: "outline",
      style: {
        width: '100%'
      },
      onClick: addSlide
    }, "Add slide")
  }, /*#__PURE__*/React.createElement(Filmstrip, {
    slides: slides,
    selectedId: cur,
    onSelect: setCur,
    renderThumb: s => /*#__PURE__*/React.createElement(SlideBody, {
      slide: s,
      scale: 0.16
    })
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      background: 'var(--canvas)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 32
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 'min(880px, 100%)',
      aspectRatio: '16 / 9',
      background: 'var(--frame)',
      border: '1px solid var(--frame-border)',
      borderRadius: 4,
      boxShadow: 'var(--shadow-sm)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(SlideBody, {
    slide: slide,
    scale: 1,
    onPatch: patch,
    key: slide.id
  }))), /*#__PURE__*/React.createElement(BsPanel, {
    side: "right",
    width: 248,
    header: /*#__PURE__*/React.createElement(BsLabel, {
      eyebrow: true
    }, "Slide")
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '8px 12px',
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    },
    key: slide.id
  }, /*#__PURE__*/React.createElement(BsTextField, {
    label: "Name",
    mono: true,
    value: slide.name,
    onChange: e => patch({
      name: e.target.value
    })
  }), /*#__PURE__*/React.createElement(BsTextField, {
    label: "Title",
    value: slide.title,
    onChange: e => patch({
      title: e.target.value
    })
  }), slide.kind === 'title' ? /*#__PURE__*/React.createElement(BsTextField, {
    label: "Subtitle",
    value: slide.sub || '',
    onChange: e => patch({
      sub: e.target.value
    })
  }) : /*#__PURE__*/React.createElement("div", {
    className: "dsr-field",
    style: {
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label",
    style: {
      paddingTop: 6
    }
  }, "Bullets"), /*#__PURE__*/React.createElement("textarea", {
    value: (slide.bullets || []).join('\n'),
    onChange: e => patch({
      bullets: e.target.value.split('\n')
    }),
    rows: 4,
    style: {
      width: '100%',
      resize: 'vertical',
      border: '1px solid transparent',
      background: 'var(--muted)',
      borderRadius: 'var(--radius-xs)',
      font: '12px/1.5 var(--font-sans)',
      color: 'var(--foreground)',
      padding: '6px 8px',
      outline: 'none'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      color: 'var(--muted-foreground)',
      lineHeight: 1.5
    }
  }, "Tambi\xE9n puedes editar directamente sobre el slide: click en el t\xEDtulo o en un bullet."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6,
      paddingTop: 8
    }
  }, /*#__PURE__*/React.createElement(BsButton, {
    style: {
      flex: 1
    }
  }, "Present"), /*#__PURE__*/React.createElement(BsButton, {
    variant: "outline",
    size: "md",
    onClick: removeSlide,
    disabled: slides.length <= 1
  }, "Delete")))));
}
Object.assign(window, {
  BoardScreen,
  SlidesScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/screen-board-slides.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/screen-buzz-make.jsx
try { (() => {
const {
  Panel: BzPanel,
  Pane: BzPane,
  Label: BzLabel,
  CollectionTable: BzTable,
  Button: BzButton,
  Badge: BzBadge,
  Input: BzInput,
  IconButton: BzIconBtn,
  DeviceFrame: BzDevice,
  Tabs: BzTabs,
  Switch: BzSwitch,
  Kbd: BzKbd
} = window.DesignerDesignSystem_6adbd8;

/* ── Buzz (batch assets from a collection) ─────────────────────── */
function BuzzScreen({
  data
}) {
  const col = data.products;
  const [accent, setAccent] = React.useState('#d97757');
  const [records, setRecords] = React.useState(col.records);
  const patchRec = (id, p) => setRecords(rs => rs.map(r => r.id === id ? {
    ...r,
    ...p
  } : r));
  const addRecord = () => {
    const id = `p${Date.now().toString(36)}`;
    setRecords(rs => [...rs, {
      id,
      title: 'New product',
      price: 0,
      category: 'Tables',
      inStock: true
    }]);
  };
  const liveCol = {
    ...col,
    records
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(BzPanel, {
    side: "left",
    width: 224
  }, /*#__PURE__*/React.createElement(BzPane, {
    title: "Template"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 12,
      color: 'var(--muted-foreground)',
      lineHeight: 1.45
    }
  }, "Product card \xB7 4:5", /*#__PURE__*/React.createElement("br", null), "Un asset por registro de ", /*#__PURE__*/React.createElement("b", {
    style: {
      color: 'var(--foreground)'
    }
  }, col.name), ".")), /*#__PURE__*/React.createElement(BzPane, {
    title: "Brand kit"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4
    }
  }, ['#1a1a1a', '#d97757', '#4864c8', '#f4f2ee'].map(c => /*#__PURE__*/React.createElement("button", {
    key: c,
    type: "button",
    title: c,
    onClick: () => setAccent(c),
    style: {
      width: 22,
      height: 22,
      borderRadius: 5,
      border: 'none',
      cursor: 'pointer',
      background: c,
      boxShadow: accent === c ? '0 0 0 1.5px var(--background), 0 0 0 3px var(--selection)' : 'inset 0 0 0 1px oklch(0 0 0 / 0.12)'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      color: 'var(--muted-foreground)',
      paddingTop: 4
    }
  }, "Geist \xB7 Geist Mono"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      overflow: 'auto',
      background: 'var(--canvas)',
      padding: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fill, minmax(170px, 1fr))',
      gap: 16
    }
  }, records.map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    style: {
      background: 'var(--frame)',
      border: '1px solid var(--frame-border)',
      borderRadius: 8,
      overflow: 'hidden',
      boxShadow: 'var(--shadow-2xs)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      aspectRatio: '4 / 3',
      background: 'var(--muted)',
      position: 'relative'
    }
  }, r.image ? /*#__PURE__*/React.createElement("img", {
    src: r.image,
    alt: "",
    loading: "lazy",
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    },
    onError: e => {
      e.target.style.display = 'none';
    }
  }) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 8,
      top: 8,
      width: 22,
      height: 4,
      borderRadius: 2,
      background: accent
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 10,
      display: 'flex',
      flexDirection: 'column',
      gap: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    contentEditable: true,
    suppressContentEditableWarning: true,
    onBlur: e => patchRec(r.id, {
      title: e.currentTarget.textContent
    }),
    title: "Click para editar",
    style: {
      fontSize: 12,
      fontWeight: 500,
      outline: 'none',
      cursor: 'text',
      borderRadius: 3
    }
  }, r.title), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    contentEditable: true,
    suppressContentEditableWarning: true,
    onBlur: e => {
      const n = parseFloat(e.currentTarget.textContent.replace(/[^\d.]/g, ''));
      patchRec(r.id, {
        price: Number.isNaN(n) ? r.price : n
      });
    },
    title: "Click para editar",
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      color: 'var(--muted-foreground)',
      whiteSpace: 'nowrap',
      outline: 'none',
      cursor: 'text'
    }
  }, r.price, " \u20AC"), r.inStock === false ? /*#__PURE__*/React.createElement(BzBadge, {
    variant: "destructive"
  }, "out") : /*#__PURE__*/React.createElement(BzBadge, {
    variant: "outline"
  }, r.category))))))), /*#__PURE__*/React.createElement(BzPanel, {
    side: "right",
    width: 300,
    header: /*#__PURE__*/React.createElement(BzLabel, {
      eyebrow: true
    }, "Collection \xB7 ", records.length, " records"),
    footer: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 6
      }
    }, /*#__PURE__*/React.createElement(BzButton, {
      variant: "outline",
      style: {
        flex: 1
      },
      onClick: addRecord
    }, "Add record"), /*#__PURE__*/React.createElement(BzButton, {
      style: {
        flex: 1
      }
    }, "Export all \xB7 2x"))
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: 'auto'
    }
  }, /*#__PURE__*/React.createElement(BzTable, {
    collection: liveCol,
    onRecordClick: () => {}
  }))));
}

/* ── Make (prompt → preview) ───────────────────────────────────── */
function MakeScreen() {
  const [tab, setTab] = React.useState('preview');
  const [prompt, setPrompt] = React.useState('');
  const [thread, setThread] = React.useState([{
    who: 'you',
    text: 'Make a checkout screen for a furniture store. Keep it minimal.'
  }, {
    who: 'ai',
    text: 'Built the layout: summary card, payment fields, sticky CTA. Geist at 13px, warm neutrals.'
  }, {
    who: 'you',
    text: 'Add an order summary with 2 items and the total in mono.'
  }, {
    who: 'ai',
    text: 'Done — items bound to the Products collection, totals tabular.'
  }]);
  const send = () => {
    const t = prompt.trim();
    if (!t) return;
    setThread(th => [...th, {
      who: 'you',
      text: t
    }, {
      who: 'ai',
      text: 'Applied — preview updated. Anything else?'
    }]);
    setPrompt('');
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(BzPanel, {
    side: "left",
    width: 320,
    footer: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 6
      }
    }, /*#__PURE__*/React.createElement(BzInput, {
      placeholder: "Describe a change\u2026",
      "aria-label": "Prompt",
      value: prompt,
      onChange: e => setPrompt(e.target.value),
      onKeyDown: e => {
        if (e.key === 'Enter') send();
      }
    }), /*#__PURE__*/React.createElement(BzIconBtn, {
      name: "play",
      label: "Send",
      onClick: send
    }))
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 12,
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, thread.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      maxWidth: '88%',
      alignSelf: m.who === 'you' ? 'flex-end' : 'flex-start',
      background: m.who === 'you' ? 'var(--primary)' : 'var(--muted)',
      color: m.who === 'you' ? 'var(--primary-foreground)' : 'var(--foreground)',
      borderRadius: 10,
      padding: '8px 11px',
      fontSize: 12.5,
      lineHeight: 1.45
    }
  }, m.text)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      color: 'var(--muted-foreground)',
      display: 'flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-statusbar__dot"
  }), " Generated in 4.2s \xB7 ", /*#__PURE__*/React.createElement(BzKbd, {
    keys: ['mod', '⏎']
  }), " para regenerar"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      background: 'var(--canvas)',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 0,
      overflow: 'auto',
      padding: '20px 24px'
    }
  }, /*#__PURE__*/React.createElement(BzTabs, {
    items: [{
      value: 'preview',
      label: 'Preview'
    }, {
      value: 'code',
      label: 'Code'
    }],
    value: tab,
    onChange: setTab,
    style: {
      marginBottom: 16
    }
  }), tab === 'preview' ? /*#__PURE__*/React.createElement(BzDevice, {
    device: "phone",
    scale: 0.62,
    label: "Preview \xB7 390\xD7844"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 20,
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      height: '100%',
      boxSizing: 'border-box',
      fontFamily: 'var(--font-sans)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 22,
      fontWeight: 600,
      letterSpacing: '-0.015em'
    }
  }, "Checkout"), /*#__PURE__*/React.createElement("div", {
    style: {
      border: '1px solid var(--border)',
      borderRadius: 10,
      padding: 14,
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, [['Arc Lounge Chair', '640 €'], ['Fold Desk Lamp', '120 €']].map(([n, p]) => /*#__PURE__*/React.createElement("div", {
    key: n,
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      gap: 8,
      fontSize: 14
    }
  }, /*#__PURE__*/React.createElement("span", null, n), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      whiteSpace: 'nowrap'
    }
  }, p))), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: '1px solid var(--border)',
      paddingTop: 10,
      display: 'flex',
      justifyContent: 'space-between',
      gap: 8,
      fontSize: 14,
      fontWeight: 600
    }
  }, /*#__PURE__*/React.createElement("span", null, "Total"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      whiteSpace: 'nowrap'
    }
  }, "760 \u20AC"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 36,
      border: '1px solid var(--input)',
      borderRadius: 8,
      display: 'flex',
      alignItems: 'center',
      padding: '0 12px',
      color: 'var(--muted-foreground)',
      fontSize: 13
    }
  }, "Card number"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 36,
      border: '1px solid var(--input)',
      borderRadius: 8
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 36,
      border: '1px solid var(--input)',
      borderRadius: 8
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'auto',
      height: 44,
      borderRadius: 10,
      background: 'var(--primary)',
      color: 'var(--primary-foreground)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontSize: 15,
      fontWeight: 500
    }
  }, "Pay 760 \u20AC"))) : /*#__PURE__*/React.createElement("div", {
    style: {
      width: 'min(560px, 100%)',
      background: 'var(--frame)',
      border: '1px solid var(--border)',
      borderRadius: 10,
      padding: 16,
      fontFamily: 'var(--font-mono)',
      fontSize: 11.5,
      lineHeight: 1.7,
      color: 'var(--muted-foreground)',
      whiteSpace: 'pre'
    }
  }, `<Checkout>
  <OrderSummary items={products.slice(0, 2)} />   // bound · products
  <PaymentFields />
  <Button size="lg">Pay {total} €</Button>        // tabular, mono
</Checkout>`)));
}
Object.assign(window, {
  BuzzScreen,
  MakeScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/screen-buzz-make.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/screen-design.jsx
try { (() => {
const {
  Panel,
  Pane,
  Toolbar,
  Canvas,
  Frame,
  LayerTree,
  PageList,
  ZoomControls,
  StatusBar,
  Tabs,
  IconButton,
  Separator,
  Label,
  Input,
  Icon,
  PositionField,
  NumberField,
  TextField,
  ActionOpacity,
  ActionRotate,
  DataSourcePicker,
  BoundField,
  InteractionEditor,
  ConnectionLayer,
  CursorChip,
  CommentPin
} = window.DesignerDesignSystem_6adbd8;

// Defensive: tolerate a stale bundle that predates ActionAlign.
const ActionAlign = window.DesignerDesignSystem_6adbd8.ActionAlign || (() => null);

/* ── element model ─────────────────────────────────────────────── */
let dsSeq = 1;
const newId = p => `${p}${Date.now().toString(36).slice(-4)}${dsSeq++}`;
const CREATE = {
  frame: {
    w: 320,
    h: 240,
    name: 'Frame'
  },
  rectangle: {
    w: 180,
    h: 120,
    name: 'Rectangle',
    fill: '#d97757'
  },
  ellipse: {
    w: 120,
    h: 120,
    name: 'Ellipse',
    fill: '#4864c8'
  },
  text: {
    w: 200,
    h: 36,
    name: 'Text',
    text: 'Edit me'
  },
  image: {
    w: 220,
    h: 160,
    name: 'Image'
  }
};
const INITIAL_ELEMENTS = {
  cover: {
    type: 'frame',
    name: 'Cover',
    x: 0,
    y: 0,
    w: 420,
    h: 300
  },
  title: {
    type: 'text',
    name: 'Title',
    parent: 'cover',
    x: 24,
    y: 24,
    w: 300,
    h: 40,
    text: 'Arc Lounge Chair',
    binding: 'title',
    size: 24
  },
  hero: {
    type: 'rectangle',
    name: 'Hero block',
    parent: 'cover',
    x: 24,
    y: 84,
    w: 372,
    h: 190,
    fill: '#d97757',
    shadows: [{
      x: 0,
      y: 12,
      blur: 32,
      spread: -8,
      color: '#000000',
      alpha: 28
    }]
  },
  photo: {
    type: 'image',
    name: 'Photo',
    x: 480,
    y: 0,
    w: 300,
    h: 220
  },
  price: {
    type: 'text',
    name: 'Price',
    x: 480,
    y: 240,
    w: 120,
    h: 30,
    text: '640 €',
    mono: true,
    size: 16
  },
  card: {
    type: 'frame',
    name: 'Product card',
    x: 0,
    y: 360,
    w: 260,
    h: 150,
    layout: {
      direction: 'column',
      gap: 12,
      padding: 16,
      align: 'start'
    },
    shadows: [{
      x: 0,
      y: 4,
      blur: 16,
      spread: -6,
      color: '#000000',
      alpha: 16
    }],
    radius: 12
  },
  cardBadge: {
    type: 'component',
    name: 'Badge',
    parent: 'card',
    component: 'Badge',
    x: 16,
    y: 16,
    w: 76,
    h: 18,
    props: {
      children: 'In stock',
      variant: 'selection'
    }
  },
  cardBtn: {
    type: 'component',
    name: 'Buy button',
    parent: 'card',
    component: 'Button',
    x: 16,
    y: 46,
    w: 120,
    h: 28,
    props: {
      children: 'Buy · 640 €',
      variant: 'primary',
      size: 'md'
    }
  }
};
const ORDER_INIT = ['cover', 'title', 'hero', 'photo', 'price', 'card', 'cardBadge', 'cardBtn'];
const TYPE_ICON_FALLBACK = {
  component: 'component'
};
const DESIGN_TOOLS = [['select', 'Select — V'], ['frame', 'Frame — F'], ['rectangle', 'Rectangle — R'], ['ellipse', 'Ellipse — O'], ['text', 'Text — T'], ['image', 'Image — I'], ['hand', 'Hand — H']];

/* ── leaf content ──────────────────────────────────────────────── */
function ElementBody({
  el,
  editing,
  onCommitText
}) {
  if (el.type === 'text') {
    return /*#__PURE__*/React.createElement("div", {
      contentEditable: editing,
      suppressContentEditableWarning: true,
      onBlur: e => onCommitText(e.currentTarget.textContent),
      style: {
        width: '100%',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        fontFamily: el.mono ? 'var(--font-mono)' : 'var(--font-sans)',
        fontSize: el.size || 16,
        fontWeight: el.mono ? 400 : 600,
        letterSpacing: el.mono ? 0 : '-0.015em',
        color: 'var(--foreground)',
        outline: 'none',
        cursor: editing ? 'text' : 'inherit',
        padding: '0 2px',
        whiteSpace: 'pre-wrap'
      }
    }, el.text);
  }
  if (el.type === 'image') {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        width: '100%',
        height: '100%',
        background: 'var(--muted)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'var(--muted-foreground)'
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: "image",
      size: 22
    }));
  }
  if (el.type === 'component') return /*#__PURE__*/React.createElement(RenderComponent, {
    el: el
  });
  return null;
}

/* ── child element (inside a frame) ────────────────────────────── */
function ChildEl({
  el,
  id,
  flexLayout,
  dir,
  zoom,
  selected,
  editing,
  onSelect,
  onMove,
  onReorder,
  onEditText,
  onCommitText
}) {
  const drag = e => {
    if (e.button !== 0 || el.locked) return;
    e.stopPropagation();
    onSelect();
    if (editing) return;
    if (flexLayout) {
      // Reorder inside the auto-layout container: live index from sibling midpoints.
      const node = e.currentTarget;
      const container = node.parentElement;
      const horizontal = dir === 'row';
      const sx0 = e.clientX;
      const sy0 = e.clientY;
      let dragging = false;
      const move = ev => {
        if (!dragging && Math.hypot(ev.clientX - sx0, ev.clientY - sy0) < 4) return;
        if (!dragging) {
          dragging = true;
          node.style.opacity = '0.55';
        }
        const sibs = Array.prototype.filter.call(container.children, c => c.hasAttribute('data-kid') && c !== node);
        let idx = 0;
        sibs.forEach(c => {
          const r = c.getBoundingClientRect();
          const mid = horizontal ? r.left + r.width / 2 : r.top + r.height / 2;
          if ((horizontal ? ev.clientX : ev.clientY) > mid) idx += 1;
        });
        if (onReorder) onReorder(idx);
      };
      const up = () => {
        node.style.opacity = '';
        window.removeEventListener('pointermove', move);
        window.removeEventListener('pointerup', up);
      };
      window.addEventListener('pointermove', move);
      window.addEventListener('pointerup', up);
      return;
    }
    const sx = e.clientX;
    const sy = e.clientY;
    const move = ev => onMove({
      x: Math.round(el.x + (ev.clientX - sx) / zoom),
      y: Math.round(el.y + (ev.clientY - sy) / zoom)
    });
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    "data-canvas-item": true,
    "data-kid": id,
    onPointerDown: drag,
    onDoubleClick: e => {
      if (el.type === 'text') {
        e.stopPropagation();
        onEditText();
      }
    },
    title: flexLayout ? 'Drag to reorder' : undefined,
    style: (() => {
      const vs = elVisualStyle(el);
      const ring = '0 0 0 1.5px var(--selection)';
      return {
        position: flexLayout ? 'relative' : 'absolute',
        left: flexLayout ? undefined : el.x,
        top: flexLayout ? undefined : el.y,
        width: el.w,
        height: el.h,
        flex: 'none',
        opacity: (el.opacity != null ? el.opacity : 100) / 100,
        transform: el.rotation ? `rotate(${el.rotation}deg)` : undefined,
        ...vs,
        boxShadow: selected ? vs.boxShadow ? `${ring}, ${vs.boxShadow}` : ring : vs.boxShadow,
        cursor: flexLayout ? 'grab' : 'default'
      };
    })()
  }, selected ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      zIndex: 2,
      pointerEvents: 'none',
      background: 'var(--selection)',
      color: 'var(--selection-foreground)',
      fontFamily: 'var(--font-mono)',
      fontSize: 9,
      lineHeight: 1,
      padding: '2px 5px',
      borderRadius: '0 0 4px 0',
      whiteSpace: 'nowrap',
      maxWidth: '100%',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, el.name) : null, /*#__PURE__*/React.createElement(ElementBody, {
    el: el,
    editing: editing,
    onCommitText: onCommitText
  }));
}

/* ── screen ────────────────────────────────────────────────────── */
function DesignScreen({
  data
}) {
  const [tool, setTool] = React.useState('select');
  const [els, setEls] = React.useState(INITIAL_ELEMENTS);
  const [order, setOrder] = React.useState(ORDER_INIT);
  const [sel, setSel] = React.useState('card');
  const [editingId, setEditingId] = React.useState(null);
  const [pageId, setPageId] = React.useState('p1');
  const [tab, setTab] = React.useState('design');
  const [viewState, setViewState] = React.useState({
    zoom: 0.8,
    x: 90,
    y: 40
  });
  const [bind, setBind] = React.useState({
    collection: 'products',
    field: 'title'
  });
  const [ix, setIx] = React.useState({
    trigger: 'click',
    action: 'navigate',
    target: 'Photo',
    transition: 'smart',
    easing: 'ease-out',
    duration: 240
  });
  const [pinOpen, setPinOpen] = React.useState(false);
  const wrapRef = React.useRef(null);
  const el = sel ? els[sel] : null;
  const patch = (id, p) => setEls(e => ({
    ...e,
    [id]: {
      ...e[id],
      ...p
    }
  }));
  const kidsOf = id => order.filter(k => els[k] && els[k].parent === id);

  // Reorder a child within its auto-layout parent (same order slots, new sequence).
  const reorderChild = (parentId, kidId, newIdx) => {
    setOrder(o => {
      const sibs = o.filter(x => els[x] && els[x].parent === parentId);
      const cur = sibs.indexOf(kidId);
      if (cur < 0) return o;
      const next = [...sibs];
      next.splice(cur, 1);
      const clamped = Math.max(0, Math.min(newIdx, next.length));
      if (sibs[clamped] === kidId && cur === clamped) return o;
      next.splice(clamped, 0, kidId);
      if (next.every((x, i) => x === sibs[i])) return o;
      const slots = [];
      o.forEach((x, i) => {
        if (els[x] && els[x].parent === parentId) slots.push(i);
      });
      const n = [...o];
      slots.forEach((slot, i) => {
        n[slot] = next[i];
      });
      return n;
    });
  };
  const frameAt = (wx, wy) => {
    for (let i = order.length - 1; i >= 0; i -= 1) {
      const id = order[i];
      const f = els[id];
      if (f && f.type === 'frame' && !f.hidden && !f.locked && wx >= f.x && wx <= f.x + f.w && wy >= f.y && wy <= f.y + f.h) return id;
    }
    return null;
  };
  const addElement = (type, at, opts = {}) => {
    const spec = opts.spec || CREATE[type];
    if (!spec) return null;
    const id = newId(type[0]);
    const parent = type === 'frame' ? null : opts.parent !== undefined ? opts.parent : frameAt(at.x, at.y);
    const base = {
      type,
      name: opts.name || spec.name,
      w: spec.w,
      h: spec.h,
      x: Math.round((parent ? at.x - els[parent].x : at.x) - spec.w / 2),
      y: Math.round((parent ? at.y - els[parent].y : at.y) - spec.h / 2)
    };
    if (parent) base.parent = parent;
    if (spec.fill) base.fill = spec.fill;
    if (spec.text) base.text = spec.text;
    if (opts.extra) Object.assign(base, opts.extra);
    setEls(e => ({
      ...e,
      [id]: base
    }));
    setOrder(o => [...o, id]);
    setSel(id);
    setTool('select');
    if (type === 'text') setEditingId(id);
    return id;
  };

  // Deploy a DS component to the canvas (into the selected frame, or view center).
  const componentOptsFor = name => {
    const def = COMPONENT_DEFS[name];
    if (!def) return null;
    return {
      spec: {
        w: def.w,
        h: def.h
      },
      name: def.defaults.children || name,
      extra: {
        component: name,
        props: {
          ...def.defaults
        }
      }
    };
  };
  const insertComponent = name => {
    const o = componentOptsFor(name);
    if (!o) return;
    if (el && el.type === 'frame') {
      addElement('component', {
        x: el.x + el.w / 2,
        y: el.y + el.h / 2
      }, {
        ...o,
        parent: sel
      });
    } else {
      const r = wrapRef.current ? wrapRef.current.getBoundingClientRect() : {
        width: 900,
        height: 500
      };
      addElement('component', {
        x: (r.width / 2 - viewState.x) / viewState.zoom,
        y: (r.height / 2 - viewState.y) / viewState.zoom
      }, o);
    }
  };
  // Drag & drop from the Insert panel onto the canvas.
  const onDragOver = e => {
    e.preventDefault();
    e.dataTransfer.dropEffect = 'copy';
  };
  const onDrop = e => {
    e.preventDefault();
    const name = e.dataTransfer.getData('text/plain');
    const o = componentOptsFor(name);
    if (!o || !wrapRef.current) return;
    const rect = wrapRef.current.getBoundingClientRect();
    addElement('component', {
      x: (e.clientX - rect.left - viewState.x) / viewState.zoom,
      y: (e.clientY - rect.top - viewState.y) / viewState.zoom
    }, o); // parent auto-detected via frameAt hit-test
  };
  const removeElement = id => {
    if (!id) return;
    const doomed = new Set([id, ...kidsOf(id)]);
    setEls(e => {
      const n = {
        ...e
      };
      doomed.forEach(d => delete n[d]);
      return n;
    });
    setOrder(o => o.filter(x => !doomed.has(x)));
    setSel(null);
  };
  const duplicateElement = id => {
    const src = els[id];
    if (!src) return;
    const nid = newId(src.type[0]);
    const adds = {
      [nid]: {
        ...src,
        name: `${src.name} copy`,
        x: src.x + 24,
        y: src.y + 24
      }
    };
    const addOrder = [nid];
    kidsOf(id).forEach(k => {
      const kid = newId(els[k].type[0]);
      adds[kid] = {
        ...els[k],
        parent: nid
      };
      addOrder.push(kid);
    });
    setEls(e => ({
      ...e,
      ...adds
    }));
    setOrder(o => [...o, ...addOrder]);
    setSel(nid);
  };
  const onCanvasDown = e => {
    setEditingId(null);
    if (!CREATE[tool]) {
      setSel(null);
      return;
    }
    const rect = e.currentTarget.getBoundingClientRect();
    addElement(tool, {
      x: (e.clientX - rect.left - viewState.x) / viewState.zoom,
      y: (e.clientY - rect.top - viewState.y) / viewState.zoom
    });
  };
  React.useEffect(() => {
    const typing = t => t.closest && t.closest('input, textarea, select, [contenteditable="true"]');
    const h = e => {
      if (typing(e.target)) return;
      const k = e.key.toLowerCase();
      if ((e.metaKey || e.ctrlKey) && k === 'd') {
        e.preventDefault();
        if (sel) duplicateElement(sel);
        return;
      }
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      if (k === 'backspace' || k === 'delete') {
        if (sel) {
          e.preventDefault();
          removeElement(sel);
        }
        return;
      }
      const map = {
        v: 'select',
        f: 'frame',
        r: 'rectangle',
        o: 'ellipse',
        t: 'text',
        i: 'image',
        h: 'hand'
      };
      if (map[k]) setTool(map[k]);
    };
    window.addEventListener('keydown', h);
    return () => window.removeEventListener('keydown', h);
  }, [sel, els, order]);
  React.useEffect(() => {
    window.EDITOR_ON_TOOL = setTool;
    return () => {
      window.EDITOR_ON_TOOL = null;
    };
  }, []);

  /* layer tree (nested) */
  const toNode = id => ({
    id,
    name: els[id].name,
    type: els[id].type === 'component' ? 'component' : els[id].type,
    hidden: els[id].hidden,
    locked: els[id].locked,
    binding: els[id].binding,
    children: kidsOf(id).map(toNode)
  });
  const layers = [...order].filter(id => !els[id].parent).reverse().map(toNode);
  const targets = order.filter(id => els[id].type === 'frame' && id !== sel).map(id => els[id].name);
  const ports = els.cover && els.photo ? [{
    id: 'c1',
    from: {
      x: els.cover.x + els.cover.w,
      y: els.cover.y + 80
    },
    to: {
      x: els.photo.x,
      y: els.photo.y + 60
    }
  }] : [];
  const parentLayoutActive = el && el.parent && els[el.parent] && els[el.parent].layout && els[el.parent].layout.direction !== 'none';
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Panel, {
    side: "left",
    width: 232,
    header: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Input, {
      size: "sm",
      quiet: true,
      placeholder: "Search layers\u2026",
      "aria-label": "Search layers"
    }), /*#__PURE__*/React.createElement(IconButton, {
      name: "add",
      label: "Add frame \u2014 F",
      size: "sm",
      onClick: () => addElement('frame', {
        x: 640,
        y: 480
      })
    }))
  }, /*#__PURE__*/React.createElement(Label, {
    eyebrow: true,
    style: {
      padding: '8px 12px 2px',
      display: 'block'
    }
  }, "Pages"), /*#__PURE__*/React.createElement(PageList, {
    pages: window.EDITOR_DATA.pages,
    selectedId: pageId,
    onSelect: setPageId
  }), /*#__PURE__*/React.createElement(Separator, null), /*#__PURE__*/React.createElement(Label, {
    eyebrow: true,
    style: {
      padding: '8px 12px 2px',
      display: 'block'
    }
  }, "Layers"), /*#__PURE__*/React.createElement(LayerTree, {
    layers: layers,
    selectedId: sel,
    onSelect: setSel,
    onToggleHidden: id => patch(id, {
      hidden: !els[id].hidden
    }),
    onToggleLocked: id => patch(id, {
      locked: !els[id].locked
    })
  }), /*#__PURE__*/React.createElement(Separator, {
    style: {
      marginTop: 8
    }
  }), /*#__PURE__*/React.createElement(InsertSection, {
    onInsert: insertComponent
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(Toolbar, null, DESIGN_TOOLS.map(([n, l]) => /*#__PURE__*/React.createElement(IconButton, {
    key: n,
    name: n,
    label: l,
    active: tool === n,
    onClick: () => setTool(n)
  })), /*#__PURE__*/React.createElement(Separator, {
    orientation: "vertical"
  }), /*#__PURE__*/React.createElement(IconButton, {
    name: "duplicate",
    label: "Duplicate \u2014 \u2318D",
    size: "sm",
    disabled: !sel,
    onClick: () => duplicateElement(sel)
  }), /*#__PURE__*/React.createElement(IconButton, {
    name: "delete",
    label: "Delete \u2014 \u232B",
    size: "sm",
    disabled: !sel,
    onClick: () => removeElement(sel)
  })), /*#__PURE__*/React.createElement("div", {
    ref: wrapRef,
    style: {
      flex: 1,
      position: 'relative',
      minHeight: 0
    },
    onDragOver: onDragOver,
    onDrop: onDrop
  }, /*#__PURE__*/React.createElement(Canvas, {
    view: viewState,
    onViewChange: setViewState,
    panOnDrag: tool === 'select' || tool === 'hand',
    onBackgroundPointerDown: onCanvasDown,
    style: {
      cursor: CREATE[tool] ? 'crosshair' : undefined
    }
  }, order.filter(id => !els[id].parent && !els[id].hidden).map((id, idx) => {
    const f = els[id];
    const locked = f.locked;
    const lay = f.type === 'frame' && f.layout && f.layout.direction !== 'none' ? f.layout : null;
    const kids = f.type === 'frame' ? kidsOf(id).filter(k => !els[k].hidden) : [];
    return /*#__PURE__*/React.createElement(Frame, {
      key: id,
      x: f.x,
      y: f.y,
      width: f.w,
      height: f.h,
      name: f.type === 'frame' || sel === id ? f.name : undefined,
      zoom: viewState.zoom,
      selected: sel === id,
      fill: f.type === 'frame' ? undefined : 'transparent',
      style: {
        zIndex: idx + 1,
        /* stack by document order */
        outline: f.type !== 'frame' && sel !== id ? 'none' : undefined,
        opacity: (f.opacity != null ? f.opacity : 100) / 100,
        transform: f.rotation ? `rotate(${f.rotation}deg)` : undefined,
        ...elVisualStyle(f)
      },
      onSelect: () => {
        if (!locked) {
          setSel(id);
          if (editingId !== id) setEditingId(null);
        }
      },
      onMove: locked || editingId === id ? undefined : ({
        x,
        y
      }) => patch(id, {
        x,
        y
      }),
      onResize: locked ? undefined : b => patch(id, {
        x: b.x,
        y: b.y,
        w: b.width,
        h: b.height
      }),
      onDoubleClick: () => {
        if (f.type === 'text') setEditingId(id);
      }
    }, f.type === 'frame' ? /*#__PURE__*/React.createElement("div", {
      style: lay ? {
        width: '100%',
        height: '100%',
        display: 'flex',
        flexDirection: lay.direction,
        gap: lay.gap,
        padding: lay.padding,
        alignItems: lay.align === 'space-between' ? 'start' : lay.align,
        justifyContent: lay.align === 'space-between' ? 'space-between' : 'flex-start',
        boxSizing: 'border-box'
      } : {
        width: '100%',
        height: '100%',
        position: 'relative'
      }
    }, kids.map(kid => /*#__PURE__*/React.createElement(ChildEl, {
      key: kid,
      id: kid,
      el: els[kid],
      flexLayout: !!lay,
      dir: lay ? lay.direction : 'column',
      zoom: viewState.zoom,
      selected: sel === kid,
      editing: editingId === kid,
      onReorder: idx => reorderChild(id, kid, idx),
      onSelect: () => setSel(kid),
      onMove: ({
        x,
        y
      }) => patch(kid, {
        x,
        y
      }),
      onEditText: () => setEditingId(kid),
      onCommitText: t => {
        patch(kid, {
          text: t
        });
        setEditingId(null);
      }
    }))) : /*#__PURE__*/React.createElement(ElementBody, {
      el: f,
      editing: editingId === id,
      onCommitText: t => {
        patch(id, {
          text: t
        });
        setEditingId(null);
      }
    }));
  }), tab === 'prototype' ? /*#__PURE__*/React.createElement(ConnectionLayer, {
    connections: ports
  }) : null, /*#__PURE__*/React.createElement(CursorChip, {
    name: "Diego Ferrer",
    x: 560,
    y: 420
  }), /*#__PURE__*/React.createElement(CommentPin, {
    name: "Ana Kova\u010D",
    time: "2h",
    x: 430,
    y: 80,
    open: pinOpen,
    onClick: () => setPinOpen(!pinOpen),
    text: "Can we bind this fill to category color?"
  })), /*#__PURE__*/React.createElement(ZoomControls, {
    view: viewState,
    onViewChange: setViewState,
    onFit: () => setViewState({
      zoom: 0.8,
      x: 90,
      y: 40
    })
  })), /*#__PURE__*/React.createElement(StatusBar, {
    savedText: "Saved \xB7 2m ago",
    selection: el ? `${el.name} · ${el.type === 'component' ? el.component : el.type}` : `${order.length} layers`,
    right: el ? `x ${el.x}  y ${el.y}  ·  ${Math.round(viewState.zoom * 100)}%` : `${Math.round(viewState.zoom * 100)}%`
  })), /*#__PURE__*/React.createElement(Panel, {
    side: "right",
    width: 264,
    header: /*#__PURE__*/React.createElement(Tabs, {
      full: true,
      value: tab,
      onChange: setTab,
      items: [{
        value: 'design',
        label: 'Design'
      }, {
        value: 'prototype',
        label: 'Prototype'
      }, {
        value: 'data',
        label: 'Data'
      }]
    })
  }, tab === 'design' && el ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '8px 12px 4px'
    }
  }, /*#__PURE__*/React.createElement(ActionAlign, {
    onAlign: () => {}
  })), /*#__PURE__*/React.createElement(Pane, {
    title: "Layer"
  }, /*#__PURE__*/React.createElement(TextField, {
    label: "Name",
    mono: true,
    value: el.name,
    onChange: e => patch(sel, {
      name: e.target.value
    })
  }), parentLayoutActive ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      color: 'var(--muted-foreground)'
    }
  }, "En auto-layout \u2014 la posici\xF3n la controla el frame padre.") : null), el.type === 'component' ? /*#__PURE__*/React.createElement(ComponentPropsPane, {
    el: el,
    onPatch: p => patch(sel, p)
  }) : null, /*#__PURE__*/React.createElement(Pane, {
    title: "Layout"
  }, !parentLayoutActive ? /*#__PURE__*/React.createElement(PositionField, {
    values: [el.x, el.y],
    onChange: ([x, y]) => patch(sel, {
      x,
      y
    })
  }) : null, /*#__PURE__*/React.createElement(PositionField, {
    labels: ['W', 'H'],
    values: [el.w, el.h],
    onChange: ([w, h]) => patch(sel, {
      w,
      h
    })
  }), el.type === 'rectangle' || el.type === 'frame' ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Radius"), /*#__PURE__*/React.createElement(NumberField, {
    icon: "radius",
    unit: "px",
    value: el.radius != null ? el.radius : el.type === 'frame' ? 0 : 10,
    min: 0,
    onChange: radius => patch(sel, {
      radius
    }),
    title: "Corner radius"
  })) : null, el.type === 'text' ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Size"), /*#__PURE__*/React.createElement(NumberField, {
    label: "A",
    unit: "px",
    value: el.size || 16,
    min: 8,
    max: 96,
    onChange: size => patch(sel, {
      size
    }),
    title: "Font size"
  })) : null), el.type === 'frame' ? /*#__PURE__*/React.createElement(AutoLayoutPane, {
    el: el,
    onPatch: p => patch(sel, p)
  }) : null, /*#__PURE__*/React.createElement(Pane, {
    title: "Appearance"
  }, /*#__PURE__*/React.createElement(ActionOpacity, {
    value: el.opacity != null ? el.opacity : 100,
    onChange: opacity => patch(sel, {
      opacity
    })
  }), /*#__PURE__*/React.createElement(ActionRotate, {
    value: el.rotation || 0,
    onChange: rotation => patch(sel, {
      rotation
    }),
    onFlipH: () => patch(sel, {
      rotation: -(el.rotation || 0)
    }),
    onFlipV: () => patch(sel, {
      rotation: 180 - (el.rotation || 0)
    })
  })), el.type === 'rectangle' || el.type === 'ellipse' || el.type === 'frame' ? /*#__PURE__*/React.createElement(FillPane, {
    el: el,
    onPatch: p => patch(sel, p),
    gradients: data.gradients || []
  }) : null, el.type === 'rectangle' || el.type === 'ellipse' || el.type === 'frame' ? /*#__PURE__*/React.createElement(StrokePane, {
    el: el,
    onPatch: p => patch(sel, p)
  }) : null, /*#__PURE__*/React.createElement(EffectsPane, {
    el: el,
    onPatch: p => patch(sel, p)
  })) : null, tab === 'design' && !el ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16,
      fontSize: 13,
      color: 'var(--muted-foreground)',
      lineHeight: 1.5
    }
  }, "No selection.", /*#__PURE__*/React.createElement("br", null), "Herramientas F, R, O, T, I + click en el canvas para crear; Insert (panel izquierdo) despliega componentes reales.") : null, tab === 'prototype' ? /*#__PURE__*/React.createElement(Pane, {
    title: "Interaction"
  }, /*#__PURE__*/React.createElement(InteractionEditor, {
    vocab: data.interactions,
    value: ix,
    onChange: setIx,
    targets: targets.length ? targets : ['—']
  })) : null, tab === 'data' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Pane, {
    title: "Binding"
  }, /*#__PURE__*/React.createElement(DataSourcePicker, {
    collections: [data.products],
    value: bind,
    onChange: setBind
  }), bind.collection && bind.field ? /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 4
    }
  }, /*#__PURE__*/React.createElement(BoundField, {
    collection: bind.collection,
    field: bind.field,
    onUnbind: () => setBind({})
  })) : null), /*#__PURE__*/React.createElement(Pane, {
    title: "Repeat"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 12,
      color: 'var(--muted-foreground)',
      lineHeight: 1.4
    }
  }, "Repeat this frame over ", data.products.records.length, " records \u2014 see the Buzz mode for the result."))) : null));
}
Object.assign(window, {
  DesignScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/screen-design.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/screen-dev.jsx
try { (() => {
const {
  Panel: DvPanel,
  Pane: DvPane,
  LayerTree: DvTree,
  Canvas: DvCanvas,
  Frame: DvFrame,
  Label: DvLabel,
  Badge: DvBadge,
  Button: DvButton,
  IconButton: DvIconBtn,
  Tabs: DvTabs,
  ZoomControls: DvZoom,
  TokenRow: DvTokenRow
} = window.DesignerDesignSystem_6adbd8;
const DV_CSS_FOR = (id, f) => `.${id} {
  position: absolute;
  left: ${f.x}px;   /* x */
  top: ${f.y}px;    /* y */
  width: ${f.w}px;
  height: ${f.h}px;
  background: var(--frame);
  border-radius: var(--radius-md);
}
.${id}__title {
  font: 600 24px/1.2 var(--font-sans);
  letter-spacing: var(--tracking-tight);
}`;
const DV_TOKENS = [{
  name: 'background',
  $type: 'color',
  $value: 'oklch(0.993 0.0016 84)',
  cssVar: '--background'
}, {
  name: 'space-6',
  $type: 'dimension',
  $value: '1.5rem',
  cssVar: '--space-6'
}, {
  name: 'radius-md',
  $type: 'dimension',
  $value: '8px',
  cssVar: '--radius-md'
}, {
  name: 'tracking-tight',
  $type: 'dimension',
  $value: '-0.015em',
  cssVar: '--tracking-tight'
}];

/* Measurement overlay line + chip, in world coords */
function DvMeasure({
  x,
  y,
  w,
  h,
  label
}) {
  const horizontal = h === 0;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: x,
      top: y,
      width: Math.max(w, 1),
      height: Math.max(h, 1),
      background: 'var(--destructive)',
      opacity: 0.9
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: horizontal ? '50%' : 6,
      top: horizontal ? 6 : '50%',
      transform: horizontal ? 'translateX(-50%)' : 'translateY(-50%)',
      background: 'var(--destructive)',
      color: 'oklch(0.99 0 0)',
      fontFamily: 'var(--font-mono)',
      fontSize: 9.5,
      lineHeight: 1,
      padding: '2px 4px',
      borderRadius: 3,
      whiteSpace: 'nowrap'
    }
  }, label));
}
function DevScreen({
  data
}) {
  const [sel, setSel] = React.useState('cover');
  const [tab, setTab] = React.useState('code');
  const [viewState, setViewState] = React.useState({
    zoom: 0.9,
    x: 110,
    y: 70
  });
  const frames = window.EDITOR_DATA.frames;
  const selFrame = frames[sel] ? sel : 'cover';
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(DvPanel, {
    side: "left",
    width: 224,
    header: /*#__PURE__*/React.createElement(DvLabel, {
      eyebrow: true
    }, "Layers \xB7 read-only")
  }, /*#__PURE__*/React.createElement(DvTree, {
    layers: window.EDITOR_DATA.layers,
    selectedId: sel,
    onSelect: setSel
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement(DvCanvas, {
    view: viewState,
    onViewChange: setViewState
  }, Object.entries(frames).map(([id, f]) => /*#__PURE__*/React.createElement(DvFrame, {
    key: id,
    x: f.x,
    y: f.y,
    width: f.w,
    height: f.h,
    name: f.name,
    zoom: viewState.zoom,
    selected: sel === id,
    onSelect: () => setSel(id)
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 18,
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 12,
      width: '58%',
      background: 'var(--muted)',
      borderRadius: 3
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      background: 'var(--canvas)',
      border: '1px solid var(--border)',
      borderRadius: 8
    }
  })))), sel === 'cover' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(DvMeasure, {
    x: frames.cover.x + frames.cover.w,
    y: frames.cover.y + 60,
    w: frames.detail.x - frames.cover.x - frames.cover.w,
    h: 0,
    label: "60"
  }), /*#__PURE__*/React.createElement(DvMeasure, {
    x: frames.cover.x + 18,
    y: frames.cover.y,
    w: 0,
    h: 18,
    label: "18"
  })) : null), /*#__PURE__*/React.createElement(DvZoom, {
    view: viewState,
    onViewChange: setViewState,
    onFit: () => setViewState({
      zoom: 0.9,
      x: 110,
      y: 70
    })
  }))), /*#__PURE__*/React.createElement(DvPanel, {
    side: "right",
    width: 300,
    header: /*#__PURE__*/React.createElement(DvTabs, {
      full: true,
      value: tab,
      onChange: setTab,
      items: [{
        value: 'code',
        label: 'Code'
      }, {
        value: 'tokens',
        label: 'Tokens'
      }, {
        value: 'assets',
        label: 'Assets'
      }]
    })
  }, tab === 'code' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '8px 12px'
    }
  }, /*#__PURE__*/React.createElement(DvBadge, {
    variant: "selection"
  }, selFrame, " \xB7 frame"), /*#__PURE__*/React.createElement(DvIconBtn, {
    name: "duplicate",
    label: "Copy CSS",
    size: "sm",
    onClick: () => {
      try {
        navigator.clipboard.writeText(DV_CSS_FOR(selFrame, frames[selFrame]));
      } catch (e) {}
    }
  })), /*#__PURE__*/React.createElement("pre", {
    style: {
      margin: 0,
      padding: '4px 12px 14px',
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      lineHeight: 1.65,
      color: 'var(--foreground)',
      whiteSpace: 'pre-wrap'
    }
  }, DV_CSS_FOR(selFrame, frames[selFrame]))) : null, tab === 'tokens' ? /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 4
    }
  }, DV_TOKENS.map(t => /*#__PURE__*/React.createElement(DvTokenRow, {
    key: t.name,
    token: t,
    showType: false
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '10px 12px',
      fontSize: 11,
      color: 'var(--muted-foreground)'
    }
  }, "Tokens usados por la selecci\xF3n.")) : null, tab === 'assets' ? /*#__PURE__*/React.createElement(DvPane, {
    title: "Exports"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, [['cover.png', '2x · 840×600'], ['cover.svg', 'vector']].map(([n, m]) => /*#__PURE__*/React.createElement("div", {
    key: n,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 8,
      border: '1px solid var(--border)',
      borderRadius: 6,
      padding: '7px 10px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11
    }
  }, n), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 10,
      color: 'var(--muted-foreground)'
    }
  }, m))), /*#__PURE__*/React.createElement(DvButton, {
    size: "sm",
    variant: "outline"
  }, "Download all"))) : null));
}
Object.assign(window, {
  DevScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/screen-dev.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/screen-sites.jsx
try { (() => {
const {
  Panel: StPanel,
  Pane: StPane,
  Label: StLabel,
  AssetGrid: StGrid,
  LibraryItem: StItem,
  Icon: StIcon,
  DataSourcePicker: StPicker,
  BoundField: StBound,
  SelectField: StSelect,
  NumberField: StNum,
  PageList: StPages,
  Separator: StSep,
  DEVICE_SIZES: ST_DEVICES,
  IconButton: StIconBtnX
} = window.DesignerDesignSystem_6adbd8;
const BP_WIDTH = {
  desktop: 1180,
  laptop: 960,
  tablet: 620,
  phone: 340
};
function BlockSkeleton({
  block,
  bp,
  heroHeading,
  onHeroEdit
}) {
  const cols = bp === 'phone' ? 1 : 3;
  const pad = bp === 'phone' ? 16 : 32;
  const inner = {
    navbar: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        height: '100%',
        padding: `0 ${pad}px`
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: 74,
        height: 12,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 8
      }
    }, [0, 1, 2].map(i => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        width: 40,
        height: 8,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        width: 56,
        height: 20,
        borderRadius: 5,
        background: 'var(--primary)'
      }
    }))),
    hero: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 12,
        height: '100%',
        padding: pad
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: '18%',
        height: 8,
        borderRadius: 3,
        background: 'var(--brand-muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      contentEditable: !!onHeroEdit,
      suppressContentEditableWarning: true,
      onClick: e => e.stopPropagation(),
      onBlur: e => onHeroEdit && onHeroEdit(e.currentTarget.textContent),
      style: {
        fontSize: bp === 'phone' ? 22 : 32,
        fontWeight: 600,
        letterSpacing: '-0.02em',
        textAlign: 'center',
        maxWidth: '80%',
        outline: 'none',
        cursor: onHeroEdit ? 'text' : 'default',
        lineHeight: 1.15
      }
    }, heroHeading || 'Design tools, assembled'), /*#__PURE__*/React.createElement("div", {
      style: {
        width: '38%',
        height: 10,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        width: 110,
        height: 26,
        borderRadius: 6,
        background: 'var(--primary)',
        marginTop: 6
      }
    })),
    features: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: `repeat(${cols}, 1fr)`,
        gap: 14,
        padding: pad,
        height: '100%',
        boxSizing: 'border-box'
      }
    }, Array.from({
      length: cols
    }).map((_, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        border: '1px solid var(--border)',
        borderRadius: 8,
        padding: 14,
        display: 'flex',
        flexDirection: 'column',
        gap: 8
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: 26,
        height: 26,
        borderRadius: 6,
        background: 'var(--muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        width: '70%',
        height: 10,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        width: '90%',
        height: 7,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    })))),
    gallery: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: `repeat(${cols}, 1fr)`,
        gap: 10,
        padding: pad,
        height: '100%',
        boxSizing: 'border-box'
      }
    }, Array.from({
      length: cols * 2
    }).map((_, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        borderRadius: 6,
        background: 'var(--canvas)',
        border: '1px solid var(--border)'
      }
    }))),
    quote: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 10,
        height: '100%',
        padding: pad
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: 'var(--font-serif)',
        fontSize: 18,
        color: 'var(--muted-foreground)'
      }
    }, "\u201C"), /*#__PURE__*/React.createElement("div", {
      style: {
        width: '56%',
        height: 10,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        width: '40%',
        height: 10,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 8,
        alignItems: 'center',
        marginTop: 6
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: 20,
        height: 20,
        borderRadius: 999,
        background: 'var(--muted)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        width: 64,
        height: 7,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    }))),
    cta: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 14,
        height: '100%',
        background: 'var(--primary)',
        borderRadius: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: '30%',
        height: 14,
        borderRadius: 4,
        background: 'oklch(1 0 0 / 0.25)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        width: 92,
        height: 26,
        borderRadius: 6,
        background: 'var(--background)'
      }
    })),
    footer: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: `repeat(${cols + 1}, 1fr)`,
        gap: 14,
        padding: pad,
        height: '100%',
        boxSizing: 'border-box'
      }
    }, Array.from({
      length: cols + 1
    }).map((_, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 6
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        width: '55%',
        height: 8,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    }), [0, 1, 2].map(j => /*#__PURE__*/React.createElement("div", {
      key: j,
      style: {
        width: '40%',
        height: 6,
        borderRadius: 3,
        background: 'var(--muted)'
      }
    })))))
  };
  return inner[block.id] || /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100%',
      color: 'var(--muted-foreground)',
      fontSize: 12
    }
  }, block.name);
}
function SitesScreen({
  data,
  bp
}) {
  const [page, setPage] = React.useState(['navbar', 'hero', 'features', 'quote', 'cta', 'footer']);
  const [sel, setSel] = React.useState('hero');
  const [bind, setBind] = React.useState({
    collection: 'products',
    field: 'title'
  });
  const [heights, setHeights] = React.useState({});
  const [heroText, setHeroText] = React.useState(null);
  const width = BP_WIDTH[bp] || 960;
  const blockOf = id => data.blocks.find(b => b.id === id) || {
    id,
    name: id,
    height: 200
  };
  const addBlock = b => setPage(p => [...p.slice(0, -1), b.id, p[p.length - 1]]);
  const insertAt = (i, id) => setPage(p => [...p.slice(0, i), id, ...p.slice(i)]);
  // Hero heading fills from the bound collection field; inline edits override.
  const boundValue = bind.collection && bind.field && data.products.records[0] ? String(data.products.records[0][bind.field] != null ? data.products.records[0][bind.field] : '') : '';
  const heroHeading = heroText != null ? heroText : boundValue || 'Design tools, assembled';
  const selIdx = page.indexOf(sel);
  const removeBlock = () => {
    if (selIdx < 0) return;
    setPage(p => p.filter((_, i) => i !== selIdx));
    setSel(null);
  };
  const moveBlock = dir => {
    const j = selIdx + dir;
    if (selIdx < 0 || j < 0 || j >= page.length) return;
    setPage(p => {
      const n = [...p];
      const t = n[selIdx];
      n[selIdx] = n[j];
      n[j] = t;
      return n;
    });
  };
  const hOf = id => heights[id] != null ? heights[id] : Math.max(56, (blockOf(id).height || 200) * (bp === 'phone' ? 0.7 : 0.8));
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(StPanel, {
    side: "left",
    width: 232
  }, /*#__PURE__*/React.createElement(StLabel, {
    eyebrow: true,
    style: {
      padding: '8px 12px 2px',
      display: 'block'
    }
  }, "Pages"), /*#__PURE__*/React.createElement(StPages, {
    pages: [{
      id: 'home',
      name: 'Home'
    }, {
      id: 'about',
      name: 'About'
    }, {
      id: 'blog',
      name: 'Blog'
    }],
    selectedId: "home",
    onSelect: () => {}
  }), /*#__PURE__*/React.createElement(StSep, null), /*#__PURE__*/React.createElement(StGrid, {
    title: "Blocks",
    count: data.blocks.length
  }, data.blocks.map(b => /*#__PURE__*/React.createElement(StItem, {
    key: b.id,
    name: b.name,
    icon: b.icon || 'rectangle',
    onInsert: () => addBlock(b)
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      overflow: 'auto',
      background: 'var(--canvas)',
      display: 'flex',
      justifyContent: 'center',
      padding: '28px 24px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width,
      maxWidth: '100%',
      flex: 'none',
      display: 'flex',
      flexDirection: 'column',
      background: 'var(--frame)',
      border: '1px solid var(--frame-border)',
      borderRadius: 6,
      overflow: 'hidden',
      height: 'fit-content',
      boxShadow: 'var(--shadow-sm)'
    }
  }, page.map((id, i) => {
    const b = blockOf(id);
    const selected = sel === id;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: `${id}-${i}`
    }, /*#__PURE__*/React.createElement("div", {
      onClick: () => setSel(id),
      style: {
        position: 'relative',
        height: hOf(id),
        borderBottom: '1px solid var(--border)',
        cursor: 'default',
        outline: selected ? '1.5px solid var(--selection)' : 'none',
        outlineOffset: -1.5
      }
    }, /*#__PURE__*/React.createElement(BlockSkeleton, {
      block: b,
      bp: bp,
      heroHeading: id === 'hero' ? heroHeading : undefined,
      onHeroEdit: id === 'hero' ? t => setHeroText(t) : undefined
    }), selected ? /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        left: 0,
        top: 0,
        background: 'var(--selection)',
        color: 'var(--selection-foreground)',
        fontSize: 10,
        fontFamily: 'var(--font-mono)',
        padding: '2px 6px',
        borderRadius: '0 0 4px 0',
        zIndex: 1
      }
    }, b.name) : null, /*#__PURE__*/React.createElement("button", {
      type: "button",
      title: "Insert features block here",
      onClick: e => {
        e.stopPropagation();
        insertAt(i + 1, 'features');
      },
      style: {
        position: 'absolute',
        left: '50%',
        bottom: -9,
        transform: 'translateX(-50%)',
        zIndex: 2,
        width: 18,
        height: 18,
        borderRadius: 999,
        border: '1px solid var(--border)',
        background: 'var(--background)',
        color: 'var(--muted-foreground)',
        fontSize: 12,
        lineHeight: 1,
        cursor: 'pointer',
        opacity: 0,
        transition: 'opacity var(--duration-fast) var(--ease-standard)'
      },
      onPointerEnter: e => {
        e.currentTarget.style.opacity = 1;
      },
      onPointerLeave: e => {
        e.currentTarget.style.opacity = 0;
      }
    }, "+")));
  }))), /*#__PURE__*/React.createElement(StPanel, {
    side: "right",
    width: 252
  }, /*#__PURE__*/React.createElement(StPane, {
    title: "Block",
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(StIconBtnX, {
      name: "chevron-up",
      label: "Move up",
      size: "sm",
      tooltip: false,
      disabled: selIdx <= 0,
      onClick: () => moveBlock(-1)
    }), /*#__PURE__*/React.createElement(StIconBtnX, {
      name: "chevron-down",
      label: "Move down",
      size: "sm",
      tooltip: false,
      disabled: selIdx < 0 || selIdx >= page.length - 1,
      onClick: () => moveBlock(1)
    }), /*#__PURE__*/React.createElement(StIconBtnX, {
      name: "delete",
      label: "Remove block",
      size: "sm",
      tooltip: false,
      disabled: selIdx < 0,
      onClick: removeBlock
    }))
  }, /*#__PURE__*/React.createElement(StSelect, {
    label: "Section",
    options: page.map(id => blockOf(id).name),
    value: sel ? blockOf(sel).name : '',
    onChange: e => {
      const found = page.find(id => blockOf(id).name === e.target.value);
      if (found) setSel(found);
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "dsr-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "dsr-field__label"
  }, "Height"), /*#__PURE__*/React.createElement(StNum, {
    label: "H",
    unit: "px",
    value: sel ? Math.round(hOf(sel)) : 0,
    min: 56,
    step: 8,
    title: "Block height",
    onChange: h => sel && setHeights(hs => ({
      ...hs,
      [sel]: h
    }))
  }))), /*#__PURE__*/React.createElement(StPane, {
    title: "CMS binding"
  }, /*#__PURE__*/React.createElement(StPicker, {
    collections: [data.products],
    value: bind,
    onChange: setBind
  }), bind.collection && bind.field ? /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 4
    }
  }, /*#__PURE__*/React.createElement(StBound, {
    collection: bind.collection,
    field: bind.field,
    onUnbind: () => setBind({})
  })) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      color: 'var(--muted-foreground)',
      lineHeight: 1.4,
      paddingTop: 2
    }
  }, "El heading del hero se llena con el campo vinculado (ed\xEDtalo inline sobre la p\xE1gina)."))));
}
Object.assign(window, {
  SitesScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/screen-sites.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/shell.jsx
try { (() => {
const {
  Menubar,
  Tabs,
  Button,
  IconButton,
  AvatarStack,
  CommandPalette,
  Toast,
  Menu
} = window.DesignerDesignSystem_6adbd8;

/** EditorShell — menubar + mode tabs + file switcher + ⌘K, wraps the active screen. */
function EditorShell({
  mode,
  onMode,
  kb,
  docName,
  files,
  fileId,
  onOpenFile,
  onNewFile,
  onWorkspace,
  center,
  right,
  toast,
  children
}) {
  const [cmdOpen, setCmdOpen] = React.useState(false);
  const [fileMenu, setFileMenu] = React.useState(false);
  React.useEffect(() => {
    const h = e => {
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') {
        e.preventDefault();
        setCmdOpen(o => !o);
      }
    };
    window.addEventListener('keydown', h);
    return () => window.removeEventListener('keydown', h);
  }, []);
  const modes = [{
    value: 'design',
    label: 'Design'
  }, {
    value: 'components',
    label: 'Components'
  }, {
    value: 'board',
    label: 'Board'
  }, {
    value: 'slides',
    label: 'Slides'
  }, {
    value: 'sites',
    label: 'Sites'
  }, {
    value: 'buzz',
    label: 'Buzz'
  }, {
    value: 'make',
    label: 'Make'
  }, {
    value: 'dev',
    label: 'Dev'
  }];
  const fileItems = (files || []).map(f => ({
    id: f.id,
    title: f.name,
    icon: f.id === fileId ? 'check' : 'file-text'
  })).concat(['-', {
    id: '__new',
    title: 'New file',
    icon: 'add'
  }, {
    id: '__ws',
    title: 'Workspace',
    icon: 'folder'
  }]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      height: '100%',
      position: 'relative',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(Menubar, {
    wordmark: /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 2
      }
    }, onWorkspace ? /*#__PURE__*/React.createElement(IconButton, {
      name: "folder",
      label: "Workspace",
      onClick: onWorkspace
    }) : null, "Designer"),
    doc: docName,
    docMeta: "saved",
    left: /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'relative'
      }
    }, /*#__PURE__*/React.createElement(IconButton, {
      name: "chevrons-updown",
      label: "Switch file",
      size: "sm",
      onClick: () => setFileMenu(o => !o)
    }), fileMenu ? /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        left: 0,
        top: 'calc(100% + 6px)',
        zIndex: 80
      },
      onPointerLeave: () => setFileMenu(false)
    }, /*#__PURE__*/React.createElement(Menu, {
      items: fileItems,
      onSelect: it => {
        setFileMenu(false);
        if (it.id === '__ws' && onWorkspace) onWorkspace();else if (it.id === '__new' && onNewFile) onNewFile();else if (onOpenFile) onOpenFile(it.id);
      }
    })) : null),
    center: center || /*#__PURE__*/React.createElement(Tabs, {
      items: modes,
      value: mode,
      onChange: onMode
    }),
    right: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(AvatarStack, {
      users: window.EDITOR_DATA.users,
      max: 3,
      size: "sm"
    }), /*#__PURE__*/React.createElement(IconButton, {
      name: "search",
      label: "Command palette \u2014 \u2318K",
      onClick: () => setCmdOpen(true)
    }), right)
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      minHeight: 0,
      position: 'relative'
    }
  }, children), /*#__PURE__*/React.createElement(CommandPalette, {
    groups: kb.groups,
    open: cmdOpen,
    onClose: () => setCmdOpen(false),
    onRun: cmd => {
      if (cmd.id && cmd.id.indexOf('tool.') === 0 && window.EDITOR_ON_TOOL) window.EDITOR_ON_TOOL(cmd.id.slice(5));
    }
  }), toast ? /*#__PURE__*/React.createElement("div", {
    className: "dsr-toaststack"
  }, /*#__PURE__*/React.createElement(Toast, {
    action: toast.action,
    onAction: toast.onAction
  }, toast.text)) : null);
}
Object.assign(window, {
  EditorShell
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/shell.jsx", error: String((e && e.message) || e) }); }

// ui_kits/editor/store.js
try { (() => {
/* Shared workspace store — plain JS, used by BOTH the root workspace
   (index.html) and the editor. Persists to localStorage (never cleared). */
(function () {
  var KEY = 'dsrEditor.v1';
  var DEMO_ELS = {
    cover: {
      type: 'frame',
      name: 'Cover',
      x: 0,
      y: 0,
      w: 420,
      h: 300
    },
    title: {
      type: 'text',
      name: 'Title',
      parent: 'cover',
      x: 24,
      y: 24,
      w: 300,
      h: 40,
      text: 'Arc Lounge Chair',
      binding: 'title',
      size: 24
    },
    hero: {
      type: 'rectangle',
      name: 'Hero block',
      parent: 'cover',
      x: 24,
      y: 84,
      w: 372,
      h: 190,
      fillToken: 'accent',
      fill: '#d97757',
      shadows: [{
        x: 0,
        y: 12,
        blur: 32,
        spread: -8,
        color: '#000000',
        alpha: 28
      }]
    },
    photo: {
      type: 'image',
      name: 'Photo',
      x: 480,
      y: 0,
      w: 300,
      h: 220
    },
    price: {
      type: 'text',
      name: 'Price',
      x: 480,
      y: 240,
      w: 120,
      h: 30,
      text: '640 €',
      mono: true,
      size: 16
    },
    card: {
      type: 'frame',
      name: 'Product card',
      x: 0,
      y: 360,
      w: 260,
      h: 150,
      radiusToken: true,
      layout: {
        direction: 'column',
        gap: 12,
        padding: 16,
        align: 'start'
      },
      shadows: [{
        x: 0,
        y: 4,
        blur: 16,
        spread: -6,
        color: '#000000',
        alpha: 16
      }],
      radius: 12
    },
    cardBadge: {
      type: 'component',
      name: 'Badge',
      parent: 'card',
      component: 'Badge',
      x: 16,
      y: 16,
      w: 76,
      h: 18,
      props: {
        children: 'In stock',
        variant: 'selection'
      }
    },
    cardBtn: {
      type: 'component',
      name: 'Buy button',
      parent: 'card',
      component: 'Button',
      x: 16,
      y: 46,
      w: 120,
      h: 28,
      props: {
        children: 'Buy · 640 €',
        variant: 'primary',
        size: 'md'
      }
    }
  };
  var DEMO_ORDER = ['cover', 'title', 'hero', 'photo', 'price', 'card', 'cardBadge', 'cardBtn'];
  var THEME_DEFAULTS = {
    mode: 'light',
    accent: '#d97757',
    radius: 'md',
    font: 'geist',
    spacing: 4,
    scale: 1
  };
  function now() {
    return Date.now();
  }
  function uid() {
    return 'f' + now().toString(36) + Math.floor(Math.random() * 999).toString(36);
  }
  function seed() {
    var a = {
      id: 'q3-campaign',
      name: 'Q3 campaign',
      els: DEMO_ELS,
      order: DEMO_ORDER,
      theme: THEME_DEFAULTS,
      updatedAt: now()
    };
    var b = {
      id: uid(),
      name: 'Untitled',
      els: {},
      order: [],
      theme: THEME_DEFAULTS,
      updatedAt: now() - 1000
    };
    var store = {
      files: {},
      fileOrder: [a.id, b.id],
      lastOpen: a.id
    };
    store.files[a.id] = a;
    store.files[b.id] = b;
    return store;
  }
  function load() {
    try {
      var raw = localStorage.getItem(KEY);
      if (raw) {
        var s = JSON.parse(raw);
        if (s && s.files && s.fileOrder && s.fileOrder.length) return s;
      }
    } catch (e) {/* corrupted → reseed below without clearing other keys */}
    var fresh = seed();
    save(fresh);
    return fresh;
  }
  function save(store) {
    try {
      localStorage.setItem(KEY, JSON.stringify(store));
    } catch (e) {/* quota — ignore */}
    return store;
  }
  function newFile(store, name) {
    var f = {
      id: uid(),
      name: name || 'Untitled',
      els: {},
      order: [],
      theme: THEME_DEFAULTS,
      updatedAt: now()
    };
    store.files[f.id] = f;
    store.fileOrder = [f.id].concat(store.fileOrder);
    store.lastOpen = f.id;
    save(store);
    return f;
  }
  function patchFile(store, id, patch) {
    var f = store.files[id];
    if (!f) return store;
    Object.assign(f, patch, {
      updatedAt: now()
    });
    return save(store);
  }
  function removeFile(store, id) {
    delete store.files[id];
    store.fileOrder = store.fileOrder.filter(function (x) {
      return x !== id;
    });
    if (store.lastOpen === id) store.lastOpen = store.fileOrder[0] || null;
    return save(store);
  }
  window.EDITOR_STORE = {
    KEY: KEY,
    THEME_DEFAULTS: THEME_DEFAULTS,
    load: load,
    save: save,
    newFile: newFile,
    patchFile: patchFile,
    removeFile: removeFile
  };
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/store.js", error: String((e && e.message) || e) }); }

// ui_kits/editor/theme-panel.jsx
try { (() => {
/* Theme panels — shared shell used by BOTH the project-variables panel
   and the editor-chrome panel. Two tabs: Theme (curated) · Variables
   (Figma-style typed table). Built only with chrome components. */
const {
  Label: TpLabel,
  Tabs: TpTabs,
  Slider: TpSlider,
  Button: TpButton,
  IconButton: TpIconBtn,
  ColorField: TpColor,
  NumberField: TpNum,
  Icon: TpIcon,
  SelectField: TpSelect
} = window.DesignerDesignSystem_6adbd8;
const PROJECT_THEME_DEFAULTS = {
  mode: 'light',
  accent: '#d97757',
  radius: 'md',
  font: 'geist',
  spacing: 4,
  scale: 1
};
const THEME_ACCENTS = ['#6b6b6b', '#4864c8', '#7a5cd6', '#a855f7', '#16a34a', '#e11d48', '#ea580c', '#e8a020', '#0891b2', '#d97757'];
const THEME_RADIUS_PX = {
  none: 0,
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 999
};
const THEME_FONTS = {
  geist: "'Geist', system-ui, sans-serif",
  serif: "'Source Serif 4', Georgia, serif",
  mono: "'Geist Mono', ui-monospace, monospace",
  system: 'system-ui, sans-serif'
};
const themeDoc = theme => theme.mode === 'dark' ? {
  surface: '#26241f',
  text: '#f2f0ec',
  muted: '#3a3833'
} : {
  surface: '#ffffff',
  text: '#1a1a1a',
  muted: '#eceae6'
};
function TpSection({
  title,
  right,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement(TpLabel, {
    eyebrow: true
  }, title), right ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      color: 'var(--muted-foreground)'
    }
  }, right) : null), children);
}

/** Curated theme controls (mode/accent/radius/font/spacing/scale). */
function ThemeControls({
  theme,
  defaults,
  onChange
}) {
  const t = {
    ...defaults,
    ...theme
  };
  const set = p => onChange({
    ...t,
    ...p
  });
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TpSection, {
    title: "Mode"
  }, /*#__PURE__*/React.createElement(TpTabs, {
    full: true,
    value: t.mode,
    onChange: mode => set({
      mode
    }),
    items: [{
      value: 'light',
      label: 'Light'
    }, {
      value: 'dark',
      label: 'Dark'
    }]
  })), /*#__PURE__*/React.createElement(TpSection, {
    title: "Accent"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6,
      flexWrap: 'wrap'
    }
  }, THEME_ACCENTS.map(c => /*#__PURE__*/React.createElement("button", {
    key: c,
    type: "button",
    title: c,
    onClick: () => set({
      accent: c
    }),
    style: {
      width: 26,
      height: 26,
      borderRadius: 999,
      border: 'none',
      cursor: 'pointer',
      background: c,
      padding: 0,
      boxShadow: t.accent === c ? '0 0 0 2px var(--popover), 0 0 0 4px var(--foreground)' : 'inset 0 0 0 1px oklch(0 0 0 / 0.15)'
    }
  })))), /*#__PURE__*/React.createElement(TpSection, {
    title: "Radius",
    right: `${THEME_RADIUS_PX[t.radius]}px`
  }, /*#__PURE__*/React.createElement(TpTabs, {
    full: true,
    value: t.radius,
    onChange: radius => set({
      radius
    }),
    items: Object.keys(THEME_RADIUS_PX).map(r => ({
      value: r,
      label: r
    }))
  })), /*#__PURE__*/React.createElement(TpSection, {
    title: "Font"
  }, /*#__PURE__*/React.createElement(TpTabs, {
    full: true,
    value: t.font,
    onChange: font => set({
      font
    }),
    items: [{
      value: 'geist',
      label: 'Geist'
    }, {
      value: 'serif',
      label: 'Serif'
    }, {
      value: 'mono',
      label: 'Mono'
    }, {
      value: 'system',
      label: 'System'
    }]
  })), /*#__PURE__*/React.createElement(TpSection, {
    title: "Spacing",
    right: `${t.spacing}px base`
  }, /*#__PURE__*/React.createElement(TpSlider, {
    min: 2,
    max: 8,
    step: 1,
    value: t.spacing,
    onChange: e => set({
      spacing: Number(e.target.value)
    }),
    "aria-label": "Spacing base"
  })), /*#__PURE__*/React.createElement(TpSection, {
    title: "Scale",
    right: `${Number(t.scale).toFixed(2)}×`
  }, /*#__PURE__*/React.createElement(TpSlider, {
    min: 0.8,
    max: 1.4,
    step: 0.05,
    value: t.scale,
    onChange: e => set({
      scale: Number(e.target.value)
    }),
    "aria-label": "Type scale"
  })));
}

/** Figma-style variables table: grouped rows, type icon, mono name, typed editor. */
function VarsTable({
  groups,
  values,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, groups.map(g => /*#__PURE__*/React.createElement("div", {
    key: g.name,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(TpLabel, {
    eyebrow: true
  }, g.name), g.vars.map(v => {
    const val = values[v.key] != null ? values[v.key] : v.default;
    return /*#__PURE__*/React.createElement("div", {
      key: v.key,
      style: {
        display: 'grid',
        gridTemplateColumns: '16px minmax(0,1fr) 108px',
        gap: 6,
        alignItems: 'center',
        minHeight: 26
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-flex',
        color: 'var(--muted-foreground)'
      }
    }, /*#__PURE__*/React.createElement(TpIcon, {
      name: v.type === 'color' ? 'field-color' : 'field-number',
      size: 12
    })), /*#__PURE__*/React.createElement("span", {
      title: v.key,
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 11,
        color: 'var(--foreground)',
        overflow: 'hidden',
        textOverflow: 'ellipsis',
        whiteSpace: 'nowrap'
      }
    }, v.label || v.key), v.type === 'color' ? /*#__PURE__*/React.createElement(TpColor, {
      value: String(val),
      onChange: c => onChange(v.key, c)
    }) : /*#__PURE__*/React.createElement(TpNum, {
      label: v.affix || '',
      unit: v.unit || 'px',
      value: Number(val),
      min: v.min,
      max: v.max,
      step: v.step || 1,
      onChange: n => onChange(v.key, n),
      title: v.label || v.key
    }));
  }))));
}

/** Panel shell: title + tabs (Theme | Variables) + reset, floating popover style. */
function ThemePanelShell({
  title,
  theme,
  defaults,
  onChange,
  varGroups,
  varValues,
  onVarChange,
  onResetVars,
  footnote,
  onClose
}) {
  const [tab, setTab] = React.useState('theme');
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: 276,
      maxHeight: '100%',
      overflowY: 'auto',
      background: 'var(--popover)',
      border: '1px solid var(--border)',
      borderRadius: 'var(--radius-lg)',
      boxShadow: 'var(--shadow-popover)',
      padding: 14,
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--text-base)',
      fontWeight: 'var(--font-weight-semibold)',
      letterSpacing: 'var(--tracking-tight)'
    }
  }, title), /*#__PURE__*/React.createElement(TpIconBtn, {
    size: "sm",
    name: "close",
    label: "Close",
    tooltip: false,
    onClick: onClose
  })), varGroups ? /*#__PURE__*/React.createElement(TpTabs, {
    full: true,
    value: tab,
    onChange: setTab,
    items: [{
      value: 'theme',
      label: 'Theme'
    }, {
      value: 'vars',
      label: 'Variables'
    }]
  }) : null, tab === 'theme' || !varGroups ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(ThemeControls, {
    theme: theme,
    defaults: defaults,
    onChange: onChange
  }), /*#__PURE__*/React.createElement(TpButton, {
    variant: "secondary",
    onClick: () => onChange({
      ...defaults
    })
  }, "Reset to defaults")) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(VarsTable, {
    groups: varGroups,
    values: varValues || {},
    onChange: onVarChange
  }), /*#__PURE__*/React.createElement(TpButton, {
    variant: "secondary",
    onClick: onResetVars
  }, "Reset variables")), footnote ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      color: 'var(--muted-foreground)',
      lineHeight: 1.45
    }
  }, footnote) : null);
}

/** ProjectThemePanel — the DOCUMENT's variables (kept API from before). */
function ProjectThemePanel({
  theme,
  onChange,
  varValues,
  onVarChange,
  onResetVars,
  onClose
}) {
  return /*#__PURE__*/React.createElement(ThemePanelShell, {
    title: "Project variables",
    theme: theme,
    defaults: PROJECT_THEME_DEFAULTS,
    onChange: onChange,
    varGroups: PROJECT_VAR_GROUPS,
    varValues: varValues,
    onVarChange: onVarChange,
    onResetVars: onResetVars,
    footnote: "Afecta al documento (fills vinculados, radius, tipograf\xEDa, espaciado). El chrome del editor no cambia.",
    onClose: onClose
  });
}

/* Project raw variables (Variables tab) — override the curated theme. */
const PROJECT_VAR_GROUPS = [{
  name: 'Color',
  vars: [{
    key: 'accent',
    label: 'accent',
    type: 'color',
    default: '#d97757'
  }, {
    key: 'surface',
    label: 'surface',
    type: 'color',
    default: '#ffffff'
  }, {
    key: 'text',
    label: 'text',
    type: 'color',
    default: '#1a1a1a'
  }]
}, {
  name: 'Dimension',
  vars: [{
    key: 'radiusPx',
    label: 'radius',
    type: 'number',
    unit: 'px',
    min: 0,
    max: 999,
    default: 8
  }, {
    key: 'spacingPx',
    label: 'spacing base',
    type: 'number',
    unit: 'px',
    min: 2,
    max: 12,
    default: 4
  }]
}];
Object.assign(window, {
  ProjectThemePanel,
  ThemePanelShell,
  ThemeControls,
  VarsTable,
  PROJECT_THEME_DEFAULTS,
  PROJECT_VAR_GROUPS,
  THEME_RADIUS_PX,
  THEME_FONTS,
  THEME_ACCENTS,
  themeDoc
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/editor/theme-panel.jsx", error: String((e && e.message) || e) }); }

__ds_ns.ActionAlign = __ds_scope.ActionAlign;

__ds_ns.ActionFill = __ds_scope.ActionFill;

__ds_ns.ActionOpacity = __ds_scope.ActionOpacity;

__ds_ns.ActionRotate = __ds_scope.ActionRotate;

__ds_ns.AvatarStack = __ds_scope.AvatarStack;

__ds_ns.CommentPin = __ds_scope.CommentPin;

__ds_ns.CursorChip = __ds_scope.CursorChip;

__ds_ns.CommandPalette = __ds_scope.CommandPalette;

__ds_ns.ShortcutsOverlay = __ds_scope.ShortcutsOverlay;

__ds_ns.BoundField = __ds_scope.BoundField;

__ds_ns.CollectionList = __ds_scope.CollectionList;

__ds_ns.CollectionTable = __ds_scope.CollectionTable;

__ds_ns.DataSourcePicker = __ds_scope.DataSourcePicker;

__ds_ns.Canvas = __ds_scope.Canvas;

__ds_ns.Filmstrip = __ds_scope.Filmstrip;

__ds_ns.FloatingToolbar = __ds_scope.FloatingToolbar;

__ds_ns.Frame = __ds_scope.Frame;

__ds_ns.LayerTree = __ds_scope.LayerTree;

__ds_ns.Menubar = __ds_scope.Menubar;

__ds_ns.PageList = __ds_scope.PageList;

__ds_ns.Pane = __ds_scope.Pane;

__ds_ns.Panel = __ds_scope.Panel;

__ds_ns.StatusBar = __ds_scope.StatusBar;

__ds_ns.Toolbar = __ds_scope.Toolbar;

__ds_ns.ZoomControls = __ds_scope.ZoomControls;

__ds_ns.BreakpointSwitcher = __ds_scope.BreakpointSwitcher;

__ds_ns.DEVICE_SIZES = __ds_scope.DEVICE_SIZES;

__ds_ns.DeviceFrame = __ds_scope.DeviceFrame;

__ds_ns.BooleanField = __ds_scope.BooleanField;

__ds_ns.ColorField = __ds_scope.ColorField;

__ds_ns.NumberField = __ds_scope.NumberField;

__ds_ns.PositionField = __ds_scope.PositionField;

__ds_ns.SelectField = __ds_scope.SelectField;

__ds_ns.TextField = __ds_scope.TextField;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.ICON_ROLE_GROUPS = __ds_scope.ICON_ROLE_GROUPS;

__ds_ns.ICON_ROLES = __ds_scope.ICON_ROLES;

__ds_ns.AssetGrid = __ds_scope.AssetGrid;

__ds_ns.LibraryItem = __ds_scope.LibraryItem;

__ds_ns.ConnectionLayer = __ds_scope.ConnectionLayer;

__ds_ns.InteractionEditor = __ds_scope.InteractionEditor;

__ds_ns.TokenList = __ds_scope.TokenList;

__ds_ns.TokenRow = __ds_scope.TokenRow;

__ds_ns.TokenSwatch = __ds_scope.TokenSwatch;

__ds_ns.PRESENCE_COLORS = __ds_scope.PRESENCE_COLORS;

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.Dialog = __ds_scope.Dialog;

__ds_ns.IconButton = __ds_scope.IconButton;

__ds_ns.Input = __ds_scope.Input;

__ds_ns.Kbd = __ds_scope.Kbd;

__ds_ns.Label = __ds_scope.Label;

__ds_ns.Menu = __ds_scope.Menu;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Separator = __ds_scope.Separator;

__ds_ns.Slider = __ds_scope.Slider;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.Tabs = __ds_scope.Tabs;

__ds_ns.Toast = __ds_scope.Toast;

__ds_ns.Tooltip = __ds_scope.Tooltip;

__ds_ns.FilterStack = __ds_scope.FilterStack;

__ds_ns.GradientCss = __ds_scope.GradientCss;

__ds_ns.GradientEditor = __ds_scope.GradientEditor;

__ds_ns.ShapeLibrary = __ds_scope.ShapeLibrary;

})();
