Comment marker on the canvas (teardrop pin, presence color); click toggles the thread.

```jsx
<CommentPin name="Ana Kovač" time="2h" x={300} y={140} open={openId==='c1'} onClick={() => toggle('c1')}
  text="Can we try the serif here?" />
```
