Keyboard shortcut caps for menus, palette rows, and tooltips; tokens map to glyphs (mod→⌘).

```jsx
<Kbd keys={['mod','K']} />   {/* ⌘ K */}
<Kbd>V</Kbd>
```

Shortcut definitions live in `data/keybindings.json` — render those, don't hardcode.
