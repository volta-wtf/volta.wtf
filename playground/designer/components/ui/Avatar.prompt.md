People disc; `presence` adds the multiplayer ring (color hashes from the name — same everywhere).

```jsx
<Avatar name="Maya Lindqvist" src={maya} presence />
<Avatar name="Diego Ferrer" size="sm" />
```
