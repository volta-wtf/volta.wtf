Status/count chip: "New", "Updated", "4", field types, bound-state.

```jsx
<Badge>Updated</Badge>
<Badge variant="outline">12 layers</Badge>
<Badge variant="selection">bound · title</Badge>
```

`selection` marks data bindings; `brand` is for editorial surfaces only.
