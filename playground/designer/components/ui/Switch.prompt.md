On/off toggle that applies immediately (visibility, clipping, autosave); checkbox is for selections that get submitted.

```jsx
<Switch label="Clip content" defaultChecked />
<Switch label="Show grid" />
```
