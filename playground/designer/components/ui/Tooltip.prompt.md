Hover label for icon-only controls; include the shortcut in the text ("Select — V"). IconButton already has this built in via its `label`.

```jsx
<Tooltip label="Zoom to fit — ⇧1"><Button variant="ghost" size="sm">Fit</Button></Tooltip>
```
