Docked strip of tool IconButtons; exactly one has `active`. For FigJam-class centered pills use `FloatingToolbar`.

```jsx
<Toolbar>
  <IconButton name="select" label="Select — V" active />
  <IconButton name="frame" label="Frame — F" />
  <Separator orientation="vertical" />
  <IconButton name="text" label="Text — T" />
</Toolbar>
```
