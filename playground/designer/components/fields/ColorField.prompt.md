Color property control: swatch opens the native picker, hex is editable text, alpha is optional.

```jsx
<ColorField defaultValue="#d97757" />
<ColorField value={fill} onChange={setFill} alpha={op} onAlphaChange={setOp} />
```
