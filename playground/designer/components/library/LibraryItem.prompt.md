One insertable tile for the assets panel (drag or click to insert).

```jsx
<LibraryItem name="Star" thumb={<svg viewBox="0 0 100 100" width="28"><path d={starPath} fill="currentColor"/></svg>} onInsert={add} />
<LibraryItem name="Navbar" icon="rectangle" onInsert={addBlock} />
```
