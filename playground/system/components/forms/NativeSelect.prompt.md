**NativeSelect** — a real `<select>` with the design-system chrome. Prefer it over `Select` for long OS-native option lists or mobile.

```jsx
<NativeSelect defaultValue="apple">
  <option value="apple">Apple</option>
  <option value="banana">Banana</option>
</NativeSelect>
```
