**Textarea** — multi-line text field. Same border/focus treatment as `Input`; resizes vertically.

```jsx
<Label htmlFor="msg">Message</Label>
<Textarea id="msg" placeholder="Type your message here." />
```
