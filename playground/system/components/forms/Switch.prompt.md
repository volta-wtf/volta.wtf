**Switch** — instant on/off toggle (no confirm step). Pair with a `Label`.

```jsx
<div style={{display:'flex',gap:8,alignItems:'center'}}>
  <Switch id="airplane" defaultChecked />
  <Label htmlFor="airplane">Airplane mode</Label>
</div>
<Switch disabled />
```
