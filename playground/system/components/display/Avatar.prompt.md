**Avatar** — circular user image; `AvatarFallback` (initials) shows if the image is missing or fails to load. Size via inline `width`/`height` on `Avatar`.

```jsx
<Avatar>
  <AvatarImage src="/user.jpg" alt="Ada Lovelace" />
  <AvatarFallback>AL</AvatarFallback>
</Avatar>
```
