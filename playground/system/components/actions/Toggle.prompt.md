**Toggle** — a button that stays pressed/unpressed. Icon-only toggles need an `aria-label`.

```jsx
<Toggle aria-label="Bold"><Icon name="edit" /></Toggle>
<Toggle variant="outline">Italic</Toggle>
```
