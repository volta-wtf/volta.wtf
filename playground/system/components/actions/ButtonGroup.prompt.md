**ButtonGroup** — joins buttons edge-to-edge (shared borders, rounded ends only). Use `outline` buttons for the classic segmented look.

```jsx
<ButtonGroup>
  <Button variant="outline">Prev</Button>
  <Button variant="outline">Next</Button>
</ButtonGroup>
```
