System -> Constantes de sistemas (escalas tipograficas, familias tipograficas de sistema, etc)
Primitives -> Escalas base del sistema (materiales crudos)
Globals -> Capa semantica de variables
Theme -> Capa de roles
Presets -> Conjunto de css reutilizable

----

# NAMESPACES

# Color
--tone-             ->For color wheel tones
--key-              -> For color palette tones
--tint-             -> For color composition
--color-            -> For theme colors
--color-chart-*
--color-text-*
--color-text-*
--color-icon-*
--color-state-*
--color-scrim-*
--color-backdrop-*
--color-shadow-*
--color-on-*

# Typography
--font-family-*     -> For safe font family
--font-weight-*     -> For primitives font weight
--font-*            -> For font family
--text-*            -> For text sizes
--weight-*          -> For font weights
--leading-*         -> For line spacing
--tracking-*        -> For letter spacings

# Borders
--ring-width-*
--ring-offset-*
--outline-width-*
--outline-offset-*
--border-width-*
--border-style-*
--divider-width-*
--divider-style-*

# Shapes
--radius-*
--shape-*

# Effects
--shadow-*
--inset-shadow-*
--drop-shadow-*
--text-shadow-*
--blur-*

# Spacing
--spacing-*         -> For spacing
--gutter-*          -> For layout gap
--gap-*             -> For component/content gap
--spacing-layout-*
--spacing-component-*
--spacing-object-*
--spacing-control-*
--spacing-element-*

# Screen
--breakpoint-*      -> For screen sizes
--layer-*           -> For elevations

# Sizing
--ratio-*           -> For scale ratios (like typescales)
--aspect-*          -> For aspect ratios (like media frames)

# Motion
--perspective-
--duration-
--translate-
--scale-
--opacity-
--ease-
--animate-


# Theme
--font-brand
--font-content
--font-size
--font-scale

--ring-width
--ring-offset
--outline-width
--outline-offset

--border-width
--border-style
--divider-width
--divider-style
--line-width
--line-style

--spacing
--margin
--gutter
--gap









# Spacing variables


# Scale variables
--scale-text-*
--scale-icon-*
--scale-radius-*
--scale-container-*
