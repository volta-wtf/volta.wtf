# Hoist dependencies to reduce duplication
shamefully-hoist=true
public-hoist-pattern[]=*

# Performance
prefer-frozen-lockfile=false
auto-install-peers=true

# Vercel deployment optimization
node-linker=hoisted
